## 0. Setup: Create Feature Branch (MANDATORY — FIRST STEP)

Context (procedural deviation, recorded honestly per `docs/openspec-tasks-mandatory-steps.md`'s Step 0 requirement): this change's `proposal.md`, `design.md`, and initial `tasks.md` were mistakenly authored on `chore/specboot-adoption-corrections` — the repository rule requiring `feature/[change-name]` before any edit was not followed at proposal time. This was caught by a read-only review before `apply`, and corrected here, before any implementation edit to `SPECBOOT_ADOPTION_GUIDE.md`, by creating the proper feature branch retroactively. The branch did **not** exist before proposal creation; it was created as an explicitly human-approved correction during the planning/proposal phase, not as apply's own execution of Step 0.

- [x] 0.1 Created and switched to `feature/consolidate-specboot-adoption-guide` via `git checkout -b feature/consolidate-specboot-adoption-guide` from HEAD `cca8e7d6c04fd22f52aa76df60b97605376072bb` ("chore: reconcile SpecBoot workflow") on `chore/specboot-adoption-corrections`, after explicit human approval of the recovery plan. Completed as part of this correction, prior to any artifact or guide edit.
- [x] 0.2 Verified after switching: `git branch --show-current` → `feature/consolidate-specboot-adoption-guide`; HEAD unchanged (`cca8e7d6c04fd22f52aa76df60b97605376072bb`); the untracked change directory (`.openspec.yaml`, `proposal.md`, `design.md`, `tasks.md`, `enriched-work-item.md`) and the working-tree `.claude/settings.json` modification carried over intact (branch creation from a commit does not touch untracked files or uncommitted working-tree edits).

## 1. Relocate Historical Prompt Chains (Sections 6, 7, 15) — Design Decision D1

- [x] 1.1 Read Section 6 (`## 6. Adapt the Repository Technical Context`) in full and copy its `### Historical prompt` block (the `HISTORICAL PROMPT — VERIFIED VERBATIM` text) byte-for-byte into a new, clearly labeled historical entry inside Section 20's `### Historical prompt chains not recovered verbatim` framework (or an equivalent adjacent, explicitly non-executable subsection there), naming it so it is traceable back to Section 6.
- [x] 1.2 Remove the `### Historical prompt` block from Section 6's live path, leaving only its `### Consolidated prompt for the clean run` (renamed `### Consolidated prompt` if needed for consistency with Sections 8/9/11/13's heading style) as the section's single live prompt, with a one-line cross-reference to where the relocated historical prompt now lives.
- [x] 1.3 Repeat 1.1–1.2 for Section 7 (`## 7. Configure OpenSpec to Consume docs/ and ai-specs/`).
- [x] 1.4 Repeat 1.1–1.2 for Section 15 (`## 15. Validate Runtime Discovery in a Fresh Client Session`), additionally relocating the `### Observed Kiro result` correction narrative (including the "initially labeled discovery FAIL... corrected result is PASS" text) alongside its historical prompt, since it is reference-run correction evidence, not a live step.
- [x] 1.5 Verify byte-for-byte fidelity of every relocated `HISTORICAL PROMPT — VERIFIED VERBATIM` block by diffing the relocated text against the pre-edit source (`git show HEAD:SPECBOOT_ADOPTION_GUIDE.md` extract) before considering this group complete.
- [x] 1.6 Confirm via `grep -n "HISTORICAL PROMPT — VERIFIED VERBATIM\|Historical Kiro prompt"` that no live numbered section (1–19) still contains a historical-prompt block after this group, and that Sections 6, 7, 8, 9, 11, 13, and 15 each show exactly one prompt block in their live path.

## 2. Author Six-Capability Consolidated Content — Design Decision D2

- [x] 2.1 Re-read `ai-specs/specboot-instructions.md` (the "Usage: Official OpenSpec Workflow" section), `ai-specs/skills/enrich-us/SKILL.md`, `ai-specs/skills/specboot-verify/SKILL.md`, `ai-specs/skills/adversarial-review/SKILL.md`, and `openspec/specs/specboot-verification-workflow/spec.md` immediately before drafting, so the content below is transcribed from disk at edit time, not from memory of this planning session.
- [x] 2.2 Rewrite `Happy Path 2` (`## Happy Path 2 — Daily Request-to-PR-Ready Workflow`) to name and sequence all six capabilities in order — `enrich-us`, propose, apply, `specboot-verify`, independent `adversarial-review`, archive — replacing the current generic flow, while preserving the existing "STOP before push or pull-request creation" / human-approval boundary.
- [x] 2.3 Restructure Section 18 into **six separately headed** consolidated prompt/procedure entries, one per capability — a single combined block covering all six is not acceptable, even if it names them all:
  - [x] 2.3.1 `enrich-us` entry: procedure/prompt for the mandatory pre-proposal refinement gate; states the `READY FOR PROPOSAL` / `NEEDS CLARIFICATION` outcome and that `NEEDS CLARIFICATION` blocks proceeding.
  - [x] 2.3.2 `propose` entry: consolidated prompt for `/opsx:propose` (Claude) / `/opsx-propose` (Kiro), using the enriched artifact as the work-item description per the handoff already documented in `ai-specs/specboot-instructions.md`.
  - [x] 2.3.3 `apply` entry: consolidated prompt/procedure for `/opsx:apply` (Claude) / `/opsx-apply` (Kiro).
  - [x] 2.3.4 `specboot-verify` entry: consolidated prompt/procedure for `/specboot-verify` (identical invocation on both clients); states explicitly that PASS/PASS WITH GAPS makes the change eligible for independent `adversarial-review` **only**, never archive approval by itself, and cross-references the installer-provenance distinction added in Group 4 (capability availability ≠ installer-provisioning provenance).
  - [x] 2.3.5 `adversarial-review` entry: consolidated prompt/procedure for the skill (invoked by description match, ideally from a different session/client); states the exhaustive two-phase verdict mapping (Blocker/Major → FAIL; else Minor-or-unresolved-Question/assumption → PASS WITH GAPS; else → PASS).
  - [x] 2.3.6 archive entry: consolidated prompt/procedure for `/opsx:archive` (Claude) / `/opsx-archive` (Kiro); states archive approval requires **both** gates' PASS/PASS WITH GAPS plus explicit human approval, and that commit/push remain separate, later, explicit approvals — cross-referencing Section 17's existing commit-approval boundary rather than restating it.
- [x] 2.4 Add, to each of the six entries in 2.3 individually (not once for the group as a whole): (a) no unconditional Java, Maven, Node.js, package-manager, frontend/backend, hosting-platform, selected-client, shell, or OS assumption — every technology-specific instruction is explicitly conditional on repository evidence; (b) an instruction that an unknown client, an uncommon stack, or an unrecognized build system falls back to the target repository's README, CI configuration, documentation, and real file evidence rather than assuming Node.js or failing closed; (c) a statement that an unexecuted or failed command is FAIL, never an inferred PASS from empty output.
- [x] 2.5 For every shell example added within the six entries in 2.3, add an explicit Bash/zsh/PowerShell/Windows/macOS/Linux portability note or provide the alternate-shell form, per the guide's own existing Section 22 guidance on this exact failure class; if an entry has no shell example, record that explicitly rather than silently omitting the check.
- [x] 2.6 Preserve Section 18's existing `### Planned pilot request` / `### Pilot validation criteria` subsections as-is (already correctly isolated as reference-run-specific content), updating only their cross-references if the surrounding structure shifts.
- [x] 2.7 Confirm every invocation string and gate statement added in 2.2–2.3 matches `ai-specs/specboot-instructions.md` word-for-word in substance (client-specific syntax, PASS/PASS WITH GAPS/FAIL wording, "NOT yet permitted" phrasing) by direct side-by-side comparison, not paraphrase.
- [x] 2.8 After 2.3–2.7, verify Section 18 contains exactly six distinct `###`-level (or equivalent) prompt/procedure headings, one per capability, and that no capability's content is merged into another's heading or into a single "daily workflow" block.

## 3. Reconcile Prompt Inventory and Validation Status Table (Sections 20–21)

- [x] 3.1 Add the six new consolidated prompts/procedures from Group 2 to Section 20's `### Consolidated prompts` table, and add the three relocated historical prompts from Group 1 to Section 20's `### Historical prompt chains not recovered verbatim` list (per task 1.1–1.4), keeping the table's existing columns and labeling conventions.
- [x] 3.2 Update Section 21's `### Validation Status Table` only where Group 1–2's edits make an existing row inconsistent (for example, clarify that the "Daily request-to-PR-ready workflow" row's `PENDING END-TO-END VALIDATION` status now refers to the six-capability sequence specifically); do not upgrade any row to `PASS` and do not add a new row asserting validation that has not occurred.

## 4. Installer-Provenance Cross-Reference — Design Decision D3

- [x] 4.1 Add a short cross-reference to `ai-specs/specboot-instructions.md`'s "Installer Scope: Claude/Cursor Provisioning Only (Kiro Configured Separately)" note at the point(s) in Sections 2, 3, 5, and 13 where client selection or adapter creation is discussed, stating that capability availability on disk is not evidence of which process provisioned it.
- [x] 4.2 Add a one-line instruction to the relevant `### Live validation record` block(s) in Sections 2, 3, 5, and 13 to record the actual per-client provisioning provenance observed during that adoption (installer-provisioned vs. separately configured), without duplicating the full explanation from 4.1 in each location.

## 5. CodeGraph Skippable-Decision Framing — Design Decision D4

- [x] 5.1 Update Section 4's `**Purpose:**` line and its opening text to state explicitly that Section 4, in its entirety, is skippable when CodeGraph is not being adopted for the target repository, cross-referencing this repository's own `.claude/CLAUDE.md` guidance ("skip CodeGraph entirely... indexing is the user's decision") as the pattern to follow.
- [x] 5.2 Update Section 5's CodeGraph-specific content only — its `### Rules`, the `codegraph install` command block, the interactive-choices table, and their validation/live-validation-record text (everything above the `### Configure Selected-Client Permissions (Early, One-Time)` subsection heading) — to state explicitly that this portion, and only this portion, is conditional on the same CodeGraph decision as Section 4.
- [x] 5.3 Update `Happy Path 1`'s CodeGraph-related lines (`→ initialize CodeGraph`, `→ configure CodeGraph for selected clients`) with a parenthetical or footnote marking them conditional, without renumbering or removing them from the linear diagram; leave `→ configure selected-client permissions` unmarked, since it is not conditional.
- [x] 5.4 Confirm Section 5's `### Configure Selected-Client Permissions (Early, One-Time)` subsection is explicitly and unambiguously stated to remain mandatory for every selected client regardless of the CodeGraph decision — it must never be described, implied, or grouped as skippable alongside 5.2's content.
- [x] 5.5 Grep the edited guide text for any remaining statement that Sections 4 **and** 5 (undifferentiated) can both be skipped entirely, and correct any match found — this was the exact error identified in this change's own correction record and must not survive into the final guide text.

## 6. Kiro Permission YAML Patch — Design Decision D5

- [x] 6.1 Add `openspec status *` and `git check-ignore *` entries to the illustrative Kiro permission YAML code block in Section 5 (`### Configure Selected-Client Permissions (Early, One-Time)`), in the same style and position as the existing `shell` capability's `match` list entries.
- [x] 6.2 Confirm this edit is confined to the Markdown code block inside `SPECBOOT_ADOPTION_GUIDE.md` and does not touch this repository's actual `.kiro/settings/permissions.yaml` — verify via `git status --short -- .kiro/settings/permissions.yaml` showing no change after this group.

## 7. Anchor, Heading, and Count Repair Pass

- [x] 7.1 After Groups 1–6 land, regenerate the heading list (`grep -n '^## ' SPECBOOT_ADOPTION_GUIDE.md`) and confirm the 1–22 section sequence, numbering, and every heading referenced by the Table of Contents are unchanged except where a group above deliberately retitled or added a subsection.
- [x] 7.2 Confirm every `[Text](#anchor)` link in the Table of Contents still resolves to a matching heading; repair any anchor broken by a heading-text change from Groups 1–6.
- [x] 7.3 Confirm no new duplicate heading text was introduced by relocating content in Group 1 (for example, two subsections both titled `### Historical prompt` inside Section 20 without disambiguating context).

## 8. Verification and Validation (MANDATORY)

- [x] 8.1 Run `wc -l SPECBOOT_ADOPTION_GUIDE.md` before starting Group 1 and again after Group 7 completes; record both counts and confirm the delta is consistent with targeted edits, not a truncation or a full-file replacement.
- [x] 8.2 Run `grep -n -i "enrich-us\|specboot-verify\|adversarial-review"` and confirm non-zero matches inside `Happy Path 2` and Section 18 (zero before this change, per this change's own `proposal.md`/`design.md` evidence).
- [x] 8.3 Run `grep -n -i "/new\b\|/ff\b\|opsx:ff\|opsx:continue\|openspec-ff-change\|openspec-continue-change\|/specboot:verify"` across the whole file and confirm every match (if any) sits inside a historical/troubleshooting block, never in a live instruction.
- [x] 8.4 Run `git status --short` and confirm only `SPECBOOT_ADOPTION_GUIDE.md` (plus this change's own `openspec/changes/consolidate-specboot-adoption-guide/` artifacts) shows as changed — no application code, test, canonical skill/agent, adapter, OpenSpec-generated client file, permission file, or global configuration touched.
- [x] 8.5 Run `openspec validate --changes "consolidate-specboot-adoption-guide" --strict` and confirm it passes.
- [x] 8.6 Save the full `git diff -- SPECBOOT_ADOPTION_GUIDE.md` for review and confirm it touches only the sections identified in Groups 1–7 — no unrelated reflow of untouched sections.

## 9. Review and Update Existing Unit Tests (MANDATORY)

- [x] 9.1 Search the existing unit test suite (`src/test/java/...`) for any test that references `SPECBOOT_ADOPTION_GUIDE.md`, its content, or any behavior this change touches: `grep -rn "SPECBOOT_ADOPTION_GUIDE\|adoption guide" src/test`.
- [x] 9.2 Record the result. Expected: zero matches, since this change edits only operational documentation and touches no class, controller, service, repository, or configuration under test. If a match is found, update that test as part of this group before proceeding.
- [x] 9.3 Confirm no test file is modified unless 9.1 found a match requiring it.

## 10. Run Unit Tests and Verify Database State (MANDATORY)

- [x] 10.1 Prepare test environment: capture `git status --short` immediately before running tests as the pre-test baseline (expected: only this change's own in-progress diff and untracked artifacts).
- [x] 10.2 Targeted tests for the changed module: not applicable — no Java source, resource, or configuration module is changed by this change; record this explicitly as the targeted-test result rather than omitting the step.
- [x] 10.3 Run the required broader test suite per this repository's own offline baseline convention: `mvn -o test`. Record exit code, total tests, failures, errors, skips, and runtime.
- [x] 10.4 Verify database state: this project's unit tests manage their own ephemeral/test-scoped persistence (no persistent development database session is mutated by running `mvn -o test`); record this explicitly as the database-state evidence, and confirm `git status --short` after the run shows no unexpected file changes (e.g. no stray `target/` artifacts tracked by Git).
- [x] 10.5 Create the verification report at `openspec/changes/consolidate-specboot-adoption-guide/reports/<YYYY-MM-DD>-step-10-unit-test-and-db-verification.md` — note in the report itself that this path (a `reports/` folder directly under the change directory, not nested under a `specs/` folder) is used because `.openspec.yaml` declares `skip_specs: true` for this change, so no `specs/` directory exists to nest under; flag this path convention as exactly the kind of change-type-awareness gap recorded under "Future governance" below.
- [x] 10.6 Mark this group complete only after `mvn -o test` passes (or any pre-existing, unrelated failure is documented as such with evidence it predates this change) and the report file in 10.5 exists.

## 11. Manual Endpoint Testing with curl (MANDATORY — conditional per endpoint, group not omitted)

- [x] 11.1 Confirm via `git diff -- docs/api-spec.yml` (expected: no diff) that this change adds or modifies zero API endpoints.
- [x] 11.2 Record the "if any" condition as evaluated and empty: zero GET/POST/PUT/PATCH/DELETE endpoints are affected by this change, so no curl command is executed against any endpoint — this is a recorded non-applicability finding for this specific change, not an omission of the group itself.

## 12. E2E Testing with Playwright MCP (MANDATORY if applicable — recorded non-applicability)

- [x] 12.1 Confirm this change has no frontend, browser, or integration behavior: `docs/frontend-standards.md` already states frontend is "Not Applicable" for this backend-only Spring Boot repository, and this change touches only `SPECBOOT_ADOPTION_GUIDE.md`. Record this evidence explicitly; E2E testing is omitted on this documented basis, per the correction that authorized omitting only this step (not the unit-test or curl groups) for this reason.

## 13. Update Technical Documentation (MANDATORY)

- [x] 13.1 Confirm `SPECBOOT_ADOPTION_GUIDE.md` — the technical documentation this change exists to update — reflects all of Groups 1–7's edits, cross-referencing each group's changed section.
- [x] 13.2 Check whether any other technical documentation requires an update as a consequence of this change: `git diff -- docs/api-spec.yml docs/data-model.md docs/backend-standards.md docs/frontend-standards.md docs/documentation-standards.md docs/base-standards.md`. Expected: no diff for any of them, since this change alters no API contract, data model, or coding/documentation standard. Record the exact files checked and the "no change needed" finding.
- [x] 13.3 Confirm this group's evidence, together with Groups 1–7, satisfies the mandatory "Update Technical Documentation" step in full — not by exemption, but because the required update (the guide itself) is this change's own deliverable.

## 14. Post-Review Corrections (Independent Adversarial Review — Minor Findings)

Context: an independent adversarial review of this change's implementation (against Groups 1–13 above) returned verdict `PASS WITH GAPS`, with exactly two Minor findings. Both are corrected here, targeted and guide-only, without rewriting or erasing the evidence recorded in Groups 1–13 above.

- [x] 14.1 **Finding (Minor):** Section 21's Validation Status Table row "CodeGraph initialization and selected-client configuration" recorded an unconditional `PASS` in the Clean-run status column, contradicting Group 5's own CodeGraph-skippable-decision framing (Sections 4/5, Happy Path 1) — a repository that does not adopt CodeGraph would have no evidence to support an unconditional PASS on this row.
  **Resolution:** Corrected the row's Clean-run status cell from `PASS` to `PASS when CodeGraph is adopted for the target repository; N/A when CodeGraph is not adopted`. Table's three-column structure (`Capability | Reference status | Clean-run status`), all other rows, and the table's closing note are unchanged.
- [x] 14.2 **Finding (Minor):** Section 18's `### Live validation record` template used a single generic `Review:` field, insufficient to record evidence for the six required workflow capabilities (`enrich-us`, propose, apply, `specboot-verify`, independent `adversarial-review`, archive) that Group 2 added to this same section.
  **Resolution:** Replaced the single `Review:` line with eight explicit fields: `enrich-us outcome (READY FOR PROPOSAL / NEEDS CLARIFICATION)`, `Proposal approval`, `Apply result`, `specboot-verify verdict (PASS / PASS WITH GAPS / FAIL)`, `Independent adversarial-review verdict (PASS / PASS WITH GAPS / FAIL)`, `Independent adversarial-review provenance (reviewing session/client; cross-session, cross-client, or same-session-fallback)`, `Archive approval (explicit human approval, both gates PASS/PASS WITH GAPS)`, `Archive result`. All other template fields (`Request`, `Change ID`, `Artifacts`, `Implementation`, `Tests`, `Docs/spec sync`, `Commit message`, `PR title`, `PR description`, `Remote mutation attempted`, `Result`, `Prompt corrections required`) are unchanged, in their original order and position.
- [x] 14.3 Re-ran strict OpenSpec validation, and the guide's heading/anchor/duplicate-heading/whitespace/scoped-diff checks (same methodology as Group 7/8), after 14.1–14.2, to confirm both targeted edits did not introduce a structural or scope regression.
- [x] 14.4 Confirmed no application code, canonical skill/agent, client adapter, permission file, OpenSpec-generated client file, or global configuration changed as a result of 14.1–14.2.

## 15. Post-Review Correction (Independent Adversarial Review — Minor Finding)

Context: a further independent adversarial review of this change's implementation (against Groups 1–14 above) returned exactly one Minor finding. Corrected here, targeted and guide-only, without rewriting or erasing the evidence recorded in Groups 1–14 above.

- [x] 15.1 **Finding (Minor):** Section 18's `archive` capability entry described the verification/adversarial-review/human-approval gate ("Archive approval must not be requested or granted while `specboot-verify` or `adversarial-review` has an open Blocker/Major finding") without stating that this gate is a documented workflow convention, not a technical modification of the selected client's installed OpenSpec archive command — a reader could infer `/opsx:archive` / `/opsx-archive` itself was changed to enforce it, contradicting the canonical `specboot-verify` skill and `openspec/specs/specboot-verification-workflow/spec.md`'s "Gate is procedural, not technical" requirement.
  **Resolution:** Inserted one clause into the existing paragraph, between the gate-requirement sentence and the sync-then-archive sentence: "This gate is enforced by the documented SpecBoot workflow convention and human/agent discipline, not by a technical modification — it does not technically modify, disable, or block the selected client's installed OpenSpec archive command, which remains unmodified." The existing archive-eligibility sentence (both gates' PASS/PASS WITH GAPS plus explicit human approval) and the sync-then-archive sentence are unchanged, word-for-word, on either side of the inserted clause. No other sentence in the `archive` entry was touched.
- [x] 15.2 Confirmed `Happy Path 2`'s `→ archive (requires both gates' PASS/PASS WITH GAPS plus explicit human approval)` line states only the eligibility requirement, never claims the installed archive command was technically modified, and therefore does not contradict 15.1's added clause — left unmodified per the instruction to touch `Happy Path 2` only if an actual contradiction would otherwise remain.
- [x] 15.3 Re-ran strict OpenSpec validation, and the guide's heading, anchor, and whitespace checks (same methodology as Group 7/8/14), after 15.1, to confirm the single-clause edit did not introduce a structural or scope regression: `openspec validate --changes "consolidate-specboot-adoption-guide" --strict` → PASS; heading list (`grep -n '^## '`) unchanged (1–22 sequence and Table of Contents intact); anchor check → 143 headings, 39 anchor links, 0 missing; `git diff --check -- SPECBOOT_ADOPTION_GUIDE.md` → clean; line count unchanged at 2045 (the clause was inserted mid-paragraph, not on a new line).
- [x] 15.4 Confirmed via `git status --short` that only `SPECBOOT_ADOPTION_GUIDE.md` and this change's own `openspec/changes/consolidate-specboot-adoption-guide/` artifacts show as changed, and via targeted `git status --short` checks that `.claude/settings.json`, `.kiro/settings/permissions.yaml`, `ai-specs/skills/`, `ai-specs/agents/`, `.cursor/`, `.claude/agents/`, `.claude/skills/`, `.kiro/agents/`, `.kiro/skills/`, `src/`, and `openspec/config.yaml`/`.yml` show zero diff.

## 16. Post-Review Correction (Independent Adversarial Review — Minor Finding)

Context: a further independent adversarial review of this change's implementation (against Groups 1–15 above) returned exactly one Minor finding. Corrected here, targeted and guide-only, without rewriting or erasing the evidence recorded in Groups 1–15 above.

- [x] 16.1 **Finding (Minor):** Section 18's `enrich-us` capability entry described the mandatory pre-proposal refinement gate (its input model, `READY FOR PROPOSAL` / `NEEDS CLARIFICATION` outcome, and invocation model) without stating that `enrich-us` never substitutes for the independent `adversarial-review`, which remains required after implementation and `specboot-verify` — a reader could infer the pre-proposal gate alone satisfies the change's independent-review requirement, contradicting `ai-specs/skills/enrich-us/SKILL.md` ("it is not `adversarial-review`, and it never substitutes for that independent, post-implementation check") and `openspec/specs/specboot-verification-workflow/spec.md`'s requirement that `enrich-us` "SHALL NOT be treated as, or substitute for, `adversarial-review`'s independent-review role."
  **Resolution:** Appended one sentence to the end of the entry's opening paragraph, after the existing invocation-model sentence: "This is a procedural quality gate and structured first review — it never substitutes for the independent `adversarial-review`, which remains required after implementation and `specboot-verify`." Wording is transcribed in substance from the canonical `enrich-us` skill and the verification-workflow spec, not re-derived. The existing input-model description, the `READY FOR PROPOSAL` / `NEEDS CLARIFICATION` outcome paragraph, the persistence/next-action wording, and the `Stack/client conditionality` paragraph are unchanged, word-for-word. No other sentence in the `enrich-us` entry was touched.
- [x] 16.2 Confirmed `Happy Path 2`'s `→ enrich-us (mandatory pre-proposal refinement gate; READY FOR PROPOSAL required, NEEDS CLARIFICATION blocks proceeding)` line states only the gate requirement, never claims `enrich-us` substitutes for `adversarial-review`, and therefore does not contradict 16.1's added sentence — left unmodified per the instruction to touch `Happy Path 2` only if an actual contradiction would otherwise remain.
- [x] 16.3 Re-ran strict OpenSpec validation, and the guide's heading, anchor, and whitespace checks (same methodology as Group 7/8/14/15), after 16.1, to confirm the single-sentence edit did not introduce a structural or scope regression: `openspec validate --changes "consolidate-specboot-adoption-guide" --strict` → PASS; `## ` heading count unchanged at 28, section 1–22 sequence and Table of Contents intact; anchor check → 143 headings, 39 anchor links, 0 missing; `git diff --check -- SPECBOOT_ADOPTION_GUIDE.md` → clean; line count unchanged at 2045 (the sentence was appended mid-paragraph, not on a new line); the added sentence confirmed present exactly once via grep.
- [x] 16.4 Confirmed via `git status --short` that only `SPECBOOT_ADOPTION_GUIDE.md` and this change's own `openspec/changes/consolidate-specboot-adoption-guide/` artifacts show as changed, and via targeted `git status --short` checks that `.claude/settings.json`, `.kiro/settings/permissions.yaml`, `ai-specs/skills/`, `ai-specs/agents/`, `.cursor/`, `.claude/agents/`, `.claude/skills/`, `.kiro/agents/`, `.kiro/skills/`, `src/`, and `openspec/config.yaml`/`.yml` show zero diff.

## Notes (not tracked tasks)

- **Future governance item (recorded, not implemented here):** `docs/openspec-tasks-mandatory-steps.md` currently has no documentation-only exception, and its report-path convention (`specs/<change-name>/reports/`) assumes every change has a `specs/` directory — which a `skip_specs: true` change like this one does not. Task 10.5 above works around this by using `openspec/changes/<change-name>/reports/` directly. A future change should consider making the mandatory task-group rules change-type-aware (for example, an explicit documentation-only or `skip_specs` profile) and should reconcile the report-path convention with `skip_specs: true` changes. This item is recorded for future consideration only; it is not implemented as part of this change.
- Per the request that produced this change's enriched work item and `proposal.md`, this change does not add, implement, or expand `make-specboot-installer-client-aware` (Kiro npm-installer provisioning) or `harden-shared-client-permissions` (permission-prompt-efficiency restructuring) — Group 4 and Group 6 above are strictly a documentation cross-reference and an illustrative example patch, respectively, not implementations of either deferred change.
- This change is expected to go through the same six-capability workflow it documents once implementation is complete — `specboot-verify` → independent `adversarial-review` → explicit human approval → archive — as separate, later, explicitly-approved steps outside this task list's scope.
