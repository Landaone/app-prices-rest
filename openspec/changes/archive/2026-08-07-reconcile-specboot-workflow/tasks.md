## 0. Setup: Create Feature Branch (MANDATORY - FIRST STEP)

- [x] 0.1 Reuse the existing dedicated branch `chore/specboot-adoption-corrections` instead of creating a new `feature/reconcile-specboot-workflow` branch — this branch was created specifically for the ordered SpecBoot adoption corrections, of which this change is one, per explicit user instruction
- [x] 0.2 Verified branch creation and current branch status: `git branch --show-current` returned `chore/specboot-adoption-corrections`; `git status --short` showed only the untracked `openspec/changes/` directory for this change, no other pending state

## 1. Canonical Verification Skill

- [x] 1.1 Wrote `ai-specs/skills/specboot-verify/SKILL.md`, adapted from `AI4Devs-LTI-extended`'s `openspec-verify-change` (1.3.1), rewritten against this repo's actual 1.7 `openspec status --change <name> --json` output shape (verified: `status --json` returns `artifactPaths.<artifact>.existingOutputPaths` directly; the skill reads that, not the 1.3.1 `contextFiles` shape)
- [x] 1.2 Kept the Completeness/Correctness/Coherence dimensions; added the severity-gate section. **Corrected in Group 6** after first independent verification found a Major defect: the original text used a CRITICAL/WARNING/SUGGESTION taxonomy and stated a passing verify made archive approval requestable. Final state (see Group 6): exactly Blocker/Major/Minor severities, and a passing verify only makes the change eligible for independent adversarial review — never archive approval directly
- [x] 1.3 Encoded the verified shell-execution rules as an explicit "Verified shell-execution rules" section in the skill (unexecuted/failed command never PASS; pipeline exit status from the original command; no `npx --yes` fallback; no assumed shell-portable list expansion; expected diffs evaluated by content)
- [x] 1.4 Encoded "Step 5 — Required workflow capability availability" in the skill: derives the selected-client roster from `.claude/`/`.kiro/`/`.cursor/`/`.codex/` presence, checks propose/apply/specboot-verify/adversarial-review/archive per client, documents each client's exact invocation, and states differing invocation names are not themselves a finding

## 2. Client Adapters (Selected Clients Only, SpecBoot-Owned Namespace)

- [x] 2.1 Created `.claude/commands/specboot/verify.md` in new `commands/specboot/` directory (sibling to `commands/opsx/`); delegates to the canonical skill; registers as `/specboot:verify` (confirmed: harness surfaced it as an available skill/command immediately after creation)
- [x] 2.2 Created `.kiro/prompts/specboot-verify.prompt.md`, `specboot-` prefix; delegates to the canonical skill; invocation `/specboot-verify` confirmed by the repo's own naming convention (see 5.3)
- [x] 2.3 Created symlink `.claude/skills/specboot-verify` → `../../ai-specs/skills/specboot-verify`; verified with `readlink` (returns `../../ai-specs/skills/specboot-verify`) and `test -f .claude/skills/specboot-verify/SKILL.md` (passed)
- [x] 2.4 Created symlink `.kiro/skills/specboot-verify` → `../../ai-specs/skills/specboot-verify`; verified with `readlink` and `test -f .kiro/skills/specboot-verify/SKILL.md` (passed)
- [x] 2.5 Ran `openspec --help`, `openspec init --help`, `openspec update --help` (all exit 0), `openspec doctor --json` (exit 0, `healthy: true`, empty `status`), and `grep -i specboot` against the installed package's `README.md` (exit 1 = no match, per the interpretation rule that grep's no-match exit is not a command failure). No collision with any OpenSpec-generated path/name found. No install/download/upgrade performed
- [x] 2.6 Confirmed `.cursor` and `.codex` do not exist in this repo (`test -d` on both → absent) — no adapter created for either; `git status --short` on `.claude/commands/opsx` and `.kiro/prompts/opsx-*` shows no modifications, only new untracked `specboot`-namespaced files

## 3. Adversarial Review Provenance

- [x] 3.1 Added "Step 6 — Reviewer provenance" to `ai-specs/skills/adversarial-review/SKILL.md`: implementing session, reviewing session/mechanism, client used, cross-client status, documented fallback
- [x] 3.2 Updated the `## Output format` template block with a "### Reviewer provenance" block carrying the same fields alongside the existing verdict format

## 4. Documentation Reconciliation

- [x] 4.1 Updated `ai-specs/specboot-instructions.md`: replaced `/new`/`/ff`/`/verify` with `/opsx:propose`, `/opsx:update`, and `/specboot:verify` (Claude) / `/specboot-verify` (Kiro); documented the corrected sequence including human approval and the process-gate language; both clients' archive commands named explicitly
- [x] 4.2 Applied the identical corrections to `packages/specboot/template/ai-specs/specboot-instructions.md`; `diff` between the two files now shows only one remaining difference — a cosmetic Google Drive reference-image URL, pre-existing and unrelated to command reconciliation (expected non-zero diff, evaluated by content per the shell-execution rule, not treated as a failure)
- [x] 4.3 Added a "Permission Setup (One-Time)" cross-reference section to both `specboot-instructions.md` copies, pointing at `SPECBOOT_ADOPTION_GUIDE.md`'s §5 one-time setup and §19 reference policy, without altering the permission approach itself

## 5. Verification and Validation (MANDATORY)

- [x] 5.1 Ran `openspec validate --changes "reconcile-specboot-workflow" --strict` → exit 0, "1 passed, 0 failed"
- [x] 5.2 Dry-ran the SpecBoot verify skill's Step 1 (`openspec status --change "reconcile-specboot-workflow" --json` → exit 0, all 4 artifacts `done`) and confirmed its severity-gate wording (in `ai-specs/skills/specboot-verify/SKILL.md`) states the process-gate distinction explicitly rather than implying a technical archive block
- [x] 5.3 Confirmed all 5 required capabilities (propose/apply/specboot-verify/adversarial-review/archive) exist as files for both clients via `test -f`/`test -L`; exact invocations: Claude `/opsx:propose`, `/opsx:apply`, `/specboot:verify`, `/opsx:archive`, `adversarial-review` (skill); Kiro `/opsx-propose`, `/opsx-apply`, `/specboot-verify`, `/opsx-archive`, `adversarial-review` (skill)
- [x] 5.4 Re-ran the task 2.5 checks after all adapters existed: `openspec --help` (exit 0, no `specboot` match), `openspec doctor --json` (exit 0, still `healthy: true`), directory listing confirms `commands/specboot/verify.md` and `specboot-verify.prompt.md` are structurally and nominally distinct from every `opsx`/`opsx-*` generated file
- [x] 5.5 Ran `git status --short`: 3 modified files and 6 new untracked paths, exactly matching `proposal.md`'s Impact section, plus the pre-existing untracked `openspec/changes/` change directory

## 6. Post-Verification Corrections (Reopened After First Independent Verification)

Context: the first independent `/specboot:verify` run (see `design.md` — "Runtime discovery evidence") self-reported `PASS WITH WARNINGS` but its own report stated archive approval could be requested directly after that passing verify — a Major defect, since the mandatory sequence requires an independent adversarial-review PASS **and** explicit human approval before archive approval, not a verify PASS alone. This group records why the already-checked Groups 1–5 above were reopened and what changed, rather than silently editing them.

- [x] 6.1 Rewrote `ai-specs/skills/specboot-verify/SKILL.md`'s severity/verdict system to use exactly Blocker/Major/Minor (no CRITICAL/WARNING/SUGGESTION alternative taxonomy): any Blocker or Major finding → FAIL; only Minor findings → PASS WITH GAPS; no findings → PASS
- [x] 6.2 Added the exact required strings to the skill's report format: a successful report states "Next gate: independent adversarial review. Archive approval is NOT yet permitted."; a failed report states "Next gate blocked. Correct findings and rerun verification in a fresh session."
- [x] 6.3 Removed all wording that permitted proceeding directly from verify to archive, or requesting archive approval from a verify PASS alone, from the skill, `proposal.md`, `design.md`, and `spec.md`
- [x] 6.4 Updated `proposal.md`, `design.md`, and `specs/specboot-verification-workflow/spec.md` consistently to state two separate, sequential gates (verify → adversarial-review eligibility only; verify PASS + review PASS + explicit human approval → archive approval); added spec scenarios "Verification PASS enables adversarial review only, not archive approval" and "Archive approval requires both gates plus human approval"
- [x] 6.5 Corrected `design.md`'s inaccurate claim that the skill calls `openspec instructions <artifact> --change <name> --json`; the implemented skill only ever called `openspec status --change <name> --json` — the false claim was removed rather than adding an unnecessary command to match it
- [x] 6.6 Added a mandatory "Execution provenance" section to the skill's canonical report format (client, session lifecycle, active agent/mode, agents/subagents actually invoked, canonical skill invocation, client adapter/invocation used, other skills invoked, MCP integrations used, auto-loaded instruction files, manually opened resources, per-client evidence classification), a new spec requirement "Mandatory execution provenance reporting", and provenance-specific guardrails (no catalog-only listing counted as usage, explicit "None" reporting required, no inferred model name, live-runtime vs. static-validation distinction)
- [x] 6.7 Recorded the first independent verification run's actual runtime-discovery evidence in `design.md`: Claude — not visible in a new chat without reload; discovered automatically after `Developer: Reload Window`; invoked with no arguments; auto-selected the sole active change; no command/skill file manually injected; live-runtime discovery PASS after reload; hot discovery (no reload) not observed. Kiro — static filesystem/naming validation only; no live runtime execution observed
- [x] 6.8 Recounted `tasks.md`'s checklist items deterministically (`grep -c '^\- \[[ x]\]' openspec/changes/reconcile-specboot-workflow/tasks.md`) → **22** at the time of the first report, not the 24 the implementing session had stated in chat. Confirmed via `grep -n "24"` across all 4 artifacts that no file contained a stale "24" count to remove — the error was confined to a chat report, not the artifacts, and is corrected in the reporting going forward from this correction
- [x] 6.9 Confirmed this correction pass touched none of the OpenSpec-generated `opsx` files, created no adapter for a client not currently selected, and left the approved thin adapters/symlinks from Group 2 unmodified (`git status --short` after this group shows the same 3 modified docs/skill files as before plus no new/removed paths from Groups 1–2)

## 7. Second-Verification Corrections (Architectural Reassessment — Official Verify Coexistence & Redundant Claude Adapter)

Context: a second independent `/specboot-verify` run correctly returned FAIL, finding: (1) an inaccurate claim that OpenSpec 1.7 lacks verify (it ships an official `/opsx:verify`; this machine's global custom profile excludes it); (2) leftover `Blocker/Major/CRITICAL` mixed-taxonomy text in both `specboot-instructions.md` copies; (3) `adversarial-review`'s Findings table omitting the defined `Question / assumption` severity; (4) the live report's actual Claude invocation (`/specboot-verify` via `.claude/skills/specboot-verify`) not matching the documented `/specboot:verify` command-adapter invocation; (5) the run's session lifecycle was "Resumed session," so it is useful evidence but not the final independent fresh-session PASS. A read-only design reassessment (recorded here) compared the official verify workflow against every SpecBoot verify requirement, evaluated three options, and reached an approved decision before any file was changed.

- [x] 7.1 Read-only reassessment: located and read the installed OpenSpec 1.7 package's official verify template (`dist/core/templates/workflows/verify-change.js`, found via `find`, exit 0); confirmed via `openspec config list` (exit 0) that this machine's profile is `profile: custom (explicit)` with `workflows: propose, explore, apply, update, sync, archive (explicit)` — a global setting in `~/.config/openspec/config.json`, not `openspec/config.yaml` (confirmed no `profile`/`workflows` keys there via `grep`, exit 1 = no match); confirmed via `dist/core/profiles.js`/`.d.ts` that `custom` profile workflows are an arbitrary subset of `ALL_WORKFLOWS`, so `verify` could be added without enabling `new`/`continue`/`ff`/`onboard`/`bulk-archive`
- [x] 7.2 Compared the official verify template against every SpecBoot verify requirement (completeness, correctness, coherence, real exit codes, severity gate, next-gate policy, execution provenance, per-client capability validation, adapter safety): official covers only completeness/correctness/coherence, uses a CRITICAL/WARNING/SUGGESTION taxonomy, and concludes "Ready for archive" directly — **no** equivalent of the other six requirements exists in it
- [x] 7.3 Evaluated and rejected Option A (enable official `/opsx:verify` alone — its own conclusion bypasses the mandatory sequence and, once OpenSpec-generated, could never be edited to fix that) and Option C (layer SpecBoot policy around the official verifier — would require fully re-deriving/overriding the official report, maintaining two overlapping implementations for no capability gain); approved Option B (retain `/specboot-verify` as the sole, policy-enhanced SpecBoot verifier)
- [x] 7.4 Verified `.claude/commands/specboot/verify.md` was the only entry under `.claude/commands/specboot/` (`find` → 1 file); removed it, then removed the now-empty `.claude/commands/specboot/` directory (`rmdir`, exit 0); confirmed `.claude/commands/opsx/*` (6 files) untouched via `git diff --stat` (empty output, exit 0)
- [x] 7.5 Standardized the documented invocation as `/specboot-verify` for both Claude and Kiro; corrected `design.md`'s runtime-discovery evidence to state the actual observed Claude invocation was `/specboot-verify` via the skill symlink, not `/specboot:verify` via the removed command adapter
- [x] 7.6 Updated `proposal.md`, `design.md`, and `specs/specboot-verification-workflow/spec.md`: replaced the false "OpenSpec 1.7 dropped verify" claim with portable wording (official verify exists; machine-global profile excludes it here; another developer's machine may have it enabled; `/specboot-verify` is authoritative regardless; coexistence is not a failure since namespaces don't collide); recorded the official-vs-SpecBoot capability comparison and the Option A/B/C rejection rationale; added spec scenarios "Official verify coexistence is not a failure" and "Redundant adapter identified and removed"; updated all planned/final affected-path listings to remove the Claude command adapter
- [x] 7.7 Corrected `design.md`'s residual claim that the 1.3.1 `contextFiles` I/O assumption "needs re-validation" against 1.7 — read-only testing (`openspec instructions apply --change "reconcile-specboot-workflow" --json`, exit 0) confirmed it works correctly; the real reason to avoid the official template is its incompatible policy conclusion, not broken I/O
- [x] 7.8 Updated both `ai-specs/specboot-instructions.md` and `packages/specboot/template/ai-specs/specboot-instructions.md` identically: replaced the inaccurate CLI-gap statement with the portable coexistence wording above; removed the leftover `/CRITICAL` from the human-approval step (now reads "Blocker/Major finding"); documented `/specboot-verify` for both selected clients; stated verify PASS only unlocks adversarial review, not archive approval; `diff` between the two files after this change shows only the pre-existing cosmetic image-URL line (exit 1, expected non-zero, evaluated by content, not treated as a failure)
- [x] 7.9 Updated `ai-specs/skills/adversarial-review/SKILL.md`'s Findings table so `Question / assumption` (already defined in Step 4) is representable in the Severity column alongside Blocker/Major/Minor
- [x] 7.10 Confirmed preserved: `ai-specs/skills/specboot-verify/SKILL.md`, `.claude/skills/specboot-verify`, `.kiro/skills/specboot-verify`, `.kiro/prompts/specboot-verify.prompt.md`, and every OpenSpec-generated adapter (`test -f`/`test -L` all passed; `git status --short` shows no `.claude/commands/opsx` or `.kiro/prompts/opsx-*` changes)
- [x] 7.11 Confirmed no machine-global OpenSpec configuration changed and `openspec update` was not run: this reassessment and correction used only `openspec config list`, `openspec instructions apply --json`, `find`, `grep`, `rm`, `rmdir` — no `openspec config set`, no profile switch, no `openspec update`, no install/download/web access
- [x] 7.12 Recounted `tasks.md`'s checklist items deterministically after adding this group (`grep -c '^\- \[[ x]\]' openspec/changes/reconcile-specboot-workflow/tasks.md`) — see final count in this session's report

## 8. Third-Verification Corrections (Stale Adapter References & Permission-Efficient Execution)

Context: an independent **fresh-session** `/specboot-verify` run returned FAIL. Major finding: `ai-specs/skills/specboot-verify/SKILL.md` still contained stale references to the removed Claude command adapter (`.claude/commands/specboot/verify.md`, invocation `/specboot:verify`) in its intro, Step 5 example, Step 5 scenario, report template, and Guardrails — contradicting the Group 7 decision that Claude reaches this skill only through `.claude/skills/specboot-verify` as `/specboot-verify`. (A separate, human-observed permission-prompt observation from that same session was scoped out of this change after review — see the note at the end of this group — and is not addressed by Group 8.)

- [x] 8.1 Located and fixed all stale Claude-command-adapter references in `ai-specs/skills/specboot-verify/SKILL.md`: the intro sentence, the Step 5 per-client invocation example, the Step 5 "differing invocation" scenario, the Execution-provenance report-template line, and the Guardrails closing line — plus one additional stale example (Step 6's adapter-safety check list still named `commands/specboot/`) found by re-grepping the file, not listed in the original five but fixed for the same reason. Confirmed via `grep -n "specboot:verify\|commands/specboot"` (exit 1, no match) that none remain
- [x] 8.2 Confirmed Claude is documented as using only `.claude/skills/specboot-verify` → `/specboot-verify`; confirmed the Kiro prompt (`.kiro/prompts/specboot-verify.prompt.md`) remains the only thin delegating adapter file anywhere in this change
- [x] 8.3 Re-grepped `proposal.md`, `design.md`, `spec.md`, and `tasks.md` for the same stale strings: all remaining matches were historical Group 1–7 task records or explanatory "this was removed/corrected" prose, correctly describing what happened and when — except one live, present-tense design rationale (the "Capability-availability validation is data-driven and name-agnostic" decision in `design.md`, which still asserted Claude and Kiro "now intentionally use different invocation syntax" for verify specifically); corrected that one paragraph to reflect that verify now converges on `/specboot-verify` for both, while other capabilities (propose/apply/archive) still legitimately differ per client
- [x] 8.4 Confirmed preserved and unmodified: `ai-specs/skills/specboot-verify/SKILL.md` (content corrected, file itself retained), `.claude/skills/specboot-verify`, `.kiro/skills/specboot-verify`, `.kiro/prompts/specboot-verify.prompt.md`, every OpenSpec-generated `opsx` file, and that `.claude/commands/specboot/` was not recreated (`test -d` → absent)
- [x] 8.5 Recounted `tasks.md`'s checklist items deterministically after adding this group — see final count in this session's report

Note (not a tracked task, scope correction): a permission-efficient-execution addition (a SKILL.md section, a spec requirement with four scenarios, and a design Decision/Risk) was briefly added to this change in response to a human-observed Minor finding about excessive permission prompts, then removed after human review determined that concern belongs to a separate, future change (`harden-shared-client-permissions`) rather than `reconcile-specboot-workflow`. The underlying observation is preserved outside this change's artifacts as input for that future change, not implemented here.

## 9. Fourth-Verification Corrections (Minor Findings: Quote Accuracy & Symmetric Archive-Approval Disclaimer)

Context: the latest adversarial review returned two Minor findings, addressed here without reopening any Major/Blocker-level decision: (1) the archive-guardrail quotation used an en dash in `design.md`, `proposal.md`, and `ai-specs/skills/specboot-verify/SKILL.md`, while both selected clients' actual source files (`.claude/commands/opsx/archive.md`, `.kiro/prompts/opsx-archive.prompt.md`) use a plain hyphen — confirmed via `grep -n "Don't block archive"` against both source files; (2) the mandatory-sequence coverage stated that a SpecBoot verify PASS alone doesn't grant archive approval, but had no symmetric, explicit statement that an `adversarial-review` PASS alone doesn't either, nor a matching disclaimer in `adversarial-review/SKILL.md`'s own Completion section.

- [x] 9.1 Confirmed the exact source text via `grep -n "Don't block archive"` against `.claude/commands/opsx/archive.md` and `.kiro/prompts/opsx-archive.prompt.md`: both read `Don't block archive on warnings - just inform and confirm` with a plain ASCII hyphen
- [x] 9.2 Corrected all 4 quoted instances (`design.md` ×2, `proposal.md` ×1, `ai-specs/skills/specboot-verify/SKILL.md` ×1) to the plain-hyphen exact quote, each now explicitly labeled "(exact quote, plain hyphen)" so a future reader doesn't need to re-diff it against the source
- [x] 9.3 Added the "Adversarial-review PASS alone does not grant archive approval" scenario to the "Verification severity gate and next-gate eligibility" requirement in `specs/specboot-verification-workflow/spec.md`, symmetric to the existing SpecBoot-verify-side scenario
- [x] 9.4 Added the matching disclaimer to `ai-specs/skills/adversarial-review/SKILL.md`'s `## Completion` section: a PASS/PASS WITH GAPS verdict there does not by itself grant archive approval; explicit human approval remains independently required
- [x] 9.5 Confirmed no client adapter, symlink, or OpenSpec-generated command file was modified: only `design.md`, `proposal.md`, `spec.md`, `ai-specs/skills/specboot-verify/SKILL.md`, and `ai-specs/skills/adversarial-review/SKILL.md` changed in this group
- [x] 9.6 Recounted `tasks.md`'s checklist items deterministically after adding this group — see final count in this session's report

## 10. Fifth-Verification Correction (Open Major: Remaining Live Documentation Drift; Deterministic Adversarial-Review Verdict Mapping)

Context: the latest adversarial review found one open Major, blocking archive: live, present-tense references to retired workflow names (`/new`, `/ff`, `/continue`, `/verify` as a slash command, `opsx:ff`, `opsx:continue`, `openspec-ff-change`, `openspec-continue-change`) remained in root/doc documentation that no prior Group in this change had inspected — `README.md`, `docs/base-standards.md`, `docs/openspec-tasks-mandatory-steps.md`, and `packages/specboot/template/docs/base-standards.md`. A related Minor observation noted `adversarial-review`'s own verdict mapping was descriptive prose rather than an explicit, checkable rule — the review that found this Major itself self-reported `PASS WITH GAPS` in a prior round despite containing an open Major, which should have forced FAIL. OpenSpec artifacts were extended first, per this change's own §7 requirement (spec-update-before-code), then the documentation and skill were corrected.

- [x] 10.1 Independently inspected every named location before editing: `test -L`/`readlink` on `README.md`, `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `codex.md` confirmed `README.md` is a real file and the other four are symlinks to `docs/base-standards.md`; `grep -n` for the retired-name pattern was run separately against `README.md`, `docs/base-standards.md`, `docs/openspec-tasks-mandatory-steps.md`, and every stale-reference hit found in `packages/specboot/template/` (`grep -rln` across the whole template directory, narrowing to `packages/specboot/template/docs/base-standards.md` and the already-corrected `packages/specboot/template/ai-specs/specboot-instructions.md`)
- [x] 10.2 Confirmed the current, correct replacement names against the actual installed inventory rather than assumption: `find .claude/skills -maxdepth 1 -type d -name "openspec-*"` and `find .claude/commands/opsx -maxdepth 1 -type f` → `openspec-propose`, `openspec-update-change`, `/opsx:propose`, `/opsx:update` (and Kiro's hyphenated equivalents) are the real current names
- [x] 10.3 Extended `proposal.md` (two new "What Changes" bullets, expanded Impact file list), `design.md` (two new Decisions with alternatives-considered, two new Risks, two new Goals), and `specs/specboot-verification-workflow/spec.md` (two new ADDED requirements: "Adversarial-review deterministic verdict mapping" with 4 scenarios, and "Live workflow documentation accuracy across root and doc locations" with 4 scenarios) *before* touching any documentation file, per this change's own mandatory spec-first order
- [x] 10.4 Corrected `README.md`: replaced the stale `/new`/`/ff`/`/verify`/`/adversarial-review`/`/archive` workflow list and example flow with the same corrected sequence already used in `ai-specs/specboot-instructions.md` (confirmed via `diff` before editing that `README.md` was an untouched, independent near-duplicate — no prior Group had edited it); the historical "Note on `/new`, `/ff`, and `/continue`" explanatory text is retained as history, not further edited
- [x] 10.5 Corrected `docs/base-standards.md` (canonical; propagates automatically through the `CLAUDE.md`/`AGENTS.md`/`GEMINI.md`/`codex.md` symlinks, which were *not* edited directly): §5 now names `openspec-propose`/`openspec-update-change` with an explanatory "(supersedes the retired ...)" note; §7 now says `opsx:update`, `opsx:propose` in place of `opsx:continue`, `opsx:ff`. §7's `/apply`/`/archive` bare mentions were left unchanged — both remain real, valid capabilities
- [x] 10.6 Corrected `docs/openspec-tasks-mandatory-steps.md` §5 "When This Applies": replaced `/opsx:ff`/`openspec-ff-change` and `/opsx:continue`/`openspec-continue-change` with `/opsx:propose`/`openspec-propose` and `/opsx:update`/`openspec-update-change`, each with a "(supersedes the retired ...)" note; its `/opsx:apply`/`openspec-apply-change` line was left unchanged — already accurate
- [x] 10.7 Corrected `packages/specboot/template/docs/base-standards.md` §6.7 using generic, client/version-neutral wording ("your OpenSpec CLI's own current artifact-generation/update workflow... check `openspec schemas`/your client's command list rather than assuming a specific name") instead of hardcoding this repository's specific current skill/command names, since that file is distributed to other, unrelated projects
- [x] 10.8 Added the "Adversarial-review deterministic verdict mapping" requirement's mechanical rule to `ai-specs/skills/adversarial-review/SKILL.md`: Step 5 now states the exact Blocker/Major→FAIL, only-Minor→PASS WITH GAPS, no-findings→PASS mapping as a numbered, mechanical procedure ("apply mechanically... recount... do not rely on an earlier impression"), the Output Format's `### Verdict` block now carries an inline derivation reminder, and a new Guardrail restates that the mapping is not a judgment call
- [x] 10.9 Ran a final sweep (`grep -n` across all six locations: the two already-corrected `specboot-instructions.md` copies plus the four newly-corrected files) confirming every remaining match is inside an explicitly historical/explanatory note, never a live instruction
- [x] 10.10 Confirmed preserved: every OpenSpec-generated `opsx` file (`git diff --quiet -- .claude/commands/opsx` and the six `.kiro/prompts/opsx-*.prompt.md` files), the `.claude/skills/specboot-verify`/`.kiro/skills/specboot-verify` symlinks and `.kiro/prompts/specboot-verify.prompt.md`, and all prior Group 1–9 historical task records — none were modified by this group; no archive, stage, commit, or push was performed; no install/download/web access occurred; no global OpenSpec configuration was touched
- [x] 10.11 Recounted `tasks.md`'s checklist items deterministically after adding this group — see final count in this session's report

Note (not a tracked task): this change touches no application code, API contract (`docs/api-spec.yml`), data model (`docs/data-model.md`), or database state, and involves no backend unit tests or API endpoints. The backend-specific mandatory testing steps in `docs/openspec-tasks-mandatory-steps.md` (unit-test review, curl endpoint testing, E2E testing) do not apply to a change that only touches SpecBoot workflow documentation, a canonical skill, symlinks, and thin client adapters, and are intentionally omitted from this task list rather than tracked as permanently-unchecked checkboxes.

## 11. Sixth-Verification Correction (Live Installer Defect; enrich-us Contract, Handoff, and Six-Capability Matrix)

Context: the latest independent adversarial review returned FAIL because `packages/specboot/bin/init.js` still printed the retired `/enrich-us -> /ff -> /apply` sequence — a live defect Group 10's stale-reference sweep missed because it only searched Markdown/doc content, not the JS installer. Investigating it surfaced a deeper, previously-unaddressed gap: this change had never formally defined `enrich-us`'s contract or how its output reaches OpenSpec proposal generation, even though the confirmed historical workflow (`enrich-us -> enriched Markdown artifact -> opsx:new -> opsx:ff -> opsx:apply`, evidenced by commit `3bcc6d1`, the historical `enrich-us` skill, historical `opsx:new`/`opsx:ff`, and archived `improved-user-story.md`/`scrum-80-enhanced.md` files) treated the enriched artifact as authoritative planning input. OpenSpec artifacts were extended first (`proposal.md`, `design.md`, `spec.md`), per this change's own spec-before-code rule, before any implementation file was touched.

- [x] 11.1 Confirmed the reported defect directly: read `packages/specboot/bin/init.js` and found line 88 printing `'  3. /enrich-us  ->  /ff  ->  /apply\n'` — the retired `/ff` command, hard-coded as Claude-style syntax in a shared, multi-client installer
- [x] 11.2 Read the pre-existing `ai-specs/skills/enrich-us/SKILL.md` and its template counterpart `packages/specboot/template/ai-specs/skills/enrich-us/SKILL.md`: both modeled Jira MCP as the default/primary input source with no direct-input-first path, and neither had a mandatory-gate framing, a `READY FOR PROPOSAL`/`NEEDS CLARIFICATION` outcome, or any artifact-handoff instruction — confirming the gap was real, not merely the one installer line
- [x] 11.3 Read `.claude/commands/opsx/propose.md` and `.kiro/prompts/opsx-propose.prompt.md` (read-only; both remain on this change's do-not-modify list) and confirmed their only documented input channel is a conversational change-name/description argument — no file-path or attachment ingestion exists to hook into, which determined the handoff-mechanism decision in `design.md`
- [x] 11.4 Extended `proposal.md` (new Group 11 "What Changes" bullet set and expanded Impact file list), `design.md` (new Context evidence, Goals, five new Decisions with alternatives evaluated and rejected, four new Risks, expanded Non-Goals and Migration Plan), and `specs/specboot-verification-workflow/spec.md` (extended the existing capability-availability requirement to six capabilities plus a new scenario; four new ADDED requirements — mandatory `enrich-us` gate, enriched-artifact handoff, Phase-1 Jira-free input, portable installer/documentation guidance — with 15 new scenarios total) *before* touching `enrich-us/SKILL.md`, `specboot-verify/SKILL.md`, `specboot-instructions.md`, `README.md`, `init.js`, or `.gitignore`
- [x] 11.5 Rewrote the canonical `ai-specs/skills/enrich-us/SKILL.md`: mandatory gate for every work-item type; direct-input-first (chat text, screenshot, readable attachment), Jira named only as a future/historical mode; inspects repository technical context; identifies the full concern list (scope/AC/edge cases/affected interfaces/testing/docs/security/performance/observability/compatibility/migration/operational); separates confirmed facts / inferred assumptions / unresolved questions; never invents a material decision; preserves meaning for already-detailed input; produces the enriched Markdown artifact plus persists it to `.specboot/staging/<slug>-enriched.md`; returns `READY FOR PROPOSAL`/`NEEDS CLARIFICATION`; states the exact next action (invoke the client's own propose command in the same conversation, then copy the staged file into the new change directory as `enriched-work-item.md` for traceability)
- [x] 11.6 Applied the equivalent, portability-adjusted rewrite to `packages/specboot/template/ai-specs/skills/enrich-us/SKILL.md` — same contract, generic/client-neutral wording consistent with the template's distributable audience (no hardcoding of this repository's specific OpenSpec change-naming conventions beyond the already-portable `.specboot/staging/` convention itself)
- [x] 11.7 Updated `ai-specs/skills/specboot-verify/SKILL.md` Step 5 from a five- to a six-capability matrix, adding `enrich-us` first in sequence, and stated Jira is explicitly excluded from the matrix
- [x] 11.8 Updated `ai-specs/specboot-instructions.md` and `packages/specboot/template/ai-specs/specboot-instructions.md` identically: step 1 (`enrich-us`) rewritten from "(optional)" to the mandatory-gate contract, the enriched-artifact handoff instructions, and the Jira-deferred wording; the six-capability list introduced; the Example End-to-End Flow updated to show `enrich-us` producing the artifact before `opsx:propose`/`opsx-propose`; `diff` between the two files after this correction shows only the pre-existing cosmetic image-URL line (expected non-zero, evaluated by content, not treated as a failure)
- [x] 11.9 Applied the identical corrections to `README.md` (confirmed via `diff` against the corrected `ai-specs/specboot-instructions.md` before editing that it was still the same independent near-duplicate established in Group 10, not yet touched by this group)
- [x] 11.10 Corrected `packages/specboot/bin/init.js` line 88: replaced the retired, Claude-hard-coded `/enrich-us -> /ff -> /apply` string with portable, client-neutral text naming `enrich-us` and deferring exact per-client propose syntax to `ai-specs/specboot-instructions.md`
- [x] 11.11 Verified no existing `.gitignore` rule already covered a `.specboot/` staging path (`grep`, no match), added the narrow rule `.specboot/staging/`, and confirmed with `git check-ignore -v` that a test path under it is ignored for exactly that reason
- [x] 11.12 Ran the repository-wide (not Markdown-only) stale-reference sweep for `/new`, `/ff`, `/continue`, obsolete `/verify`/`/archive` slash-command mentions, `opsx:ff`, `opsx:continue`, `openspec-ff-change`, `openspec-continue-change` across all tracked and newly-untracked text files (excluding build/dependency/Git-internal paths); classified every match — see this session's final report for the complete inventory. Confirmed the only new **live** defect was `packages/specboot/bin/init.js:88` (fixed in 11.10); all other matches were either this change's own historical/task-record artifacts, already-labeled historical notes from Group 10, or OpenSpec-generated files (`.claude/commands/opsx/*`, `.claude/skills/openspec-update-change`, `.claude/skills/openspec-apply-change`, `.kiro/skills/openspec-apply-change`) this change is not permitted to modify
- [x] 11.13 Ran validation: Node syntax check on the modified installer (`node --check packages/specboot/bin/init.js`); `openspec validate --changes "reconcile-specboot-workflow" --strict`; `diff` for every duplicated canonical/template pair touched this group; `test -L`/`readlink` symlink checks for `enrich-us` on both clients (pre-existing, unchanged); a repository-wide broken-symlink scan; confirmation that every OpenSpec-generated file (`.claude/commands/opsx/*`, `.kiro/prompts/opsx-*`, `.claude/skills/openspec-*`, `.kiro/skills/openspec-*`) is byte-unchanged; confirmation that the separately-committed shared-permissions checkpoint commit (`67f2c67`) is untouched; confirmation no adapter was added for any unselected client — see this session's final report for exact results
- [x] 11.14 Recounted `tasks.md`'s checklist items deterministically after adding this group — see final count in this session's report

## 12. Seventh-Verification Correction (Narrow, Pre-Verification: Staging-Ignore Portability)

Context: before this change was ever submitted for independent verification, a portability defect was found in Group 11's own `.specboot/staging/` design: the ignore rule was added only to this repository's own root `.gitignore`; `packages/specboot/template/` ships no `.gitignore` at all; and `packages/specboot/bin/init.js`'s `copyRecursive` skips (rather than merges into) any destination file that already exists. A newly adopting repository with its own pre-existing `.gitignore` could install the distributed `enrich-us` skill without ever receiving the staging ignore rule. OpenSpec artifacts were extended first, per this change's own spec-before-code rule, before touching `init.js` or either `enrich-us/SKILL.md`.

- [x] 12.1 Confirmed the defect directly: `find packages/specboot/template -iname ".gitignore"` returned no match (the template ships no `.gitignore`); re-read `packages/specboot/bin/init.js`'s `copyRecursive` and confirmed it calls `pathExists(destPath)` and pushes to `stats.skipped` — never merges — for any destination file that already exists
- [x] 12.2 Extended `proposal.md` (new Group 12 "What Changes" bullet set and Impact entries), `design.md` (new Context evidence, one new Goal, one new Non-Goal, one new Decision with three alternatives evaluated and rejected, two new Risks, updated Migration Plan), and `specs/specboot-verification-workflow/spec.md` (two new ADDED requirements — portable idempotent staging-ignore provisioning, and the `enrich-us` ignored-path guard — with 8 new scenarios total) *before* touching `init.js` or either `enrich-us/SKILL.md`
- [x] 12.3 Implemented `ensureGitignoreStagingRule()` in `packages/specboot/bin/init.js`, invoked once from `main()`: creates `.gitignore` with the rule if absent; checks for the exact trimmed line `.specboot/staging/` and makes no change if already present; otherwise appends a `### SpecBoot ###` header, an explanatory comment, and the rule, preceded by a newline only when the existing content doesn't already end in one, and always ending the file in a trailing newline; never writes a bare `.specboot/` line
- [x] 12.4 Added the ignored-path guard to `ai-specs/skills/enrich-us/SKILL.md` (canonical) and `packages/specboot/template/ai-specs/skills/enrich-us/SKILL.md` (template), identically: before writing to `.specboot/staging/`, verify the path is ignored (`git check-ignore` inside a git repository; otherwise treat as not confirmed); if not confirmed ignored, do not write the file — report the missing one-time `.gitignore` setup by exact rule name and present the artifact in the chat response only
- [x] 12.5 Ran `node --check packages/specboot/bin/init.js` — pass
- [x] 12.6 Validated the installer against four isolated temporary target directories (created under the session scratchpad, never under this repository): (a) no existing `.gitignore`; (b) existing `.gitignore` with unrelated content; (c) existing `.gitignore` already containing the rule; (d) the same target directory run twice, to confirm idempotency. Recorded exact commands, exit statuses, resulting `.gitignore` content, and cleanup for each — see this session's final report
- [x] 12.7 Ran `openspec validate --changes "reconcile-specboot-workflow" --strict` — pass
- [x] 12.8 Re-ran the canonical/template diff for `enrich-us/SKILL.md` — confirmed the guard was added identically to both, with the pre-existing intentional template-only portability note as the only other difference
- [x] 12.9 Confirmed this correction did not touch `.claude/commands/opsx/*`, `.kiro/prompts/opsx-*`, any OpenSpec-generated skill directory, this repository's own already-provisioned root `.gitignore` entry, client-selection logic, or any other part of `init.js` beyond the new provisioning function; no stage, commit, push, `/specboot-verify`, or adversarial review was run
- [x] 12.10 Recounted `tasks.md`'s checklist items deterministically after adding this group — see final count in this session's report

## 13. Eighth-Verification Correction (Documentation/Provenance: Installer-vs-Configured-Capability Distinction)

Context: the latest independent adversarial review returned FAIL with one Major and one Minor. Major: `README.md`, `ai-specs/specboot-instructions.md`, and its template copy present Kiro command syntax throughout the workflow section (`/opsx-propose`, `/opsx-apply`, `/opsx-archive`, the "Kiro syntax" example block) with no disclosure that `packages/specboot/bin/init.js`'s client-provisioning loop (`for (const tool of ['.claude', '.cursor'])`) has never provisioned Kiro — this repository's working `.kiro/` setup was configured separately from that installer, not by it, and the documentation never said so. Minor: `ai-specs/skills/specboot-verify/SKILL.md` Step 5's capability-availability check reports whether paths currently exist and resolve, but didn't clarify this is availability, not provisioning provenance — a PASS on this repository's own Kiro paths could be misread as evidence the installer provisions Kiro. OpenSpec artifacts were extended first, per this change's own spec-before-code rule, before touching any documentation or skill file. Per explicit instruction, this correction does not add Kiro provisioning to the installer — that remains a recorded, future, separately-approved change (`make-specboot-installer-client-aware`).

- [x] 13.1 Confirmed the defect directly: re-read `packages/specboot/bin/init.js` and confirmed the provisioning loop is `for (const tool of ['.claude', '.cursor'])` with no `.kiro` branch anywhere in the file (`grep -in kiro packages/specboot/bin/init.js` → no match); `grep -in kiro` against `README.md`, `ai-specs/specboot-instructions.md`, and the template copy found Kiro syntax presented as an equal, always-available alternative to Claude throughout the workflow section, with no installer-provisioning caveat anywhere in any of the three files
- [x] 13.2 Read `SPECBOOT_ADOPTION_GUIDE.md` to confirm it does not make the same false claim: its own "Important shell behavior" note (§2 experiment path) already states hidden client directories including `.kiro/` are not copied by a plain `cp`, and that "hidden client resources must be created by OpenSpec, CodeGraph, or the adapter steps" — already accurate regarding OpenSpec's own client selection, and its one reference to `packages/specboot/bin/init.js` only concerns the four root Markdown symlinks, not Kiro provisioning; left unmodified since it makes no defective claim
- [x] 13.3 Extended `proposal.md` (new Group 13 "What Changes" bullet set and Impact entries), `design.md` (new Context evidence explaining both findings, a documented rejection of unconditionally adding `.kiro` to the installer loop, one new Goal, one new Non-Goal, three new Decisions — chosen documentation/provenance option vs. rejected installer-redesign option, the documentation wording itself, the verifier wording itself — plus the `make-specboot-installer-client-aware` future-change record, two new Risks, updated Migration Plan), and `specs/specboot-verification-workflow/spec.md` (five new ADDED requirements — documentation distinguishes availability from provisioning, Kiro guidance applies only where separately configured, distributed templates disclose provisioned clients accurately, verifier reports availability not provenance, selected-client-aware installer redesign is a future non-goal — with 9 new scenarios total) *before* touching `README.md`, either `specboot-instructions.md` copy, `init.js`, or `specboot-verify/SKILL.md`
- [x] 13.4 Added an "Installer Scope: Claude/Cursor Provisioning Only (Kiro Configured Separately)" note to `ai-specs/specboot-instructions.md`, immediately after the Quick Install (`npx @lidr/lidr-specboot`) instructions: states the installer provisions Claude/Cursor shared adapters only, does not provision Kiro, that this repository's `.kiro/` paths were configured separately and remain valid, that Kiro examples in the document apply only where Kiro has been separately configured, and cross-references `SPECBOOT_ADOPTION_GUIDE.md` §2/§13 for the separate client-selection/adapter-creation path; also added an inline qualifier to the "Kiro syntax" example header pointing back to this note
- [x] 13.5 Applied the identical correction to `packages/specboot/template/ai-specs/specboot-instructions.md` and `README.md`; `diff` confirms all three still differ only by the pre-existing cosmetic image-URL line (canonical vs. template) and the pre-existing, already-accepted absence of the "Permission Setup (One-Time)" section in `README.md` (established in Group 4/10, not touched by this group)
- [x] 13.6 Corrected `packages/specboot/bin/init.js`'s printed next-steps/summary text only (no change to the `.claude`/`.cursor` `createSymlink` loops, and no `.kiro` branch added) so it states that the installer provisions Claude/Cursor shared adapters and that other selected clients (e.g. Kiro) require separate configuration, pointing to `ai-specs/specboot-instructions.md`
- [x] 13.7 Corrected `ai-specs/skills/specboot-verify/SKILL.md` Step 5: added explicit wording that capability availability is determined from the current repository filesystem, that a PASS means the required paths currently exist and resolve for the selected client, and that a PASS does not assert which installer, person, or process provisioned them — installer-provenance validation is called out as a separate, unimplemented concern
- [x] 13.8 Ran `node --check packages/specboot/bin/init.js` — pass; confirmed via `diff` against the pre-Group-13 version that only the `console.log` next-steps block changed, not `copyRecursive`, `createSymlink`, `ensureGitignoreStagingRule`, or the `for (const tool of ['.claude', '.cursor'])` loop
- [x] 13.9 Ran `openspec validate --changes "reconcile-specboot-workflow" --strict` — pass
- [x] 13.10 Confirmed current Claude and Kiro capability paths remain unchanged and resolve (`test -L`/`test -f` for all six capabilities, both clients); confirmed `.claude/commands/opsx/*`, `.kiro/prompts/opsx-*`, every OpenSpec-generated skill directory, the shared-permissions checkpoint commit (`67f2c67`), and application code are untouched; confirmed no `.cursor` or `.codex` directory was introduced into this repository; confirmed the installer's provisioning loops are byte-unchanged; no stage, commit, push, `/specboot-verify`, or adversarial review was run
- [x] 13.11 Recounted `tasks.md`'s checklist items deterministically after adding this group — see final count in this session's report

## 14. Ninth-Verification Correction (Minor Findings: Scenario-Count Miscount and .gitignore Trailing Newline)

Context: the latest independent adversarial review returned `PASS WITH GAPS` with three Minor findings, none blocking archive eligibility on their own but corrected here to keep artifacts and reporting accurate: (1) task 13.3, above, stated Group 13 added "13 new scenarios total" for its five new requirements; the actual count, confirmed by isolating those five requirements in `spec.md` and counting their `#### Scenario:` lines, is 9 — corrected in place above. (2) this repository's root `.gitignore` (the Group 11 staging-rule addition) had no trailing newline — its last byte was `/` from the `.specboot/staging/` line, not `\n` — violating the same final-newline-correctness expectation this change's own Group 12 requirement holds the installer's provisioning function to. (3) an earlier session report in this conversation stated the spec contained "20 requirements" at a point where the actual, on-disk count (confirmed by `grep -c '^### Requirement:'`) was already 21; this was a chat-report undercount made while summarizing, not a defect in any artifact — no artifact required correction for this finding, only this note recording that it was chat-only.

- [x] 14.1 Corrected task 13.3's scenario count from 13 to 9, confirmed via `awk '/^### Requirement: Documentation distinguishes capability availability from installer provisioning/,0' specs/specboot-verification-workflow/spec.md | grep -c '^#### Scenario:'` → 9 (this isolates the five Group 13 requirements, all of which appear after and including that one, to the end of the file)
- [x] 14.2 Recorded the review verdict and all three Minor findings in this Group 14 context note, including the explicit finding that the "20 requirements" chat undercount was conversational, not an artifact defect, so no `spec.md`/`proposal.md`/`design.md` edit was made for that finding
- [x] 14.3 Verified the root `.gitignore`'s pre-correction last byte was `/` (`tail -c 1 .gitignore | xxd` → `2f`, not `0a`) via direct byte inspection before making any change
- [x] 14.4 Appended exactly one `\n` byte to the end of the root `.gitignore` — no other byte changed, no rule added, removed, or reordered; confirmed via `xxd`/`wc -c` before-and-after comparison that the diff is exactly one appended `0a` byte
- [x] 14.5 Confirmed exactly one `.specboot/staging/` line remains in the root `.gitignore` (`grep -c '^\.specboot/staging/$' .gitignore` → 1) and that `git check-ignore -v .specboot/staging/probe.md` still resolves to that same line after the newline fix
- [x] 14.6 Ran `openspec validate --changes "reconcile-specboot-workflow" --strict` — pass
- [x] 14.7 Confirmed via `git status --short` that only `tasks.md` and `.gitignore` changed in this correction; confirmed `.claude/commands/opsx/*`, `.kiro/prompts/opsx-*`, every OpenSpec-generated skill directory, the shared-permissions checkpoint commit (`67f2c67`), `packages/specboot/bin/init.js`, application code, and every symlink are untouched; no stage, commit, push, install, download, web access, `/specboot-verify`, or adversarial review was run
- [x] 14.8 Recounted `tasks.md`'s checklist items deterministically after adding this group, and reconfirmed `spec.md` still contains 21 requirements and 69 scenarios — see final counts in this session's report

## 15. Tenth-Verification Correction (Major Defect: Broader-Rule No-Op Promise Not Implemented)

Context: an independent adversarial review found one Major defect: `ensureGitignoreStagingRule()` (Group 12) only ever recognized the exact trimmed line `.specboot/staging/`, so a target `.gitignore` already containing an equivalent, broader rule (for example a bare `.specboot/` line) would still receive the narrow rule appended — contradicting `proposal.md`'s own Group 12 promise that an equivalent broader rule should make the installer a no-op. OpenSpec artifacts were extended first, per this change's own spec-before-code rule, before touching `init.js`. Tasks below are marked complete only once their corresponding action and its evidence exist on disk, per explicit instruction for this correction.

- [x] 15.1 Extended `proposal.md` (new Group 15 "What Changes" bullet set and Impact entries), `design.md` (new Context evidence naming the exact broken promise, one new Goal, a Decision detailing the Git-evaluation-first algorithm with three alternatives evaluated and rejected — custom parser, mandatory Git, manual combination of exclude sources — a second Decision recording the verified negation-case Git behavior, three new Risks, updated Migration Plan), and `specs/specboot-verification-workflow/spec.md` (rewrote the existing "Portable, idempotent staging-ignore provisioning" requirement to require Git-evaluation-first detection with the prior exact-line fallback, plus 3 new scenarios) *before* touching `init.js`
- [x] 15.2 Implemented `gitSaysStagingIgnored(targetDir)` in `packages/specboot/bin/init.js` using `child_process.spawnSync('git', ['-C', targetDir, 'check-ignore', '--quiet', probe])` against the literal probe path `.specboot/staging/.specboot-probe` (which does not need to exist on disk): exit `0` → `true` (ignored); exit `1` → `false` (not ignored); any spawn error or other exit status → `null` (evaluation unusable)
- [x] 15.3 Rewired `ensureGitignoreStagingRule()`: `true` → return with no file write (byte-for-byte no-op); `false` → run the create-or-append logic via new helper `writeOrAppendStagingBlock()` (unchanged `STAGING_BLOCK`, unchanged newline handling); `null` → fall back to the pre-Group-15 exact-line-only check before deciding whether to create-or-append
- [x] 15.4 Ran `node --check packages/specboot/bin/init.js` — pass
- [x] 15.5 Ran isolated tests in temporary directories under the session scratchpad (never under this repository), each initialized as a real Git repo where noted: (a) no `.gitignore`, Git repo → created, ends in `\n`; (b) unrelated content (`node_modules/`, `*.log`, `dist`), Git repo → appended, pre-existing lines preserved in order; (c) exact `.specboot/staging/` rule already present, Git repo → `git`-branch reports "already ignored (git)", SHA-256 unchanged; (d) existing broader `.specboot/` rule, Git repo → **the Major-defect fix**: `git check-ignore -v` on the probe returns exit 0 (matched by `.specboot/` line 2) before the installer runs, installer reports "already ignored (git)", SHA-256 before/after identical — byte-for-byte no-op confirmed; (e) repeated execution on (a)/(b)/(d)/(f) → all four SHA-256-identical on second run; (f) broader rule plus a negation, tested in two real variants: `.specboot/` + `!.specboot/staging/` → `git check-ignore` still reports ignored (exit 0; Git's "cannot re-include if a parent directory is excluded" rule blocks the negation) → installer correctly no-ops; `.specboot/*` + `!.specboot/staging/` → `git check-ignore` reports not ignored (exit 1; the negation *does* take effect here) → installer appends the narrow rule, and a follow-up `git check-ignore` confirms the probe is ignored again afterward; (g) the original four Group 12 scenarios re-run in directories confirmed *not* inside any Git repository (`git rev-parse --is-inside-work-tree` → "fatal: not a git repository") → fallback behaves identically to Group 12/14, including the documented, accepted limitation that a broader rule outside Git evaluation still gets a redundant (but harmless, non-broadening, non-destructive) narrow line appended. Every command's real exit status was recorded; none were assumed
- [x] 15.6 Ran `openspec validate --changes "reconcile-specboot-workflow" --strict` → exit 0, "1 passed, 0 failed"
- [x] 15.7 Recounted `tasks.md`'s checklist items and `spec.md`'s requirement/scenario counts deterministically — see final counts in this session's report
- [x] 15.8 Confirmed preservation: `.claude/commands/opsx/*`, `.kiro/prompts/opsx-*`, every OpenSpec-generated skill directory, the shared-permissions checkpoint commit (`67f2c67`), every existing symlink (repository-wide broken-symlink scan: none found), and application code are untouched (`git status --short` shows no non-SpecBoot paths changed); confirmed this repository's own root `.gitignore` was not touched by any test this turn (still 521 bytes, single `.specboot/staging/` occurrence, ends in `\n` — unaffected since every test ran against scratchpad directories only, all cleaned up afterward); confirmed only `packages/specboot/bin/init.js` plus the four OpenSpec artifacts changed; nothing staged; no stage, commit, push, install, download, web access, `/specboot-verify`, or adversarial review was run

## 16. Eleventh-Verification Correction (Minor Finding: Verdict Mapping Not Exhaustive Over Unresolved Questions)

Context: the final independent adversarial review returned `PASS WITH GAPS` with one Minor finding: Step 5's mapping (Group 10, refined through Group 13/14) never explicitly stated how a Findings table containing only unresolved `Question / assumption` rows — zero Blocker, zero Major, zero Minor — should be classified, and could be misread as falling through to the mapping's "no findings of any severity → PASS" branch despite a genuinely open question. `Question / assumption` remains exactly what it always was — a finding category needing confirmation, not a fourth severity; only the mapping's exhaustiveness is corrected. Tasks below are marked complete only once their corresponding action and its evidence exist on disk.

- [x] 16.1 Extended `proposal.md` (corrected the Group 10 "What Changes" bullet's verdict-mapping wording in place, plus a new Group 16 "What Changes" bullet and Impact entry), `design.md` (new Context evidence naming the exact gap, one new Goal, one new Non-Goal explicitly ruling out a fourth severity, a Decision detailing the two-phase resolve-then-count procedure with three alternatives evaluated and rejected — a fourth severity, requiring all questions resolved before any verdict, and silently dropping unresolved questions — one new Risk, updated Migration Plan), and `specs/specboot-verification-workflow/spec.md` (rewrote the existing "Adversarial-review deterministic verdict mapping" requirement to be exhaustive over all four finding categories and to state the resolved-with-evidence rule, plus 4 new scenarios) *before* touching `adversarial-review/SKILL.md`
- [x] 16.2 Rewrote Step 5 of `ai-specs/skills/adversarial-review/SKILL.md` as an explicit two-phase procedure: phase 1 resolves every `Question / assumption` row (removed only with recorded evidence; otherwise left open in the table), phase 2 counts and applies the exhaustive three-branch mapping (Blocker/Major → FAIL; else Minor-or-unresolved-question → PASS WITH GAPS; else → PASS)
- [x] 16.3 Updated the `## Output format` template: added a line under the Findings table stating an unresolved `Question / assumption` row is never silently dropped, and rewrote the inline verdict-derivation reminder to state the exhaustive mapping explicitly, including "A Question/assumption-only table is PASS WITH GAPS, never PASS"; added a matching Guardrails bullet
- [x] 16.4 Confirmed via `grep -n -i severity` and `grep -n "Question / assumption"` against the file: exactly the pre-existing three severities (Step 4's Blocker/Major/Minor definitions, unchanged) plus `Question / assumption` still described as a finding category needing confirmation ("not a fourth severity", stated explicitly in the new Step 5 text) — no new severity label introduced anywhere
- [x] 16.5 Ran `openspec validate --changes "reconcile-specboot-workflow" --strict` → exit 0, "1 passed, 0 failed"
- [x] 16.6 Recounted `tasks.md`'s checklist items and `spec.md`'s requirement/scenario counts deterministically — see final counts in this session's report
- [x] 16.7 Confirmed preservation: only `ai-specs/skills/adversarial-review/SKILL.md` plus the four OpenSpec artifacts changed this turn (`git status --short` and `git diff --stat` for `init.js` show no new diff from this turn); `.claude/commands/opsx/*`, `.kiro/prompts/opsx-*`, every OpenSpec-generated skill directory, the shared-permissions checkpoint commit (`67f2c67`), every symlink (broken-symlink scan: none found), `packages/specboot/bin/init.js`, and application code are untouched; nothing staged; no stage, commit, push, install, download, web access, `/specboot-verify`, or adversarial review was run
