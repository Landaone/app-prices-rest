## 0. Setup: Create Feature Branch (MANDATORY — FIRST STEP)

- [x] 0.1 Create feature branch `feature/restructure-specboot-adoption-guide` from the current branch, before any edit to `SPECBOOT_ADOPTION_GUIDE.md` or any new file
- [x] 0.2 Verify branch creation and current branch status with `git rev-parse --abbrev-ref HEAD` and `git status --short`

## 1. Pre-Edit Baseline Capture and Mapping Contract — Design Decision D8

- [x] 1.1 Capture the pre-edit baseline to the session scratchpad: `wc -l -w -c SPECBOOT_ADOPTION_GUIDE.md` (expect 2045 / 11786 / 87106), the full `grep -n '^#\{1,4\} '` heading list, and `git rev-parse HEAD`
- [x] 1.2 Capture pre-edit invariant counts: `HUMAN APPROVAL REQUIRED` (expect 11), `Live validation record` + `Live permission record` (expect 20), and one count per prompt-label class (`HISTORICAL PROMPT — VERIFIED VERBATIM`, `CANONICAL CONSOLIDATED PROMPT`, `CANONICAL REUSABLE PROMPT`, `LIVE VALIDATION REQUIRED`)
- [x] 1.3 Capture the pre-edit internal-anchor inventory: every `](#...)` target and the heading it resolves to (measured: 39 links across 25 distinct targets; the task's original "32" estimate was an undercount, corrected in `reports/section-mapping.md`)
- [x] 1.4 Write the source-section → destination-file mapping table covering all 22 numbered sections plus the 4 front-matter blocks (`Prompt Label Conventions`, `Happy Path 1`, `Happy Path 2`, `Table of Contents`) and the 2 closing blocks (`Clean-Install Working Method`, `Final Acceptance Criteria`); save it to `reports/section-mapping.md` in the change directory
- [x] 1.5 Verify the mapping table has zero unmapped source sections and zero destination files without a source; this table is the completeness contract for every later group

## 2. Cross-Cutting Conventions File — Design Decision D9 (built first, everything references it)

- [x] 2.1 Create `specboot-adoption/00-conventions.md` containing the prompt-label conventions table, the approval-gate protocol, the PASS/FAIL evidence discipline ("an unexecuted or failed command is FAIL, never an inferred PASS from empty output"), the shell-portability rule, the capability-availability-vs-installer-provisioning distinction, and the **bounded per-step working set** (design D12): `00-conventions.md`, the single phase file containing the current step, and the operator's filled copy of `ADOPTION-RUN-LOG.template.md` — all three are working files; loading any of them is correct behaviour, while unrelated phase, history, support, or canonical-workflow files stay out unless the current step's documented path requires one. **Reopened by the D4/D13 corrections:** the step-contract field table in this file must state the corrected **`Action` four-form rule** and the corrected **`On failure` three-form rule** verbatim as design D4 and D13 define them. The current text (`Action` — "either shell commands or exactly one labeled canonical prompt, never both"; `On failure` — "Where to go in `22-troubleshooting.md`") asserts universals that `ADOPT-08`, `ADOPT-05B`, and nine steps' inline or owning-step recoveries do not satisfy. This file is inside the bounded per-step working set, so an agent reads these definitions while executing every step
- [x] 2.2 Extract each of the above from the committed source via `git show HEAD:SPECBOOT_ADOPTION_GUIDE.md` rather than retyping, per D9
- [x] 2.3 Physically verify the file after writing: `wc -l`, heading list, tail check — do not trust the edit tool's success signal (§22 "Large documentation write truncates or replaces content")

## 3. One-Time Adoption Phase Files — Design Decisions D2, D4, D7

- [x] 3.1 Create `specboot-adoption/01-prerequisites-and-install.md` from current §1–§3, reshaped to the D4 step contract as `ADOPT-01`…`ADOPT-03`. `ADOPT-03` must state the import relationship explicitly (design D17): the general SpecBoot copy excludes hidden client directories by design, and `ADOPT-05B` is the mandatory step that provisions or reconciles the selected client's permission file afterward — permissions are not covered by the existing "OpenSpec, CodeGraph, or the adapter steps" routing
- [x] 3.2 Create `specboot-adoption/02-codegraph.md` from current §4 plus §5's CodeGraph-specific content only (everything above the `Configure Selected-Client Permissions` subsection), as conditional steps `ADOPT-04`/`ADOPT-05`
- [x] 3.3 Create `specboot-adoption/03-client-permissions.md` from §5's `Configure Selected-Client Permissions (Early, One-Time)` subsection as unconditionally mandatory step `ADOPT-05B`; confirm no "skip CodeGraph" path reaches this file (D7). `ADOPT-05B` must define how the permission file **arrives**, not assume it exists (design D17): (1) initialize/configure the selected client so its project-local structure exists; (2) locate the approved generic permission baseline for that client; (3) copy the baseline explicitly when the target permission file does not exist; (4) merge safely when the target already exists — never overwrite existing project configuration blindly
- [x] 3.4 State the CodeGraph adoption decision once as a decision node with a testable predicate and a recorded operator decision; reference it from both 3.2 and Happy Path 1, replacing the prose currently spread across §4, §5, Happy Path 1, and §21
- [x] 3.5 Create `specboot-adoption/04-context-and-openspec.md` from current §6–§8 as `ADOPT-06`…`ADOPT-08`, with `ADOPT-08`'s `On failure` pointer resolving to the "OpenSpec command not found" entry added in 5.4 alongside its existing pointers (design D13)
- [x] 3.6 Create `specboot-adoption/05-agents-and-skills.md` from current §9–§12 as `ADOPT-09`…`ADOPT-12`, keeping each adapt step adjacent to its validate step
- [x] 3.7 Create `specboot-adoption/06-adapters-and-discovery.md` from current §13–§15 as `ADOPT-13`…`ADOPT-15`
- [x] 3.8 Create `specboot-adoption/07-baseline-and-checkpoint.md` from current §16–§17 as `ADOPT-16`/`ADOPT-17`
- [x] 3.9 For every step in 3.1–3.8: apply the D4 field order exactly (`Condition` / `Purpose` / `Preconditions` / `Action` / `Approval gate` / `Validation` / `Evidence to record` / `On failure`). `Action` holds exactly **one of four forms** (design D4): **(1)** shell commands — expected at `ADOPT-01/-02/-03/-04/-05/-10/-12/-14/-16/-17`; **(2)** exactly one labeled canonical prompt — expected at `ADOPT-06/-07/-09/-11/-13/-15`; **(3)** shell commands that establish or version-gate the prompt's preconditions, followed by exactly one labeled canonical prompt — `ADOPT-08`; **(4)** a structured written procedure, where the work is provisioning and judgement rather than a single command or prompt — `ADOPT-05B`. Never more than one labeled prompt, never a historical prompt. **Confirm the form distribution against the tree rather than assuming it**, and record the form each of the 18 steps uses
- [x] 3.10 Open every phase file with the one-line "read `00-conventions.md` first" directive, so an agent loading a single file does not lose the cross-cutting rules
- [x] 3.11 Physically verify each file after writing (`wc -l`, heading list, tail); confirm no file exceeds ~300 lines
- [x] 3.12 Record — do not fix — any genuine defect discovered in a step while reshaping it, per design Non-Goals and `CLAUDE.md` §7; append findings to `reports/followup-candidates.md`
- [x] 3.13 In `ADOPT-05B`, require the operator to **declare the supported-environment matrix** — the clients, stacks, shells, and operating systems the adopting project and team support — and to reconcile the copied permission file against that declaration (design D17): remove permissions outside the matrix; **retain** variants required by every supported environment, including macOS, Ubuntu/Linux, and Windows where applicable, **even when unused on the machine performing the adoption**. Reconciling against the adopting machine instead is explicitly wrong and must be called out
- [x] 3.14 In `ADOPT-05B`, require adding **only** the additional permissions the real project needs, then verifying the resulting shared file contains no credentials, no personal absolute paths, no machine-specific dependency locations, and no unsafe broad command patterns; then validating the client-specific syntax (JSON for Claude, YAML for Kiro, or the selected client's own format)
- [x] 3.15 In `ADOPT-05B`, require fresh-session permission smoke tests for every supported client/OS combination that is **actually available**, and require every unavailable combination to be recorded as **pending evidence, never PASS** — consistent with `00-conventions.md`'s rule that an unexecuted command is FAIL, never an inferred PASS
- [x] 3.16 In `ADOPT-05B`, state that the approved **generic source baseline is left unchanged**: only the copy incorporated into the adopting project is reconciled. Mirror the existing warning that distinguishes the guide's illustrative YAML from a real permission file

## 4. Daily Workflow Pointer Reduction — Design Decision D6

- [x] 4.1 Create `specboot-adoption/08-daily-workflow.md` keeping the ordered six-capability sequence, the blocking-semantics summary (`enrich-us` blocks on `NEEDS CLARIFICATION`; both gates plus explicit human approval precede archive), and the `PENDING END-TO-END VALIDATION` marker
- [x] 4.2 Replace the detailed transcription with pointers to `ai-specs/specboot-instructions.md` and `openspec/specs/specboot-verification-workflow/spec.md`
- [x] 4.3 Verify no gate semantic stated in the reduced file is absent from a linked canonical source, and that no gate semantic present only in old §18 is lost — re-confirm against the counts recorded in design D6 before marking this group done

## 5. Support Files: Permissions Policy and Troubleshooting

- [x] 5.1 Create `specboot-adoption/19-permissions-policy.md` from current §19, keeping its existing cross-reference to the one-time setup now living in `03-client-permissions.md`
- [x] 5.2 Create `specboot-adoption/22-troubleshooting.md` from current §22, preserving all 17 existing symptom/cause/recovery entries
- [x] 5.3 Confirm the illustrative Kiro permission YAML moved verbatim into `03-client-permissions.md` and that the real `.kiro/settings/permissions.yaml` is byte-unchanged; re-verify after the D17 revision of that file, and additionally confirm the approved generic source baseline is byte-unchanged
- [x] 5.4 Add a dedicated "OpenSpec command not found" entry to `22-troubleshooting.md`, in the established symptom/cause/recovery shape (design D13), specifying: read-only diagnosis first — executable resolution (`command -v openspec`), npm global prefix (`npm prefix -g`), and `PATH` inspection — before any conclusion; explicit return to `ADOPT-02`, which owns installation; **[HUMAN APPROVAL REQUIRED]** renewed before any reinstall or environment mutation, because `ADOPT-02`'s original approval covered that execution and not a later recovery; and a full rerun of `ADOPT-08` after recovery rather than resuming mid-step
- [x] 5.5 Record the new entry in `reports/section-mapping.md` as net-new content that has no source block in the pre-restructure guide, so the mapping table stays a complete account of what each destination file contains
- [x] 5.6 **Re-audit every step's `On failure` against the three-form contract (design D13)**, not against "does it point into `22-troubleshooting.md`" — the earlier predicate is what let nine steps pass while the entry file and `00-conventions.md` asserted a universal they do not satisfy. For each of the 18 steps record which form it uses and the evidence that its recovery actually applies. Expected distribution to **confirm, not assume**: form A (inline, same step) — `ADOPT-01/-02/-03/-04/-05/-07` via `Failure`/`Recovery` tables and `ADOPT-16` via named inline guidance = 7; form B (specifically named `22-troubleshooting.md` entry) — `ADOPT-05B/-06/-08/-09/-11/-12/-13/-15/-17` = 9; form C (explicitly identified owning step) — `ADOPT-10` → `ADOPT-09` and `ADOPT-14` → `ADOPT-13` = 2. Confirm `ADOPT-16`'s reference ("the stale-output guidance above") resolves to an **identifiable named** target; if it does not, name that guidance in place rather than relocating it. **Do not duplicate working inline or owning-step recovery into `22-troubleshooting.md`** to make the form uniform. Report any step resolving to nothing applicable as a defect

## 6. History Extraction — Design Decision D9 (verbatim fidelity)

- [x] 6.1 Create `specboot-adoption/history/prompt-inventory.md` from current §20, including the relocated `HISTORICAL PROMPT — VERIFIED VERBATIM` blocks
- [x] 6.2 Create `specboot-adoption/history/reference-run-java-maven.md` collecting every per-section `Reference outcome` block plus §21's `Clean-run status` column, preserving the existing caveat that a future run must re-earn PASS rather than inherit it
- [x] 6.3 Prove byte-for-byte fidelity of every `VERIFIED VERBATIM` block by diffing the relocated text against its extract from `git show HEAD:SPECBOOT_ADOPTION_GUIDE.md`; save the proof to `reports/verbatim-fidelity.txt`. **Regenerate the proof against the final restructured tree** so recorded locations and occurrence counts are accurate — the existing file was generated before the entry-file rewrite and still reports 2 occurrences per block, listing `SPECBOOT_ADOPTION_GUIDE.md` as a location. Expect exactly **1** occurrence of each block, in `history/prompt-inventory.md` only
- [x] 6.4 Confirm every relocated historical prompt appears exactly once across the whole new set — no stray duplicate left in a live phase file
- [x] 6.5 Correct the prompt-inventory table's row 4 (finding **AR3**): it still records the Claude Code project-permission smoke test as "Still live in `ADOPT-05B` … — out of scope for this relocation", contradicting the same file's own "Relocated during the guide restructure" section and `reports/verbatim-fidelity.txt` (`occurrences: 1`, in `history/prompt-inventory.md` only). State what is true: the labeled verbatim block was relocated into this file, and `ADOPT-05B` presents the consolidated form. Do not alter the verbatim block itself

## 7. Run-Log Template Extraction — Design Decision D5

- [x] 7.1 Create `specboot-adoption/run-template/ADOPTION-RUN-LOG.template.md` collecting all 20 `Live validation record` blocks plus the `Live permission record`. Extend the `ADOPT-05B` evidence block with the fields design D17 requires: source baseline located, copy-or-merge decision and why, entries removed as out-of-matrix, entries added for the real project, the declared supported-environment matrix, safety-check and syntax-validation results, smoke-test results per client/OS combination with unavailable combinations marked **pending**, and remaining limitations
- [x] 7.2 Add a step-state checklist keyed by `ADOPT-nn` so an agent or operator can resume a partially completed adoption
- [x] 7.3 Replace each step's inline record block with a pointer to the run-log fields it must fill; confirm each block now appears exactly once in the whole set
- [x] 7.4 Document the filled-run-log lifecycle (committed as adoption evidence vs. kept local) as an operator choice; make no `.gitignore` change

## 8. Entry File Rewrite — Design Decisions D1, D3 (written last, indexes what now exists)

- [x] 8.1 Rewrite `SPECBOOT_ADOPTION_GUIDE.md` in place at its existing root path as the entry file: scope, purpose, reference status, and a pointer to `00-conventions.md`
- [x] 8.2 Keep `Happy Path 1` and `Happy Path 2` in the entry file as the at-a-glance sequences, with the CodeGraph-conditional markers and the non-conditional permission line intact
- [x] 8.3 Add the how-to-use section addressed to both audiences, including the agent-execution preamble stating the **bounded per-step working set** as exactly three files (design D12): `00-conventions.md`, the single phase file containing the current step, and the operator's filled copy of `ADOPTION-RUN-LOG.template.md`. State that loading all three is correct and never a boundary violation, and that unrelated phase, history, support, or canonical-workflow files stay out unless the current step's documented path requires one. The preamble also states: never self-approve an `[HUMAN APPROVAL REQUIRED]` gate; record evidence after each step; treat an unexecuted or failed command as FAIL. Resolve the prior contradiction in which the boundary excluded the run log while the same section required recording evidence into it
- [x] 8.4 Add the step index and the permanent §-number → step-ID → file map (D3), covering all 22 former section numbers
- [x] 8.5 Preserve the closing `Clean-Install Working Method` and `Final Acceptance Criteria` content, updating the companion-files list to reference the new run-log template. **Restore the `## Clean-Install Working Method` heading itself**, immediately before its clean-validation rules ("For the clean validation run:") — the rules survived the restructure but the heading was dropped, leaving the block unlabelled and absent from the entry file's heading list
- [x] 8.6 Save the full guide diff to `reports/guide-diff.patch` for reviewable evidence
- [x] 8.7 Add the plain-language **human operating model** to the entry file (design D14), answering all six orientation questions self-containedly — as explanations, not as pointers to where something is documented. A link may follow an answer to supply execution detail; it may not be the answer. Cover: adoption begins at `ADOPT-01` in an existing repository that may have no AI tooling installed; the procedure is sequential, with CodeGraph the only optional branch under the currently preserved methodology — when it is not adopted, `ADOPT-04` and `ADOPT-05` are skipped and mandatory `ADOPT-05B` still follows
- [x] 8.8 In that model, define **evidence** in plain language (design D14): the commands executed, their exit statuses, the relevant output, the files changed, the approval decisions taken, the validation results, and any recovery attempts — and explain that it lives in the filled run log so another person or agent can verify the adoption and resume it
- [x] 8.9 In that model, state the **operator and approver roles** (design D15): an AI agent can never approve its own mutation; an authorized human operator may make and record the decision personally; a human operator without sufficient authority must obtain approval from the designated repository owner or approver; approval remains limited to the named mutation and must be recorded
- [x] 8.10 In that model, state the **human failure flow** (design D14) and the **interruption/resume semantics** (design D16). Failure: mark the step FAIL, record the command and the error, follow the step's `On failure` pointer, and continue only after recovery and complete validation; if the failure is undocumented, stop, consult the selected AI client or the maintainer, record the diagnosis as a follow-up candidate, and add it to troubleshooting later through the governed change workflow — never by editing troubleshooting mid-adoption. Interruption: for a planned pause, finish and record the current step first; after an unexpected interruption, never infer completion — resume from the first step lacking complete PASS evidence, inspect its partial effects, and rerun its validation in full. **Reopened by D13:** the failure flow must state the **three-form contract**, not a `22-troubleshooting.md` universal. This governs both places the entry file states it — the plain-language "When something fails" paragraph and the orientation table's "What do I do on failure?" row — which currently read "which names the troubleshooting entry for that failure" and "Each step's `On failure` field points into `22-troubleshooting.md`". Both are false for nine of eighteen steps, and the paragraph immediately following tells the operator that an undocumented failure means stop and escalate, so a correct inline or owning-step recovery can be misread as no recovery at all. The undocumented-failure escalation itself stays exactly as written — it applies when **none** of the three forms resolves

## 9. External Reference Repair — Design Decision D10

- [x] 9.1 Repoint `README.md:101`'s §2/§13 references to the new locations
- [x] 9.2 Repoint `ai-specs/specboot-instructions.md:101,177` (§2, §13, §5-subsection, §19) to the new locations
- [x] 9.3 Apply the identical edit to `packages/specboot/template/ai-specs/specboot-instructions.md:101,177`
- [x] 9.4 Verify the two `specboot-instructions.md` copies still differ **only** at line 204 (image URL) — `diff` must show exactly that one hunk and nothing else
- [x] 9.5 Verify `packages/specboot/bin/init.js:160` still resolves correctly (it cites the guide by filename only; expected: no edit needed) — confirm, do not assume
- [x] 9.6 Rewrite all 32 internal anchor links as cross-file links using the inventory from 1.3
- [x] 9.7 Record the distributed-template reference limitation (finding **AR4**): `packages/specboot/template/` ships only `ai-specs/` and `docs/` — it contains neither `SPECBOOT_ADOPTION_GUIDE.md` nor `specboot-adoption/` — so the pointers added by 9.3 to `specboot-adoption/03-client-permissions.md` and `specboot-adoption/19-permissions-policy.md` do not resolve in an adopting repository. This is not a regression (the §-number text they replaced was equally unresolvable there, and the guide is deliberately undistributed), but criterion 11's `test -f` evidence holds **in this repository only**. State the limitation in the reference-repair evidence and in criterion 11's row; do not add the guide to the template, which the proposal excludes by decision

## 10. Mechanical Verification Suite (MANDATORY) — Design Decision D8

- [x] 10.1 Run the link checker over the new set: every relative link and anchor resolves against the actual heading set; zero dangling targets
- [x] 10.2 Check for within-file duplicate headings (identical headings across different files are expected and fine; within-file duplicates break anchor uniqueness)
- [x] 10.3 Re-run the 1.2 invariant counts against the new set and compare: `HUMAN APPROVAL REQUIRED` = 11, record blocks = 20, each prompt-label class unchanged **Re-derive again after 10.10–10.11**: the counts themselves are unchanged (20 approval markers, 20 record blocks, 18 troubleshooting entries, 4 labeled verbatim blocks), but `reports/invariant-reconciliation.md` is edited by those tasks, so re-derive and restate rather than carrying the figures forward
- [x] 10.4 Reconcile word count against the ~11,786 baseline, explaining the §18 reduction and any de-duplication rather than absorbing the delta silently **Re-confirm after 10.10**: the word-count table's cause column cites the restored `## Clean-Install Working Method` heading by finding ID and must read `VF1`, not `F1`; the counts (17,566 → 17,570) do not change
- [x] 10.5 Verify each of the 11 `[HUMAN APPROVAL REQUIRED]` gates is still attached to the specific mutation it guards, not merely present. **Re-derive every gate's `file:line` reference against the current tree and correct the stale ones in `reports/invariant-reconciliation.md`** — `01-prerequisites-and-install.md`, `03-client-permissions.md`, `04-context-and-openspec.md`, and `07-baseline-and-checkpoint.md` all gained lines after that table was written, so any reference into them may have shifted. **Second correction in the same report**: rename the section heading `Why the raw count is 18, not 11` to `Why the raw count is 20, not 11` — re-runs 2 and 3 moved the raw count 18 → 19 → 20 and the heading was never updated — and correct the explanatory-mention breakdown so the entry file contributes **4** mentions (`SPECBOOT_ADOPTION_GUIDE.md` lines 21, 31, 71, 102) and `00-conventions.md` contributes **2**, totalling **6**; the stated total of 6 is already correct, only the parenthetical detail is wrong. The 11 gates and their re-derived `file:line` references are unchanged
- [x] 10.6 Prove the untouched-file list with `git diff --exit-code` on `packages/specboot/package.json`, `.gitignore`, `.claude/settings.json`, `.kiro/settings/permissions.yaml`, `openspec/config.yaml`, `openspec/specs/`, `ai-specs/agents/`, and `ai-specs/skills/`
- [x] 10.7 Confirm `git diff --stat` shows changes confined to the guide, `specboot-adoption/**`, and the three reference-repair files from group 9
- [x] 10.8 Confirm every acceptance criterion in `enriched-work-item.md` is satisfied, recording the checking command per criterion. **Refresh all three stale figures in `reports/acceptance-criteria-verification.md`** so the criterion rows agree with its own re-run header and with the current tree: (1) relative-link count — criterion 8 still records **104**, current is **107**; (2) approval-marker count — criterion 5 still records **19 markers (11 preserved + 5 explanatory + 2 formalized + 1 new from D13)**, current is **20**, so correct the breakdown as well as the total (the additional marker is the D15 explanatory mention in the entry file's human operating model, not a new gate — the 11 real gates are unchanged); (3) run-template line count — criterion 3's stated exception still records **391**, current is **424**. Where a criterion is superseded by the expanded acceptance gates in group 16, record the supersession rather than editing `enriched-work-item.md`, which stays byte-identical to its staged source. **Second refresh — the criterion-3 line counts.** Correct the re-run header to state max working file **262** lines and entry file **277** lines, and re-derive the per-file table against the current tree: `04-context-and-openspec.md` **262**, `01-prerequisites-and-install.md` **259**, `03-client-permissions.md` **254**, `SPECBOOT_ADOPTION_GUIDE.md` **277**, `22-troubleshooting.md` **193**; the other nine rows are already correct and stay unchanged. Correct criterion 3's own row, which still records **260**, to **262** — it currently contradicts the re-run header in the same file. **No criterion verdict changes**: the largest working file remains below the ~300-line target and the run-log template's stated exception remains **424** lines. **Re-derive again after the D4/D13 corrections**: criterion 4's contract-field-label figure is wrong independently of them (finding **VF6** — it records 145; the actual count is 144 = 18 steps × 8 fields), and its verdict must now be re-derived against the corrected four-form `Action` contract rather than the superseded two-form rule. Criterion 11 must additionally carry the distributed-template limitation from task 9.7
- [x] 10.9 **Agent-evidence regression confirmation.** Agent attempt 2 was scored against the pre-D14/D16 entry file. Prove that the human-model additions left the agent-facing content byte-unchanged: the agent-execution preamble, the three-file bounded working set statement, the step index, the §-number map, every step's `On failure` pointer, `ADOPT-05B`'s unconditionally-mandatory statement, and the run log's step-state table — the last two because agent attempt 2 relied on them for its Q2 and Q3 answers, and both files are revised by D17. Diff each against its pre-edit form and record the proof. If any of them changed, the agent gate is reopened and tasks 16.1–16.7 and 16.16 are unchecked pending a new fresh-session attempt — do not assume the change is harmless **Re-confirm after 10.8, 10.10, and 10.11**: those tasks edit report prose only and touch no file under `specboot-adoption/` or the entry file, so the seven protected fragments stay byte-identical and the agent gate does not reopen. Restate the recorded limitation unchanged — the byte-identity proof is not independently reproducible while `specboot-adoption/**` is untracked, because no pre-edit form exists in git to re-diff. **Extend the protected set.** The seven fragments listed above are necessary but not sufficient: a byte-identity check over *named* fragments cannot detect newly added agent-facing text that contradicts them, which is how the entry file's failure paragraph came to assert a universal the steps do not satisfy while every protected fragment stayed unchanged. Add to the protected set the human operating model's **failure**, **approval**, and **evidence** prose, and `00-conventions.md`'s step-contract field table. Report a delta in any of them as a gate reopening, not as an acceptable edit — and note that the D13 corrections **will** produce such a delta by design, which is why task 16.24 requires a fresh attempt 3 unconditionally rather than conditionally
- [x] 10.10 **Separate the two `F`-prefixed identifier namespaces.** `reports/section-mapping.md` uses `F1`–`F5` for the five pre-restructure front-matter blocks; the verification findings reused the same IDs, so a grep for `F1` returns both meanings. Reserve `F1`–`F5` for the front-matter blocks — `reports/section-mapping.md` stays **byte-unchanged** — and rename the verification findings to `VF1`–`VF5`, updating every verification-finding reference in `reports/invariant-reconciliation.md` (the re-run 4 heading, the word-count cause column, and the task 10.9 statement) and in `reports/acceptance-criteria-verification.md` (the re-run 4 header). State the namespace rule once, where the collision is resolved, rather than editing the mapping report
- [x] 10.11 **Make every `VF` reference resolvable without the originating session.** Add a concise `Verification findings VF1–VF5` table to `reports/invariant-reconciliation.md` with columns ID, original finding, correction applied, and closing evidence (`file:line` where applicable). The assignment is fixed and recorded here — use it exactly, do not renumber it, and do not add a sixth finding: **VF1** — missing `## Clean-Install Working Method` heading; **VF2** — stale figures in `reports/acceptance-criteria-verification.md`; **VF3** — stale approval-gate `file:line` references; **VF4** — stale/incorrect occurrence evidence in `reports/verbatim-fidelity.txt`; **VF5** — stale status in `reports/PENDING-acceptance-gate-requests.md`. Take each row's correction and current closing evidence from what the change reports already record: `SPECBOOT_ADOPTION_GUIDE.md:225` for **VF1**; the three figures task 10.8 refreshed — relative-link count **107**, approval markers **20** with the corrected breakdown, run-log template **424** — for **VF2**; the 11 gate references re-derived under task 10.5 for **VF3**; `reports/verbatim-fidelity.txt` regenerated against the final tree, 4/4 byte-identical at exactly one occurrence each, for **VF4**; and the Request A **RUN → attempt 2 PASS** / Request B **NOT RUN** disposition header added under task 16.23 for **VF5**
- [x] 10.12 **Persist the outstanding verification and review evidence under `reports/`, with fixed identifiers.** Record the latest canonical `/specboot-verify` verdict — **PASS WITH GAPS**, zero Blocker, zero Major, four accepted Minor evidence-report inaccuracies — and the independent `/adversarial-review` verdict that produced this task set. Use this assignment **exactly**; do not renumber it and do not add a tenth entry: **VF6** — a criterion records 145 contract-field labels; the actual count is 144. **VF7** — the claim-integrity audit records an inaccurate artifact/report scope count (`human-orientation-acceptance.md` says "all 12 change artifacts"; the change holds 16 files — 4 root `.md` plus `.openspec.yaml` plus 11 reports). **VF8** — `PENDING-acceptance-gate-requests.md` contains stale unchecked-task wording ("Tasks 16.1–16.12 remain unchecked", contradicting its own status header). **VF9** — `section-mapping.md` records source range 2032–2046 although the source ends at 2045. **AR1** — the failure-path universal contradicts 9 of 18 steps. **AR2** — the `Action` contract excludes `ADOPT-08` and `ADOPT-05B`. **AR3** — `prompt-inventory.md` row 4 has stale relocation status. **AR4** — the distributed template points to guide files it does not ship. **AR5** — the latest `specboot-verify` verdict and accepted findings are absent from `reports/`. Preserve `F` (pre-restructure front-matter blocks), `VF` (verification findings), and `AR` (adversarial-review findings) as three **non-overlapping** namespaces; state the rule once where VF1–VF5 already state it
- [x] 10.12.1 **Provenance required with every persisted verdict** (proposal, "Verification-evidence persistence contract"): branch, `HEAD`, working-tree scope or fingerprint identifying the reviewed state, client/session evidence, the verdict, and the findings. Record it for the VF6–VF9 verify run and for the AR1–AR5 review, so each verdict identifies **which state it judged** — the reviewed tree is untracked, so a bare date is not provenance
- [x] 10.12.2 **Do not let recording a verdict invalidate it.** Persisting a byte-faithful gate record after the gate, where only the report record and its task checkbox change, leaves that verdict valid. A change to implementation files, to proposal or design semantics, to acceptance criteria, or to other validated evidence **does** invalidate the affected gate and requires a fresh run. Apply this test explicitly when closing 10.12: the VF6–VF9 corrections and AR1–AR5 edit implementation files and design semantics, so the affected gates are reopened here (10.1–10.9, 16.10, 16.21, 16.24) rather than treated as still-valid records
- [x] 10.12.3 **VF9 carve-out against task 10.10.** 10.10 requires `reports/section-mapping.md` to stay byte-unchanged; VF9's correction edits it. 10.10 protects the `F1`–`F5` **identifiers** from being reused or renamed, not a factual line range. Correct the C2 range to end at 2045; leave every `F` identifier untouched; restate the namespace rule rather than dropping it
- [x] 10.12.4 **Persist the two outstanding verdicts under `reports/`, with the provenance 10.12.1 requires** (branch, `HEAD`, working-tree scope or fingerprint identifying the reviewed state, client/session evidence, the verdict, and the findings). Record both in `reports/invariant-reconciliation.md`'s existing verification-evidence section, matching the precedent set by AR5, so the change stays at 16 files and VF7's artifact/report scope count stays correct. **Use this identifier mapping exactly — do not renumber it and do not infer identifiers later:** canonical `/specboot-verify` — **PASS WITH GAPS**, 0 Blocker, 0 Major, 3 accepted Minor: **VF10** stale Criterion 3 worst-case line counts, 160/262 instead of 208/265, with the 424-line run log omitted from the complete working-set figure; **VF11** `invariant-reconciliation.md` still records 145 field labels where the actual count is 144; **VF12** design D6 does not state the case-insensitive matching-line counting method. Independent `/adversarial-review` — **FAIL**, 1 Major, 6 Minor, 1 unresolved Question/assumption: **AR6** (Major) persisted derived figures have no reproducible regeneration mechanism; **AR7** stale explanatory-marker line references, entry 102→112 and conventions 77→125; **AR8** stale Criterion 3 operands and omitted run-log contribution; **AR9** remaining 145→144 inconsistency; **AR10** D6 counting method unstated; **AR11** `daily-workflow-pointer-verification.md` column label incorrectly claims all eight semantics occur in the reduced file; **AR12** latest `specboot-verify` verdict and findings are not persisted; **AR13** human archive decision with the orientation gate undischarged remains unresolved. Record the three equivalences explicitly — VF10 ≡ AR8, VF11 ≡ AR9, VF12 ≡ AR10 — and record that AR7, AR11, AR12 were reached only by the adversarial review and AR6 only by aggregating across cycles; that overlap-plus-remainder pattern is itself the evidence for AR6 and must not be flattened into a single merged list. Record the reviewer-provenance limitation the review stated about itself: session distinctness rested on that conversation's empty prior context, not on an externally attested identity, and the client was the same as the implementing session — weaker than a cross-client review, stronger than a same-session fallback.
- [x] 10.13 Re-run `openspec validate restructure-specboot-adoption-guide --strict` after all corrections in groups 2–10 and 16 are applied, and record the result. A strict-validation failure blocks archive consideration independently of every other check in this group
- [x] 10.14 **Give every derived-figure block in `reports/acceptance-criteria-verification.md` the command that reproduces it (design D19)**, runnable from the repository root against the working tree — no fixed line numbers, no intermediate files, no scratch paths. State the snapshot rule once in that file: the figure is a point-in-time snapshot, the command is the durable artifact. **Produce an explicit inventory covering every derived-figure block in the file**, one entry per block, each carrying: report file and section; the figures that block produces; the exact repository-root command; the command's exit status; and a comparison of the command's output against the persisted figures. **Reopened by AR14 — every `CURRENT` row must now meet design D19's literal-command standard**: it either contains the complete literal executable command, copy-paste runnable from the repository root exactly as written, or references exactly one stable command ID resolving **one-to-one** to a copy-paste executable fenced command block in the same report. No ellipses, no placeholders (`<n>`, `<file>`, `<label>`, `<path>`), no pseudo-commands, no prose stand-ins ("link checker", "extract each labeled block", "re-derived per step"), no vague references ("commands below combined", "same over the guide set"), and no unstated manual filtering ("with the negating-context matches excluded"). Multi-step checks carry an explicit ordered executable sequence; a check needing a script carries that script inline as a heredoc. The row's commands must prove **every** figure the row claims. **The complete violator list for this file is fixed — correct exactly these eight: `A1`, `A3`, `A4`, `A5`, `A7`, `A8`, `A9`, `A11`.** `A4` is a violator despite carrying a real command: that command reproduces the 144 field labels only, leaving the 18 steps, the 3 further prose matches, and the `Action` distribution 10/6/1/1 unproven, and it leans on "same over the whole guide set". `A2` is **clean** — its abbreviated hash `0405b6bd…` sits in the Figures field, not the Command field, and abbreviation there stays legal. `A6`, `A10`, `A12`, `A13`, `A14`, `A15` are clean and must not be churned. **Execute every command and compare its actual output *and* exit status against the persisted figures.** **This task FAILS if any derived-figure block in the file lacks an inventory entry, if any `CURRENT` row's command is not executable exactly as written, or if any command's output or exit status disagrees with its persisted figure** — a disagreement is corrected under 10.16 and the inventory re-run, never reconciled by editing the inventory to match.
- [x] 10.15 **Same for `reports/invariant-reconciliation.md` (design D19)**, under the identical inventory requirement: one entry per derived-figure block with report file and section, figures produced, exact repository-root command, command exit status, and comparison of produced output against the persisted figures. **FAILS if any derived-figure block lacks an inventory entry, if any `CURRENT` row's command is not executable exactly as written, or if any command output or exit status disagrees with its persisted figure.** This file carries the re-run tables, the 11-gate map, the raw-count decomposition, the word-count reconciliation, and the VF/AR finding tables — every one of those is a derived-figure block for this task's purposes, including the per-re-run invariant tables that were previously carried forward between re-runs. **Reopened by AR14 under the same literal-command standard as 10.14.** **The complete violator list for this file is fixed — correct exactly these nine: `B1`, `B2`, `B3`, `B4`, `B7`, `B8`, `B9`, `B12`, `B14`.** Their defects, so the correction is not rediscovered: `B1` placeholders `<n>` / `<file>` plus "for each of the 11"; `B2` "then each … resolved with"; `B3` ellipses in both `sed` commands; `B4` placeholder `<label>`; `B7` an ellipsis inside the alternation; `B8` "the B1/B4/B6 commands plus …" and "the link checker"; `B9` "per-step parse" with no command at all; `B12` "`shasum -a 256` over the 15 guide-set files" with the file list unstated; `B14` "with the negating-context matches excluded", an unstated manual filter. `B6` and `B13` are clean and must not be churned. `B5`, `B10`, `B11` stay **HISTORICAL** and exempt by D19's own scoping rule — they are preserved unchanged and must never be updated to match the present tree. Where a check genuinely needs a script — the link checker (`B8`) and the per-step `Action`-form parse (`B9`) are the two — that script's full source is embedded inline as a heredoc under a stable command ID, and the row references that ID.
- [x] 10.16 **Correct the six recorded evidence defects, re-deriving each figure against the tree at correction time rather than copying it from this task text.** (a) **AR7** — `reports/invariant-reconciliation.md`'s explanatory-mention breakdown cites `SPECBOOT_ADOPTION_GUIDE.md` line 102 and `00-conventions.md` line 77; both are stale (102 is a horizontal rule, 77 is unrelated prose). Correct to 112 and 125, and re-derive all six explanatory references, not only the two known-stale ones — this is a recurrence of VF3's class in the table VF3 corrected, caused twice by re-deriving the 11 preserved gate references and skipping the 6 explanatory ones. The 11 + 6 + 2 + 1 = 20 decomposition itself is correct and must be preserved. (b) **VF10 / AR8** — `reports/acceptance-criteria-verification.md`'s Criterion 3 worst-case operands 160 and 262 are stale against the same file's own line-count table; correct to 208 and 265, and state the run-log template's 424 lines as part of the complete D12 three-file working set rather than omitting it. (c) **VF11 / AR9** — the same file's sibling report records 145 field labels in the word-count source table while its own invariant row records 144; correct to 144 (18 steps × 8 fields). (d) **VF12 / AR10** — design D6's counting method: **already corrected in `design.md` during the planning-artifact update that created D19/D20; apply VERIFIES that D6 names the case-insensitive matching-line method and does not re-apply the edit.** (e) **AR11** — `reports/daily-workflow-pointer-verification.md`'s column is headed "Rule in reduced file" but 2 of its 8 rows ("Archive performs sync-then-archive internally" and "`openspec validate --strict` after archiving") appear nowhere in `specboot-adoption/08-daily-workflow.md`; retitle the column so it claims only what it proves. Criterion 10's verdict does not change — a semantic absent from the reduced file satisfies "no gate semantic absent from a linked source" a fortiori — and that reasoning is recorded rather than left implicit. (f) **AR12** — the persistence performed by 10.12.4.
- [x] 10.17 **Prove the scope boundary by checksum, not by `git diff`.** **Reopened by AR14: this correction opens a new evidence-only window, and the boundary must be re-proven around *this* window rather than inherited from the previous one — the earlier checksum pair scoped a correction that has already closed.** `git diff --exit-code` against `HEAD` is the wrong instrument here: all 15 guide-set files are already modified or untracked relative to `HEAD` by this change, so the check would fail trivially and prove nothing. Instead: capture SHA-256 for all 15 guide-set files (`SPECBOOT_ADOPTION_GUIDE.md`, the 11 files directly under `specboot-adoption/`, `history/prompt-inventory.md`, `history/reference-run-java-maven.md`, `run-template/ADOPTION-RUN-LOG.template.md`) **immediately before** the evidence-only work of 10.12.4 and 10.14–10.16 begins; capture them again after; **require all 15 to be byte-identical**. Additionally confirm no guide path was created and none removed, by comparing the full recursive path listing of `specboot-adoption/` plus the entry file before and after. Record both checksum sets and both path listings in the report. All 15 byte-identical plus an unchanged path set is what establishes that no guide file and no executable contract file changed, and therefore that the bounded agent gate (tasks 16.1–16.7, 16.16, 16.24–16.27, including agent attempt 3's PASS) and the mechanically validated implementation tasks 10.1–10.9 are **not** reopened. If any checksum differs or any path appears or disappears, this correction has left its scope: stop, and reopen the affected gate under the persistence contract rather than proceeding.
- [x] 10.18 **Record what is deliberately not corrected here.** `specboot-adoption/22-troubleshooting.md:157` still reads "Configure the **Section 5** permission files early", using retired numbering where the live target is `ADOPT-05B` in `03-client-permissions.md`. This is **already accepted** as follow-up candidate 5 in `reports/followup-candidates.md`, found by agent attempt 3, and it stays accepted and unfixed: correcting it would edit an implementation file after the gate that judged it, invalidating attempt 3's PASS under the persistence contract and requiring a fourth fresh attempt for a cosmetic reference. It is **not** part of this correction, and no new correction cycle is opened for it. Confirm the follow-up record still states this disposition; do not restate the finding as new. **Reopened by AR15 (≡ VF13): the record is incomplete, not wrong.** A **second** live stale reference exists at `specboot-adoption/19-permissions-policy.md:13`, which reads "…subsection at the end of **Section 5**" — the same retired numbering, in a live support file, describing a structure that no longer exists. Extend accepted follow-up candidate 5 in `reports/followup-candidates.md` to cover **both** live references (`22-troubleshooting.md:157` and `19-permissions-policy.md:13`) under the same accepted-and-unfixed disposition, and correct candidate 5's claim that agent attempt 3 was "the only reviewer to reach it" — attempt 3 reached one of the two. **Preserve the intentional historical aliases**: "formerly §N" / "Formerly section 5" wording in `SPECBOOT_ADOPTION_GUIDE.md`'s §-number map, in `README.md`, in both `specboot-instructions.md` copies, and in `history/prompt-inventory.md` is deliberate back-compatibility under design D3 and is **not** a stale reference — do not touch it. **Modify no guide file**: this is a `reports/` edit only, and the guide's two live references stay exactly as they are. Record why the widened scope was missed: the reproducing command for this figure was scoped to a single file (`grep -n 'Section 5' specboot-adoption/22-troubleshooting.md`), so it could not surface an instance elsewhere in the set — replace it with a guide-set-wide literal command under 10.15's standard.
- [x] 10.19 **Record the pending human archive decision (design D20).** State in `reports/` that the human orientation and self-description gate is self-declared blocking, is `NOT RUN`, and that its only human attempt stands as a permanent FAIL; that no human archive approval exists in any artifact of this change; and that the decision to archive with that gate undischarged is **pending, belongs to a human, and may never be inferred** from a gate verdict, from the absence of open findings, or from elapsed time. Record what would discharge it: a human statement under `reports/` accepting archive with the gate undischarged — carrying task 16.20's undischarged independent-review obligation forward unchanged — or deferring archive until an unexposed reviewer is available. Re-run task 16.21's claim-integrity grep across `proposal.md`, `design.md`, `tasks.md`, and every file under `reports/` extended with this check: no artifact asserts or implies archive approval.
- [x] 10.20 Re-run `openspec validate restructure-specboot-adoption-guide --strict` after 10.12.4 and 10.14–10.19 are applied, and record the result. Task 10.13 stays checked as the historical record of the earlier strict validation; this is a separate, later run against the corrected artifacts. A strict-validation failure blocks archive consideration independently of every other check. **Reopened by AR14/AR16**: this run now covers the AR14–AR16 corrections (10.14, 10.15, 10.17, 10.18, 10.22, 10.23) as well, and is the final strict validation of this correction round.
- [x] 10.21 **Record the human decision under design D20.** The decision arrived after the correction was applied and is the instrument D20 named as the only thing that could discharge the pending archive decision. Record it under `reports/` with its full scope and provenance: what is **accepted** (independent human-orientation validation remains `NOT RUN`), what is **deferred** (the obligation itself, to a future review by an unexposed developer, leaving task 16.20 open), and the **verified ground** — that this change claims only mechanical validation, agent navigability, and readiness for a controlled pilot, never independent human usability or end-to-end adoption, checked against `proposal.md` at the moment of recording rather than taken on trust. Record explicitly that the decision **resolves AR13** but is **not archive approval**, and that archive still requires a fresh `specboot-verify`, a fresh independent `adversarial-review`, and a separate explicit final human approval. Update AR13's disposition and the task 10.19 status record, preserving the pre-decision text as the evidence D20 was applied to. Report-only: no guide file, no executable contract file, and no readiness claim changes
- [x] 10.22 **Persist the fresh `specboot-verify` verdict and the Codex cross-client `adversarial-review` verdict under `reports/` (AR16).** Record both in `reports/invariant-reconciliation.md`'s existing verification-evidence section, matching the precedent set by AR5 and 10.12.4, so the change's report count and VF7's artifact/report scope figure stay correct. Each record carries the provenance 10.12.1 requires: branch, **full** `HEAD` (not abbreviated), the reviewed-tree scope or fingerprint identifying the state reviewed, client/session provenance, the verdict, and the findings. **Use this identifier mapping exactly — do not renumber it and do not infer identifiers later:** canonical `/specboot-verify` — **PASS WITH GAPS**, 0 Blocker, 0 Major, 1 accepted Minor: **VF13** accepted follow-up candidate 5 records only `22-troubleshooting.md:157`, while a second live stale "Section 5" reference exists at `19-permissions-policy.md:13`. Independent `/adversarial-review` — **FAIL**, 0 Blocker, 1 Major, 2 Minor, 0 unresolved questions: **AR14** (Major) the D19 inventories use non-literal or incomplete command fields, so the reproduction mechanism remains incomplete; **AR15** (Minor) candidate 5 omits the second live stale reference at `19-permissions-policy.md:13`; **AR16** (Minor) the latest verify and Codex cross-client adversarial-review verdicts are not yet persisted. **Record `VF13 ≡ AR15` as an equivalence without merging their provenance** — the two reviewers reached the same defect independently, from different clients, and flattening them into one entry destroys exactly the cross-client corroboration that makes the pair informative. Record the adversarial-review provenance verbatim: **Codex Desktop, cross-client, read-only, no subagent and no fallback**, branch `feature/restructure-specboot-adoption-guide`, full `HEAD` `0e6eb75d974d7851c948888225c0f252d05aac27`. Record explicitly that this is the **first cross-client** review of this change and therefore stronger provenance than the earlier same-client review, whose own recorded limitation was precisely that its session distinctness rested on empty prior context rather than an externally attested identity. Preserve the VF10–VF12 / AR6–AR13 records unchanged: this is a new round appended, never a rewrite of a prior one
- [x] 10.23 **Audit the literal-command standard mechanically, not by review (design D19, AR14).** Add to each of the two reports a check that every `CURRENT` inventory row's **Command** field — and every fenced command block its command IDs resolve to — satisfies the standard. The audit examines the Command field and its resolved blocks **only**: the Figures field may legitimately abbreviate a hash, and `HISTORICAL` rows are exempt by D19's scoping rule. The audit **FAILS** on any of: a command ID that resolves to no block, or to more than one; a prohibited token in a Command field (ellipsis, placeholder, pseudo-command, prose stand-in, vague cross-reference, unstated manual filter); a command that is not executable exactly as written; an unexpected non-zero exit status; or a command whose output does not prove **every** figure its row claims. The audit's own command must itself meet the standard it enforces — a prose description of this audit is the same defect it exists to catch. Record the audit's command, its exit status, and its output alongside the inventories

## 11. Review and Update Existing Unit Tests (MANDATORY)

- [x] 11.1 Review the existing test suite under `src/test/` for any test asserting on documentation paths or guide content
- [x] 11.2 Record the outcome: this is a documentation-only change touching no Java source, so no test is expected to require update — state the evidence (`git diff --name-only` shows no `src/` path), do not assert non-applicability without it

## 12. Run Unit Tests and Verify Database State (MANDATORY)

- [x] 12.1 Capture the pre-change baseline evidence: current branch, `git status --short`, and the last known baseline result
- [x] 12.2 Run the project baseline `mvn -o test` and record exit code plus test counts (reference baseline: 8 tests, 0 failures, 0 errors, 0 skipped)
- [x] 12.3 Verify database state: this repository's test suite uses no external database and this change touches no persistence code or `docs/data-model.md` — record the pre/post indicators as N/A with that justification rather than omitting the check
- [x] 12.4 Create the report `reports/YYYY-MM-DD-step-12-unit-test-and-db-verification.md` following the template in `docs/openspec-tasks-mandatory-steps.md`, including executed commands, results, and the recorded non-applicability
- [x] 12.5 Mark this group complete only after the baseline passes and the report file physically exists

## 13. Manual Endpoint Testing with curl (MANDATORY — recorded non-applicability)

- [x] 13.1 Record explicitly that this change adds, removes, and modifies no HTTP endpoint: `docs/api-spec.yml` is untouched and no controller source is in the diff — evidenced by `git diff --name-only`
- [x] 13.2 Confirm no curl testing is required, stating the evidence rather than silently omitting the group

## 14. E2E Testing with Playwright MCP (MANDATORY if applicable — recorded non-applicability)

- [x] 14.1 Record explicitly that this repository is backend-only (`docs/frontend-standards.md` — Not Applicable) and this change touches no user-facing workflow, so Playwright E2E does not apply
- [x] 14.2 Confirm the non-applicability with evidence rather than silently omitting the group

## 15. Update Technical Documentation (MANDATORY)

- [x] 15.1 Confirm `docs/data-model.md` and `docs/api-spec.yml` need no update (no data model or API contract change) — state the evidence
- [x] 15.2 Confirm no `*-standards.md` file needs a stack/tooling update (no dependency, build, or tooling change)
- [x] 15.3 Record the deferred follow-up candidate from design Open Questions: whether `docs/documentation-standards.md` should adopt the entry-file-plus-phase-files pattern as a repository convention — deferred pending a real adoption run, not done here

## 16. Bounded Fresh-Session Acceptance Gates (BLOCKING) — Design Decision D11

Both gates below are blocking: a FAIL in either blocks the change regardless of every mechanical check in group 10 passing. They are bounded checks of the restructured guide's navigability and self-description — see 16.11–16.12 for what they deliberately do not prove.

### 16.A Agent acceptance gate

- [x] 16.1 In a fresh client session given only `SPECBOOT_ADOPTION_GUIDE.md` (no other file supplied, no prior conversation context), confirm the agent identifies the correct starting step and stays within the bounded per-step working set (design D12): `00-conventions.md`, the single phase file containing the current step, and the operator's filled copy of `ADOPTION-RUN-LOG.template.md`. **Loading the run log is correct behaviour and must not fail this check.** Loading an unrelated phase, history, support, or canonical-workflow file is a FAIL unless the current step's documented path required it. Record every file opened, in order
- [x] 16.2 Confirm the agent follows the "CodeGraph not adopted" path correctly: it skips `02-codegraph.md` and still treats `03-client-permissions.md` (`ADOPT-05B`) as mandatory
- [x] 16.3 Confirm the agent resumes from a non-initial `ADOPT-nn` state supplied only through a partially filled run-log template, selecting the correct next step from the recorded state
- [x] 16.4 Confirm the agent stops at a `[HUMAN APPROVAL REQUIRED]` gate and does not self-approve, proceed past it, or treat its own reasoning as approval
- [x] 16.5 Confirm the agent identifies the evidence the step requires and records it in the correct run-log fields
- [x] 16.6 Supply an explicitly identified **synthetic** failed-or-unexecuted command outcome and confirm the agent classifies it as FAIL — never an inferred PASS from empty output — and follows the documented failure path into `22-troubleshooting.md`. Do not execute any installation, configuration, or adoption command merely to manufacture failure evidence; the outcome is supplied to the agent, not produced by running something
- [x] 16.7 Record in `reports/agent-acceptance.md`: the exact files loaded (in order), every decision made at each conditional and gate, transcript evidence for each of 16.1–16.6, an explicit note that the 16.6 outcome was synthetic and supplied, and a per-check PASS/FAIL

### 16.B Human orientation and self-description acceptance gate

- [x] 16.8 Record independent human-orientation **attempt 2 as `NOT RUN`** in `reports/human-orientation-acceptance.md`, with the reason: no independent reviewer was available. Never record it as PASS, never as N/A, never blank — `NOT RUN` with its reason is the accurate state (design D18), the same principle as `ADOPT-05B`'s `PENDING EVIDENCE`. Human attempt 1 stays permanently FAIL and is not reinterpreted
- [x] 16.9 For the approval question specifically, confirm the entry file explains the general approval semantics, how an approval gate is identified, and where step-specific gates are found — it must **not** duplicate the individual approval gates that live in the phase files
- [x] 16.10 Record the **bounded readiness claim** in `reports/human-orientation-acceptance.md` and confirm it matches `proposal.md` **as re-narrowed by the D4/D13 corrections**: this change is **mechanically validated**; **agent navigability is no longer claimed as validated** and readiness for a controlled pilot adoption is contingent on restoring it, because the corrections changed agent-facing prose that attempt 2 never read — a fresh attempt 3 (task 16.24) is required and attempt 2's PASS is not carried forward; and it remains **not** validated for independent human usability and **not** validated end-to-end. State that task 16.9's objective check verifies the document's content, not a reader's comprehension, and must not be reported as human validation

### 16.C Scope of the evidence

- [x] 16.17 Preserve the first human-orientation attempt in `reports/human-orientation-acceptance.md` as **human attempt 1**, with the six reviewer responses reproduced **verbatim** in the language given, and an overall verdict of blocking **FAIL** with all six checks FAIL
- [x] 16.18 Record human attempt 1 as a defect in the guide, not in the reviewer: do not reinterpret the responses into the expected answers, do not score them as partially correct, and do not attribute the failure to the reviewer. Record which orientation question each response maps to and what the entry file failed to supply
- [x] 16.19 Record the **deferred-validation contract** in `reports/human-orientation-acceptance.md` (design D18): the two unverified properties move to the **first real human-led adoption in another repository**, which must record (a) the operator's prior exposure to this guide, (b) the full evidence trail, (c) every deviation from the documented procedure, (d) every failure and its recovery, and (e) proposed improvements to the guide and to `22-troubleshooting.md`
- [x] 16.20 Record that a **future independent review by an unexposed developer** remains required and is **not** discharged by the pilot — a pilot operator who has read the guide to run it cannot also be its independent reviewer (design D18). Keep it as a separate, undischarged obligation
- [x] 16.22 Verify the **historical-versus-current distinction** is stated consistently wherever human attempt 1 is described (`proposal.md`, `design.md` D18, `reports/human-orientation-acceptance.md`): its FAIL is permanent and its transcript unaltered; its "blocking" was against the pre-D14/D15/D16 guide; all six failures were remediated by tasks 8.7–8.10; and it is preserved as historical defect evidence, **not** a current release blocker. Confirm no artifact leaves it readable as an open blocker
- [x] 16.23 Add a **status header** to `reports/PENDING-acceptance-gate-requests.md` recording the current disposition of both requests: **Request A — RUN, produced agent attempt 2 PASS** (see `reports/agent-acceptance.md`); **Request B — NOT RUN** under design D18, no independent reviewer available, obligation deferred and undischarged. **Preserve both reusable prompts and their expected-results tables verbatim** — the header records disposition, it does not retire the requests, which remain the instrument for the deferred independent review
- [x] 16.21 Verify that **no artifact overstates the claim**: grep `proposal.md`, `design.md`, `tasks.md`, and every file under `reports/` for any statement asserting human-usability validation or end-to-end validation, and confirm each surviving mention is explicitly negated or scoped. Report any that is not **Re-run after 10.8–10.11**: those tasks edit `reports/invariant-reconciliation.md` and `reports/acceptance-criteria-verification.md`, so re-grep all four artifacts plus every file under `reports/` and confirm the corrections introduced no new unqualified claim
- [x] 16.11 State explicitly in both reports that these are bounded navigability and self-description checks against a restructured document; they do not prove a complete real adoption. End-to-end validation will come from the next real SpecBoot adoption performed in another repository, and no PASS here may be reported as end-to-end validation
- [x] 16.12 State explicitly in `reports/human-orientation-acceptance.md` that it evidences entry-file orientation and self-description only — it does not prove complete human usability of the guide, nor successful human execution of an adoption
- [x] 16.13 Record that `enriched-work-item.md`'s narrower "Agent dry-run" testing line is superseded by the expanded gates in this group; the enriched artifact itself stays unchanged as the historical input record
- [x] 16.14 Preserve the first fresh-session attempt in `reports/agent-acceptance.md` as **attempt 1**, verbatim, with its overall verdict recorded as a blocking **FAIL** and tasks 16.1 and 16.6 recorded as **FAIL** — never retroactively passed, and never rewritten to match the corrected guide
- [x] 16.15 Record in the same report which two guide defects attempt 1 exposed (the load-boundary contradiction, design D12; the missing `openspec: command not found` recovery, design D13), and that both were fixed in the guide rather than by relaxing the gate
- [x] 16.16 Run a **second, genuinely fresh-session** agent attempt after groups 2, 3, 5, 8, and 10 are complete, recorded as **attempt 2** in the same report, in a session with no prior context and no knowledge of attempt 1. Only attempt 2's evidence may satisfy tasks 16.1–16.7; attempt 1's FAIL stands as permanent record

### 16.D Agent re-validation after the D4/D13 corrections

- [x] 16.24 **Run a genuinely fresh agent attempt 3 — unconditionally.** The D13 corrections change the entry file's failure prose and `00-conventions.md`'s field definitions, both agent-facing, so the state attempt 2 tested no longer exists and its evidence is not carried forward. Run attempt 3 **after** implementation and after the group-10 verification suite passes, in a session with no prior context, given only `SPECBOOT_ADOPTION_GUIDE.md`. Until it passes, **no artifact may state that agent navigability remains validated** — the readiness claim stays narrowed per `proposal.md` and design D18
- [x] 16.25 Attempt 3 must exercise the behaviour that actually changed, not re-run attempt 2's script. It must demonstrate, with transcript evidence for each: **(a)** one **form-A** recovery, including `ADOPT-16`'s named inline stale-output guidance; **(b)** one **form-B** recovery through a specifically named `22-troubleshooting.md` entry; **(c)** one **form-C** recovery through an explicitly identified owning step; **(d)** `ADOPT-08`'s commands-then-one-canonical-prompt `Action` form; and **(e)** `ADOPT-05B`'s structured-procedure `Action` form. Supply failure outcomes as explicitly identified **synthetic** inputs, as 16.6 requires — do not run installation, configuration, or adoption commands to manufacture them
- [x] 16.26 Two behaviours decide attempt 3, and both must hold: the agent must **not** classify a valid inline (form A) or owning-step (form C) recovery as undocumented, and it must **still stop and escalate** when none of the three forms resolves to an applicable recovery. Passing only the first would mean the correction taught the agent to improvise; passing only the second would mean the correction never landed
- [x] 16.27 Preserve attempts 1 and 2 in `reports/agent-acceptance.md` as historical evidence — transcripts and verdicts unaltered, attempt 1 permanently FAIL, attempt 2 permanently PASS **against the state it tested**. Attempt 3 is recorded as a third attempt, never as a correction or re-score of either. Apply the same rule to the human-orientation record: attempt 1 stays FAIL, attempt 2 stays `NOT RUN`

## Notes (not tracked tasks)

- Groups 13 and 14 are retained with recorded non-applicability rather than omitted, per `docs/openspec-tasks-mandatory-steps.md` and the precedent set by the archived `2026-08-07-consolidate-specboot-adoption-guide` change.
- Reports live at `reports/` in the change directory (not `specs/<change-name>/reports/`), matching that same archived precedent; this change sets `skip_specs: true`, so no `specs/` directory exists.
- Commit, push, and PR creation are explicitly out of scope for `apply` and remain separate, later, human-approved steps.
