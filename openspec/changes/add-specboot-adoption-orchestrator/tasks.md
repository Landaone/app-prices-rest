> **Implementation status at this revision (2026-08-12).** Before recalculation the apply run had
> completed **55 of 127** tasks. The apply run then discovered a real installer-test design defect
> (design **D-O**): the suite was state-dependent — after a canonical file changed, the first
> `npm test` failed on a stale assembled payload, and the `npm pack` test's `prepack` side effect
> repaired that state so a second invocation passed. Seven tasks whose evidence that defect
> invalidates have been **reopened** (4.2, 4.5, 4.6, 4.9, 4.10, 5.8, 5.9) and two RED tasks added
> (4.11, 4.12). Unrelated completed tasks stay checked. **No implementation file was changed by
> this revision** — the installer code and tests on disk still reflect the pre-correction state and
> are re-validated when group 4 is re-run.

> **Second revision, reverted (2026-08-12).** A `/writing-skills` session edited `SKILL.md` and
> `references/rationalizations.md` on the strength of three continuation scenarios (S-15, S-16,
> S-17). **All three agents passed.** `reports/skill-tdd-continuation.md` states plainly that this
> was *not* a RED baseline. Under the Iron Law — *no skill without a failing test first*, which
> binds edits as well as new skills — that evidence authorized no skill change, so the planning
> state it produced has been reverted: design **D-P**, the requirement *"Execution continues between
> gates and operator absence is not a gate"* and its four scenarios, and tasks **9.11-9.14** are
> removed. The three guide-side gaps those scenarios surfaced are preserved as **unimplemented
> improvement proposals** in `reports/skill-tdd-continuation.md`, feeding the D-K batch at task
> 21.10; none of them justifies a skill edit.
>
> Six tasks are **reopened** because the on-disk artifacts no longer satisfy them. **9.2** and
> **12.4**: `SKILL.md` measured **816 words** against their under-500 requirement; 9.2 now names the
> D-P sections to delete outright before the remainder is condensed. **12.1**:
> `references/rationalizations.md` gained a section explicitly labelled as having no RED baseline,
> violating "from the verbatim RED rationalizations captured in 2.3"; 12.1 now names it for removal.
> **9.8**: its execution-mode correction originated in the reverted revision and is re-justified
> independently below. **9.10** and **12.2**: the GREEN evidence was recorded against the 816-word
> file, and 9.2 both deletes sections and materially condenses what remains — a change of that size
> cannot be assumed to generalize, so the restored skill is revalidated against the **existing**
> S-1…S-14 baseline. No new scenarios are added. Apply must restore these files, not merely
> re-verify them. **No implementation file was changed by this revision.** Unrelated completed work
> is untouched.

> **Third revision — delivery modes (2026-08-13).** The canonical SpecBoot source now lives **outside**
> adopting projects and is supplied at run time, so the change gains a preferred **source-linked**
> delivery mode alongside the preserved **packaged-snapshot** mode (design **D-R**, **D-S**), an
> explicit human **client selection** step with a read-only autodiscovery route (**D-T**), and an
> improvement path that targets the canonical source rather than any local copy (**D-U**).
> **21 tasks are reopened** because their delivered artifacts describe one mode only, and **32 tasks
> are added**, including a RED-before-GREEN campaign for the seven delivery-mode behaviors in groups
> 4 and 5. Task counts move from **136 (124 done / 12 open)** to **168 (103 done / 65 open)**. **Group 21 stays open** until this correction is implemented and the pilot runs.
> Per design D-R the source path is **runtime input**: no task, recipe, phase file, or schema records
> a machine-specific path, and the path used for the pilot appears only in that run's evidence.
> **No implementation file was changed by this revision.**

> **Fourth revision — canonical entry prompt (2026-08-13).** Adoption previously started from whatever
> the operator typed, so an incomplete opening prompt forced improvised corrective patches — unrecorded
> instructions no artifact governs. `specboot-adoption/bootstrap-kit/ADOPTION-ENTRY-PROMPT.md` becomes a
> **required implementation deliverable** (design **D-V**): one canonical, parameterized entry prompt,
> complete in its parameters and gates and silent on procedure. **7 tasks are added** (3.10-3.13, 15.6-15.7,
> 21.16); tasks **3.1** and **3.2** gain the deliverable in their scope and were already open from the third
> revision, so no task is newly reopened. Task counts move from **168 (103 done / 65 open)** to
> **175 (103 done / 72 open)**. The prompt file itself is created during `/opsx:apply`, not by this revision.
> **`SKILL.md` is not edited and the skill TDD campaign is not reopened.**
> **No implementation file was changed by this revision.**

> **Fifth revision — cold-start contract for the entry prompt (2026-08-13).** The fourth revision
> described the entry prompt as one that "invokes the `specboot-adopt` skill", which describes a
> repository that has already been adopted. At the only moment the prompt is ever used, the target
> repository has **no SpecBoot, no OpenSpec, no `/opsx:*` commands, no discoverable `specboot-adopt`
> skill, and no `.specboot/` directory** — each is an *output* of the adoption. Design **D-V** and the
> entry-prompt requirement are amended accordingly: ordinary prompt text; delivery-mode question and
> three-artifact source validation (guide, phase directory, **readable `SKILL.md`**) before any
> orchestration load and before the first write; a **source-relative direct read** of that validated
> `SKILL.md` that is explicitly not native skill discovery; a zero-write stop on an invalid source;
> exact project mutations at a human-approval gate before provisioning; read-only autodiscovery that
> never authorizes configuration; a **bounded initial session** that stops after provisioning and
> generates the fresh-session handoff prompt; native discovery attempted only in that fresh session;
> **no OpenSpec or `/opsx:*` use** before `ADOPT-02` has completed and OpenSpec availability has
> explicitly passed; and a fresh session that resumes from the durable manifest and run log, verifies
> source identity, and blocks on drift. **8 tasks are added** (3.14-3.18, 15.8-15.9, 21.17) and **three
> already-open tasks are revised in scope** (3.7, 3.10, 3.13) — **no task is reopened**, since every
> task this correction touches was already open from the third and fourth revisions. Task counts move
> from **175 (103 done / 72 open)** to **183 (103 done / 80 open)**. The prompt file itself is still
> created during `/opsx:apply`. **`SKILL.md` is not edited and the skill TDD campaign is not reopened.**
> **No implementation file was changed by this revision.**

> **Sixth revision — the resolved source path is never committed (2026-08-14).** Task 13.11 required
> the committed tree to contain no absolute external path, while design **D-S** required the committed
> manifest to record the resolved source path as machine-specific-labelled execution evidence. Both
> could not hold, and the pilot's integrity run stopped on exactly that occurrence
> (`reports/source-linked-integrity.md`). Resolved with **option B**: the resolved absolute path is
> **machine-local runtime state and is never committed**. The committed manifest's `source` block
> retains **portable identity only** — delivery mode, guide checksum, skill checksum, the source Git
> commit or an explicit unavailable record with its reason, and an explicit statement that the local
> source path is resolved per machine and is not stored there. Where execution needs the path it is
> kept only in machine-local, git-ignored state under `.specboot/local/`: a convenience pointer
> holding the path and nothing else, never read as evidence, never an error when absent, removed by
> `ADOPT-18`. On resume the orchestrator obtains a local source path — from that store where one
> exists on this machine, otherwise by asking or rediscovering — and accepts it only when the recorded
> checksums, and the recorded commit where one exists, match; **no previously used path is ever
> required to exist**, because none is recorded. **10 tasks are reopened** (3.3, 3.17, 4.16, 4.22,
> 5.4, 5.13, 5.17, 5.18, 8.8, 15.6) because their delivered artifacts write, assert, or verify the
> removed `path` field, and **2 tasks are added** (4.23, 5.19) for the machine-local store and the
> no-committed-path assertion. Task counts move from **183 (164 done / 19 open)** to
> **185 (154 done / 31 open)**. Tasks 4.19, 5.15, 13.11, and 21.12 are revised in scope without being
> reopened. **The guide, the skill TDD campaign, task 11.4, and Group 21 are not reopened or rerun.**
> **No implementation file was changed by this revision.**

> **Seventh revision — source-linked only, and the refusals that make it safe (2026-08-14).** The
> change claimed readiness for a first controlled pilot while shipping two delivery modes, one of
> which had never been driven from a virgin repository to a discovered skill. **Scope narrows to
> source-linked adoption, validated for a Claude-only pilot** (design **D-R**, **D-W**).
> Packaged-snapshot is removed from the readiness claims, the entry prompt, the active requirements,
> the documentation, and the executable user flow, and is deferred **whole** to one named follow-up
> change, `add-specboot-packaged-snapshot-delivery`, whose required scope D-W enumerates. **Its
> historical evidence is preserved verbatim** — the RED/GREEN records and the 4.21 behavior-pinning
> baseline stay as history for a deferred mode rather than being rewritten to match a later scope
> decision. With no validated canonical source the CLI now **fails closed with zero
> target-repository writes**; it must not expose a partially implemented mode.
>
> Five correctness gaps are corrected in the same pass, each folded into the section that owns it
> rather than collected into a bugfix group: an **explicit supported client** is required before
> every bootstrap write, with a missing or unknown selection refusing and never recording a
> committed `undeclared` placeholder (**D-T**); a plain **pointer file is not a discovered skill**,
> so a client that cannot natively discover the external skill without symlinks fails closed before
> provisioning and reports the capability limitation (**D-D**); a **complete preflight** detects
> every collision at once, presents the exact mutation inventory for approval, and leaves no partial
> discovery entry, manifest, run log, ignore rule, or machine-local store on failure (**D-X**); **Git
> provenance gains a third disposition** — a working tree with uncommitted changes records
> `unavailable` with that reason rather than a HEAD that does not identify what the run read, with
> checksums remaining identity in every case (**D-S**); and **`ADOPT-18`'s written procedure and its
> executable behavior are reconciled** around replacement-before-removal, terminal dispositions, and
> refusal on unresolved replacement or unrecorded content (**D-C**).
>
> **The stale D-S option-A wording is swept out.** A resolved source path exists only in ignored
> `.specboot/local/` state — never in the committed manifest, the run log, the handoff prompt, or any
> other evidence — so the surviving "recorded only in that run's evidence, labelled machine-specific
> and non-portable" sentences in the guide, the kit README and manifest, both source-resolution
> references, the bootstrap/de-bootstrap reference, the root README, and both `specboot-instructions`
> files are corrections, not restatements.
>
> **Task 14.3 is reopened as a genuine false green**: `packages/specboot/template/ai-specs/specboot-instructions.md`
> is missing the entire "Bootstrap entry point" section that the live file carries, so the two are not
> synchronized except for the intentionally different image URL. **The false-green verification tasks
> are reopened** — the cold-start check, prompt consistency and coverage, installer-spec coverage,
> symlink-unavailable validation, and every report still certifying superseded behavior.
>
> **Codex (task 11.4) stays open and unexecuted**, recorded as a client-specific deferred obligation:
> `PENDING EVIDENCE` as a shipped recipe, `NOT SELECTED` for the Claude-only pilot, and therefore
> neither a blocker on this change nor something this change may mark PASS (**D-Q**). **Group 21 stays
> unexecuted**, and gains a **pilot precondition**: the shared canonical source must be Git-backed and
> byte-match the final committed guide, phase files, and skill. `/Users/landaeta/repos/specboot` is
> currently a **stale non-Git copy** — no `.git/`, a guide that differs from the committed one, and a
> kit missing `ADOPTION-ENTRY-PROMPT.md`, `source-resolution.md`, and `client-autodiscovery.md` — and
> **must not be used for the pilot**.
>
> **TDD applies to the corrected source-linked critical path only.** Six RED cases are added before
> their GREEN work: missing client, unknown client, a pre-existing collision with zero writes, dirty
> Git source provenance, unsupported no-symlink discovery, and a genuinely virgin source-linked cold
> start. The guide-wide and S-1…S-14 campaigns are **not** rerun: `SKILL.md` is not edited by this
> revision, and the delivery rules land in `references/source-resolution.md` and
> `references/bootstrap-and-debootstrap.md`, which is the same Iron-Law reasoning that governed the
> third and sixth revisions.
>
> Task counts, recalculated from disk both before and after, move from **185 (167 done / 18 open)**
> to **208 (133 done / 75 open)**: **34 tasks reopened** and **23 tasks added**. **No implementation
> file is changed by this revision.**

> **Eighth revision — the `ADOPT-18` replacement refusal is written but not executable (2026-08-14).**
> A bounded correction for one confirmed defect. **No governing semantics change**: design **D-C**
> and the delta requirement *De-bootstrap reconciles against the committed manifest* already require
> **replacement before removal**, and `10-debootstrap.md` already requires an unresolved replacement
> to record **FAIL** and leave the transient entry in place. `proposal.md`, `design.md`, and both
> delta specs are therefore **not modified** — the rule exists and is correct; only the
> implementation and its coverage are missing.
>
> **The defect.** `runDebootstrap()` in `packages/specboot/bin/init.js` contains **zero** references
> to `intended-permanent-replacement`. It removes every entry unconditionally and records
> `cleanup-status: removed`, so an entry whose permanent replacement is missing or unresolved is
> deleted rather than refused. That is the exact failure mode design D-C calls out: a removal that
> outruns its replacement leaves the repository worse than not running the step at all, because
> `ADOPT-13`'s skip-on-exist rule then makes the gap permanent and the only trace is a *skipped*
> line. **No active test block exercises this refusal** — the de-bootstrap coverage in
> `delivery-modes.test.js` walks the success path only, and task 13.6's container-refusal evidence
> is the *other* refusal, recorded against the earlier procedure.
>
> **1 task reopened** (5.26, because the active suite changes) and **3 added** (4.32 RED, 5.27 GREEN,
> 13.12 verification), plus **16.1 reopened** because the installer-to-spec coverage review must
> account for the new assertions. Task counts move from **208 (186 done / 22 open)** to
> **211 (184 done / 27 open)**. **Task 11.4 and every Group 21 task stay unexecuted.**
> **No implementation file, historical report, `SKILL.md`, or pilot repository is changed by this
> revision.**
>
> **The canonical pilot source cannot be created yet, and this revision is why.** Task 21.18 requires
> a Git-backed source that byte-matches this repository's **committed** guide, phase files, and
> skill. The implementation is **uncommitted**: `HEAD` is
> `c900394104ca1be31d59684d31b2b2aa63fbd2f1`, and `packages/specboot/bin/init.js` along with the
> phase files, kit, skill references, and documentation are all working-tree modifications. Creating
> a canonical source from the current committed state would publish the pre-adoption contract;
> creating one from the working tree would publish a defect this revision exists to correct.
> **Canonical-source creation happens only after this defect is corrected, validated, explicitly
> approved, and committed** — in that order, and none of those steps is authorized here.

## 0. Setup: Feature Branch (MANDATORY — FIRST STEP)

- [x] 0.1 Confirm the working branch is `feature/add-specboot-adoption-orchestrator` and record the starting HEAD; the branch already exists at `c900394104ca1be31d59684d31b2b2aa63fbd2f1`, so create it only if the check shows otherwise — never re-create or reset it
- [x] 0.2 Record `git status --short` as the pre-implementation baseline. The expected state is the untracked planning directory `openspec/changes/add-specboot-adoption-orchestrator/` and nothing else — not a fully clean tree, since this change's own artifacts are already on disk and uncommitted. Any tracked-file modification or any other untracked path is unexpected and must be explained before implementation starts

## 1. Baseline Capture and Amendment Mapping

- [x] 1.1 Capture the pre-change text of every contract passage this change amends: `02-codegraph.md`'s decision node, `ADOPT-17`'s "Do not push. Remote mutation is outside this guide entirely.", and `00-conventions.md` §Scope boundaries' remote-mutation line — quote them verbatim so the amendment diff is auditable
- [x] 1.2 Record `ADOPT-17`'s current precondition (`ADOPT-16 = PASS`) verbatim as the evidence for design D-L: it is why the reusable checkpoint protocol cannot be attached to `ADOPT-17`
- [x] 1.3 Record the current phase-file inventory (`00`–`08`, `19`, `22`) as evidence that `09`, `10`, and `11` are free, confirming design D-G
- [x] 1.4 Record the current `packages/specboot/template/` drift set with evidence (`code-auditing`, `enrich-us`, `using-git-worktrees` differ from canonical) as the warn-only baseline for design D-I; do not fix it in this change
- [x] 1.5 Record the environment constraints that bound what may be claimed: host OS is `Darwin 22.6.0` with no Windows host available, and the repository has no `.github/workflows/`

## 2. RED Baseline: Skill Test Campaign Before Any Skill Body Exists (TDD Iron Law)

- [x] 2.1 Read `ai-specs/skills/writing-skills/SKILL.md`, `testing-skills-with-subagents.md`, `examples/CLAUDE_MD_TESTING.md`, and `anthropic-best-practices.md`, and record the authoring and testing constraints they impose
- [x] 2.2 Write the 14 baseline scenarios S-1 … S-14 from the enriched work item §9.1, each combining 3+ pressures (time / sunk cost / authority / exhaustion), in the `CLAUDE_MD_TESTING.md` format
- [x] 2.3 Run every baseline scenario with subagents **before `SKILL.md` exists**, capturing each rationalization **verbatim** — paraphrasing a rationalization destroys the evidence the REFACTOR pass needs
- [x] 2.4 Record the RED results in `reports/skill-tdd-red-baseline.md`, including which disciplines failed and the exact rationalizations produced
- [x] 2.5 Confirm no `ai-specs/skills/specboot-adopt/SKILL.md` file existed at any point during group 2 — a RED baseline recorded after the skill exists is not a baseline

## 3. Bootstrap Kit: Manifest and Discovery Recipes

- [x] 3.1 Create `specboot-adoption/bootstrap-kit/` with a `README.md` stating what the kit is and that it contains references, never copies of canonical content. State that **source-linked is the only delivered mode** — no payload, nothing canonical copied — that a validated canonical source is a **precondition** rather than a branch, and that packaged-snapshot is **deferred to `add-specboot-packaged-snapshot-delivery`** and is not offered here (design D-R, D-W). **Name `ADOPTION-ENTRY-PROMPT.md` as the kit's single entry point** and state that an adoption starts from it (design D-V). *(Reopened, seventh revision: the delivered README describes two modes and preferred/fallback selection, and carries the superseded D-S option-A sentence at lines 63-64 — "recorded only in that run's evidence, labelled machine-specific and non-portable" — which option B removed. No committed artifact records the resolved path at all.)*
- [x] 3.2 Create the kit manifest listing canonical repository-relative paths plus the source-linked entry set — the canonical paths a discovery entry may point at. **Register `ADOPTION-ENTRY-PROMPT.md`** so the entry point is discoverable from the manifest rather than only from the README. Verify by inspection that it embeds no canonical content. The payload→target mapping is **retained as installer machinery for the deferred mode** and labelled as such, so nothing reads it as an available delivery path. *(Reopened, seventh revision: the manifest's `$comment` and its `recordedOnlyIn` value both carry the superseded option-A wording, and the payload mapping is presented as a live delivery path.)*
- [x] 3.6 State in the kit manifest and `README.md` that the kit is **client-agnostic**: it carries every recipe but provisions only the declared client's, and records the others `NOT SELECTED`. Confirm no kit file assumes a particular client, and that no adoption is required to configure or validate more clients than it selected. **Add the matching source-agnostic statement**: the kit assumes no particular canonical source location, and the source is runtime input. *(Reopened: client-agnosticism is stated, source-agnosticism is not, and a kit that hard-codes a source is exactly as broken as one that hard-codes a client.)*
- [x] 3.3 Create `specboot-adoption/bootstrap-kit/BOOTSTRAP-MANIFEST.schema.json` defining `path`, `source`, `checksum`, `ownership`, `mode`, `intended-permanent-replacement`, `cleanup-status`, and `final-disposition`, with the enumerations from design D-C. **Add the run-level `source` block** of design D-S carrying **portable identity only**: `delivery-mode`, `guide-checksum`, `skill-checksum`, and an explicit statement that the local source path is resolved per machine and is **not stored in the committed manifest**. The block carries **no field for a resolved absolute path**, and `additionalProperties: false` rejects one. **Model `git` with three dispositions, not two** (design D-S): a commit for a **clean** Git working tree; an explicit unavailable record with its reason where the source is not a Git working tree; and an explicit unavailable record with its reason where the source **is** a Git working tree with **uncommitted changes**, with any observed HEAD representable only as context explicitly labelled as not identifying the source content and **never** in the field a consumer reads as identity — the schema must make the dirty case representable and must make writing a bare commit for it invalid. **Add the client-selection record**: the route used (`manual` | `autodiscovery`), the clients selected, and the clients recorded `NOT SELECTED`, with **no placeholder value** such as `undeclared` permitted for a selection. Remove `pointer-file` from the `mode` enumeration, since a pointer file is never a discovery entry (design D-D). *(Reopened, seventh revision: the schema's `git` is two-state, so a dirty working tree records a HEAD that does not identify what the run read; and `mode` still admits `pointer-file` as a discovery entry.)*
- [x] 3.4 Write the discovery recipes **per client, as separate recipes** (design D-Q): Claude and Kiro from the symlink shapes those clients already use in this repository, and Codex from its own gate evidence. Each recipe carries only the client-*dependent* surface — project-instruction discovery path, skill exposure mechanism, permission-file format and reconciliation, native capability invocation, fresh-session procedure, observable model/reasoning evidence, and client-specific bootstrap/de-bootstrap operations. State that a recipe is applied **only when its client is declared SELECTED at `ADOPT-00`**, and that an unselected client's recipe is not executed. *(Reopened: the delivered text framed Codex as a candidate "pending group 11", which encodes the Codex-as-special-case reading D-Q removes; all three are now peer recipes, and selection — not gate status — decides which one runs.)*
- [x] 3.5 Rewrite the symlink-capability detection recipe for the **fail-closed** answer (design D-D): symlink support is capability-detected, and where symlinks are unavailable **and the selected client's recipe offers no native discovery mechanism that works without one**, the run stops **before provisioning**, reports the capability limitation naming the client, the mechanism, and what was attempted, and leaves the target repository byte-for-byte unchanged. State that a real pointer file naming the external path is **not a discovery entry**, never satisfies the discovery gate, and never permits the run to continue as though discovery were provisioned; it may be written only as operator-facing context, never recorded as `mode: pointer-file` discovery. A content copy remains forbidden outright. *(Reopened, seventh revision: the delivered recipe records a `mode: pointer-file` entry and treats it as satisfying source-linked discovery, which records a PASS for a client capability nobody observed.)*
- [x] 3.7 Write the **source-resolution recipe** in the kit: the operator supplies the canonical source path at run time; it is accepted only when `SPECBOOT_ADOPTION_GUIDE.md`, `specboot-adoption/`, and a **readable `ai-specs/skills/specboot-adopt/SKILL.md`** are all present — the skill artifact is validated as a readable file, not merely a present directory, because the initial session reads that file directly to obtain its procedure (design D-V); validation runs **before any orchestration instruction is loaded and before the first project write**; **a source that validates is the only way the run proceeds, and no source or an invalid source fails closed with zero target-repository writes** rather than selecting a fallback (design D-R, D-W); and the validated source is **read-only for the whole adoption**, including for improvement proposals. *(Reopened, seventh revision: the recipe describes packaged-snapshot as the no-source fallback, and lines 12-13 carry the superseded option-A sentence about a resolved path recorded in run evidence.)*
- [x] 3.8 Write the **client-autodiscovery recipe**: the read-only probes each client recipe contributes, the format in which findings are displayed, and the explicit guarantee that the probe writes nothing — no directory, no file, no permission entry, no discovery link — before a human selection exists. State that findings are **candidates, not authorization**, and that finding no candidate does not authorize proceeding with no client
- [x] 3.9 Confirm no kit file, recipe, schema, or example records a machine-specific canonical source path; grep the kit for absolute paths and record the result as evidence for design D-R
- [x] 3.10 Create `specboot-adoption/bootstrap-kit/ADOPTION-ENTRY-PROMPT.md` — the **single canonical, parameterized entry prompt** for starting an adoption (design D-V). It assumes only that it is invoked from the target repository, obtains every runtime input by asking, and describes **source-linked adoption only**: it asks for a canonical **source**, never for a delivery mode, and nowhere presents packaged-snapshot as a reachable path. It must be **executable end to end from a virgin repository**, stating each of the seven spine steps well enough to be performed without a corrective patch: **(1)** source question and three-artifact validation, refusing with zero writes when absent or invalid; **(2)** the source-relative **direct read** of the validated `SKILL.md`; **(3)** an explicit, supported **client selection**, refusing with zero writes when missing or unknown; **(4)** the complete **preflight** and the **exact mutation inventory** at a human-approval gate; **(5)** provisioning of exactly that inventory, all-or-nothing; **(6)** **stop and hand off** with the generated fresh-session prompt; **(7)** **fresh-session discovery** and resume from the durable manifest and run log with identity verified by checksum. It must further direct a run that: offers exactly two client-selection routes and states that autodiscovery never writes and never authorizes configuration; provisions only explicitly selected clients and records all others `NOT SELECTED`; keeps canonical orchestration client-agnostic; initializes and validates the durable manifest and run log; resumes deterministically; detects source drift and blocks; records failures, deviations, recoveries, and improvement proposals without editing the canonical source; routes accepted improvements through the governed follow-up workflow (design D-U); and never modifies model, reasoning effort, permission mode, or execution mode. *(Reopened, seventh revision: the delivered prompt opens with a delivery-mode question (Q1) and routes to packaged-snapshot on a *no* answer, and its spine has no preflight, no client refusal, and no fail-closed no-source path.)*
- [x] 3.11 State the **operating model** explicitly inside the prompt: one canonical entry prompt; the human responses expected at each explicit approval gate; fresh-session handoff prompts generated by the run rather than composed by the operator; deterministic resume from the run log; and **no improvised corrective prompt patches**, with the rule that an adoption which required one has found a gap in the prompt — recorded as an improvement proposal, never accepted as normal use
- [x] 3.12 Verify the prompt uses **placeholders and runtime questions only**: grep it for absolute filesystem paths and for any laboratory-specific or pilot-specific value, and record a zero result as evidence. A prompt carrying one operator's path would hard-code into the most-copied file in the kit exactly what design D-R forbids every other artifact to record
- [x] 3.13 Verify the prompt **does not duplicate the guide or the skill**: confirm it takes its procedure from `specboot-adopt` and names canonical rules by reference — where "invokes" means the **source-relative direct read** of 3.15, never a skill invocation the target repository could not resolve at cold start — and that it restates no `ADOPT` step contract, no checkpoint-protocol step, no manifest procedure, and no de-bootstrap ordering. **The seven-step spine of 3.10 is the specific risk here**: it names *what happens in what order*, which is a parameter-and-gate statement, and it must not slide into *how each step is performed*, which is the guide's. Completeness is in its parameters and gates, never in its procedure (design D-V). *(Reopened, seventh revision: the prompt is rewritten around the spine, so the recorded no-duplication inspection describes a file that no longer exists.)*

- [x] 3.14 Write the prompt's **cold-start preamble** (design D-V): state that at the moment the prompt is used the target repository has no SpecBoot files, no OpenSpec installation, no `/opsx:*` commands, no discoverable `specboot-adopt` skill, and no `.specboot/` directory, and that each of those is an *output* of the adoption rather than a precondition of it; state that the prompt is **ordinary prompt text** run as-is, requiring no slash command, no skill invocation, no command infrastructure, no installed package, and no pre-existing target-repository configuration; and state that it **never assumes `specboot-adopt` is installed in or discoverable from the target repository**. Confirm by inspection that no sentence in the prompt depends on infrastructure the adoption has not yet created
- [x] 3.15 Write the prompt's **opening order of operations**: request the **canonical source path** → validate `SPECBOOT_ADOPTION_GUIDE.md`, the `specboot-adoption/` phase directory, and a **readable** `ai-specs/skills/specboot-adopt/SKILL.md` → only then load any orchestration instruction or write to the target repository. There is **no delivery-mode question**. State that the initial session obtains its procedure by **reading that validated canonical `SKILL.md` at a source-relative path**, labelled explicitly as a direct read and **not** native skill discovery; and that a failed validation, or no source at all, stops the run with **zero target-repository writes** and reports that a validated canonical source is required — nothing to clean up because nothing was created, and no fallback offered. *(Reopened, seventh revision: the delivered order opens with the delivery-mode question and treats a missing source as selecting packaged-snapshot.)*
- [x] 3.16 Write the prompt's **preflight and pre-provisioning approval gate** (design D-X): before anything is written, every target path the selected client's recipe and the durable state will occupy is resolved and classified — absent, `pre-existing-untouched`, or colliding — and **all** collisions are reported together rather than discovered one failed write at a time. The gate then presents the **exact mutation inventory** — each path, created or modified, by what mechanism, and whether reversible — and the run stops for human approval, performs exactly that inventory and nothing outside it, and treats a path needing a different mechanism than the one approved as a **new gate** rather than an adjustment. State that a provisioning failure **restores the pre-provisioning state**, leaving no partial discovery entry, manifest, run log, ignore rule, or machine-local store. State at that point that read-only autodiscovery **never writes and never authorizes configuration**, so displayed findings are candidates that still require both an explicit human selection and this gate. *(Reopened, seventh revision: the delivered gate presents an intention with no preflight behind it, so collisions surface during provisioning and a failure leaves the half-state the gate was meant to prevent.)*
- [x] 3.17 Write the prompt's **stop-and-handoff boundary**: once the temporary discovery entries and the durable state (`BOOTSTRAP-MANIFEST.json` and the adoption run log) are provisioned, the initial session **stops** instead of continuing into the adoption steps, and generates the **exact** fresh-session handoff prompt; **native skill discovery is attempted only in that fresh session**; and the fresh session resumes from the durable manifest and run log, **obtains a local canonical source path** — from the machine-local `.specboot/local/` store where one exists on this machine, otherwise by asking the operator or rediscovering it — verifies source identity against the recorded checksums, and **blocks on drift**. The run's identity travels in durable state as checksums, never as a path embedded in the prompt text and never as a path in the committed manifest; the handoff prompt therefore carries no source path, and being asked for one on another machine is the ordinary case rather than a failure. *(Reopened: design D-S option B removes the resolved path from the committed manifest, so the resume the prompt hands off to must now acquire a local source path; the delivered prompt describes verification and drift but not acquisition.)*
- [x] 3.18 Write the prompt's **OpenSpec embargo** and its **single sequence**: no OpenSpec command and no `/opsx:*` command is used before the documented installation step (`ADOPT-02`) has completed and OpenSpec availability has **explicitly passed** its documented check, and OpenSpec's absence at cold start is the expected state rather than a blocker; and state the cold-start sequence **once**, for the one delivered mode, with no mode branch and no second copy of the guide or the skill body. *(Reopened, seventh revision: the delivered text states the sequence as mode-independent and serving both modes, a framing that no longer describes what the prompt offers.)*
- [x] 3.19 State the prompt's **four fail-closed refusals** explicitly, each with what the run reports and the guarantee that the target repository is byte-for-byte unchanged: **(a)** no canonical source supplied, or the supplied one fails the three-artifact validation; **(b)** no client selected; **(c)** a selected client with no recipe, reported together with the clients that are supported; **(d)** the selected client cannot natively discover the external skill without symlinks, reported as a capability limitation naming the client, the mechanism, and what was attempted. State that a refusal is a **clean stop, never a degraded success**, and that none of the four is resolved by the run choosing on the operator's behalf

## 4. RED: Installer Test Campaign Before Any Installer Change (design D-M)

> **Execution order within this group is not task-ID order.** Task IDs are stable identifiers, not
> positions — the same rule this change applies to `ADOPT` IDs. Run them in this order:
>
> **4.11 → 4.12 → 4.2 → 4.5 → 4.6 → 4.9 → 4.10 → 5.8 → 5.9**
>
> **4.11 and 4.12 come first, and 4.2 is BLOCKED until both are complete.** 4.2 adds
> `assemblePayload()`, which *is* the correction for the D-O order-dependence defect. Running it
> before 4.11 has reproduced that defect would destroy the very RED evidence 4.11 exists to
> capture — there would be no recorded failing first invocation, and the fix would be unverified.
> Tasks 4.1, 4.3, 4.4, 4.7 and 4.8 are already complete and unaffected by this ordering.
>
> **4.13 and 5.10 sit outside this chain entirely** — the flag-parsing defect is independent of the
> D-O state-dependence defect, so they may run at any point, in RED-before-GREEN order.
>
> **4.14-4.22 are the delivery-mode campaign** (design D-R, D-S, D-T). They run **after** 4.2 lands
> `assemblePayload()`, since several of them need a deterministic payload state, and every one of
> them obeys D-O: each establishes the state it needs in its own setup, none depends on another's
> side effect, and each must pass on a **first** invocation. Within the campaign, run **4.21 first** —
> the packaged-snapshot regression pins today's behavior *before* source-linked mode exists, so
> "preserved unchanged" becomes a measured claim rather than an assertion made afterwards.

- [x] 4.1 Add a `scripts.test` entry to `packages/specboot/package.json` invoking Node's built-in test runner (`node --test test/`); confirm no `dependencies` or `devDependencies` entry is added and record that `engines.node >= 18` already guarantees `node:test`
- [x] 4.11 **Reproduce the order-dependence defect as RED evidence, before correcting it.** From a state where a canonical source has changed since the last assembly, run `npm test` **once** and record that it fails; run it a second time and record that it passes. Capture both invocations verbatim in `reports/installer-tdd-red-baseline.md` as the defect this correction removes — a fix with no reproduced failure is unverified
- [x] 4.12 Record the two RED preconditions the corrected suite must satisfy: a **clean checkout** with no `bootstrap-payload/` present at all, and an **intentionally stale** assembled payload. Both must be exercised, and both are FAIL until the fixture step lands
- [x] 4.13 **RED: write the failing regression test for flag arguments before touching `bin/init.js`.** Invoking the CLI with `--help` (and with `-h`) must print usage and create **nothing**. Assert three things against a virgin fixture: the CLI exits 0, usage text appears on stdout, and the fixture tree is **byte-for-byte unchanged** after the invocation — specifically that no directory named `--help` or `-h` exists anywhere beneath it, and that no `.specboot/`, adapter, or symlink was written. The test must **FAIL against the current implementation**: `bin/init.js` performs no flag handling at all, so `const rawArg = process.argv[2]` (line 13) treats `--help` as the target directory and installs a full SpecBoot tree into it. Record the failing output verbatim in `reports/installer-tdd-red-baseline.md` — a fix with no reproduced failure is unverified
- [x] 4.2 Create `packages/specboot/test/helpers.js` providing temporary-fixture setup and teardown (make a scratch target directory, run the CLI as a child process, capture stdout/stderr/exit code, remove the fixture); assert fixtures are created outside the repository working tree. **Add `assemblePayload()`** — a deterministic fixture step that regenerates `bootstrap-payload/` from canonical sources on demand, so any test needing an assembled payload establishes that state itself (design D-O)
- [x] 4.3 Write the failing test for the `bootstrap` subcommand: invoking the CLI with `bootstrap` against a virgin fixture creates the transient payload under `.specboot/bootstrap/` and the durable `.specboot/adoption/BOOTSTRAP-MANIFEST.json`. **Scope the assertion to packaged-snapshot mode explicitly**, so it does not read as the universal contract for the subcommand once source-linked mode exists. *(Reopened: the delivered test asserts a payload unconditionally, which source-linked mode must not produce.)*
- [x] 4.4 Write the failing test for unchanged default invocation: invoking the CLI **without** `bootstrap` produces exactly today's install result and creates no `.specboot/bootstrap/` payload and no bootstrap exposure — capture today's behavior as the fixture's expected baseline before group 5 changes anything
- [x] 4.5 Write the failing test for canonical payload assembly: **call `assemblePayload()` in the test's own setup**, then assert every file in the assembled payload is byte-identical to the canonical source named for it in the kit manifest. The test must not depend on `prepack`, on another test, or on a prior invocation
- [x] 4.6 Write the failing test for drift detection: **call `assemblePayload()` in setup to reach a known-good state**, then mutate one payload file and require the drift check to fail (non-zero exit) for the kit payload, while pre-existing `packages/specboot/template/` drift produces a warning and does **not** fail — the split enforcing/warn-only behavior of design D-I. Determinism must not weaken the assertion: the test still proves genuine drift fails, and must be able to distinguish *drifted* from *never assembled*
- [x] 4.7 Write the failing tests for transient ignore provisioning, covering each path in the set `.specboot/staging/` and `.specboot/bootstrap/`: no existing `.gitignore`; existing `.gitignore` with unrelated content preserved in order; exact rule already present is a byte-for-byte no-op; repeated runs are idempotent; and a broader pre-existing rule that Git already reports as ignoring the probe leaves the file unchanged
- [x] 4.8 Write the failing test protecting the durable path: after any installer run, `.specboot/adoption/` is not ignored (`git check-ignore` reports it as not ignored) and no bare `.specboot/` or other broader pattern was ever written
- [x] 4.9 Write the failing test for packaged-file inclusion: **call `assemblePayload()` in setup, then run `npm pack --dry-run --json --ignore-scripts`** so the test cannot regenerate the payload on another test's behalf (design D-O); assert the output includes `bin/`, `template/`, and the assembled payload directory
- [x] 4.10 Run `npm test` in `packages/specboot/` and confirm the suite **fails** for the expected reasons, recording the output in `reports/installer-tdd-red-baseline.md`; a suite that passes here proves the tests assert nothing and must be corrected before group 5
- [x] 4.21 **RED: pin packaged-snapshot behavior before source-linked mode exists.** Capture the full packaged-snapshot end state against a virgin fixture — the payload tree, the manifest, the discovery entries, and the `.gitignore` result — as a recorded baseline, and write the regression test that asserts a later run reproduces it exactly. Run this **first** in the delivery-mode campaign: a regression written after the feature lands proves only that the code agrees with itself
- [x] 4.14 **RED: source-linked mode creates no copied guide, phase files, or skill body.** Against a fixture canonical source built in the test's own setup, invoke `bootstrap` in source-linked mode and assert the project contains **no** copied `SPECBOOT_ADOPTION_GUIDE.md`, **no** copied `specboot-adoption/` phase file, **no** copied skill body, and **no** `.specboot/bootstrap/` directory at all — asserting absence of the directory itself, not merely that it is empty
- [x] 4.15 **RED: an incomplete source is rejected before any write.** For each of the three required artifacts, build a fixture source missing exactly that one, invoke `bootstrap`, and assert the invocation is rejected **and the project tree is byte-for-byte unchanged**. A run that creates a manifest and then reports the problem fails this test
- [x] 4.16 **RED: the manifest records portable source identity and no path.** Assert the run-level `source` block carries `delivery-mode`, `guide-checksum`, `skill-checksum`, and the explicit statement that the local source path is resolved per machine and is not stored in the committed manifest — and assert it carries **no** resolved absolute path, with the schema rejecting one. Cover Git provenance both ways with two fixtures: a source that **is** a Git working tree records the commit; a source that is **not** records an explicit unavailable value **with its reason**, and the assertion fails on a missing field, an empty string, or any inferred commit value. *(Reopened: the delivered assertion requires the machine-specific `path` field that design D-S option B removes from the committed manifest.)*
- [x] 4.23 **RED: the resolved path lives only in machine-local ignored state.** Assert that after a source-linked bootstrap the resolved absolute path appears in **no** committed artifact — not the manifest, not the run log, not any other file the run writes into the tree. Where the run retains it for reuse, assert it is written under `.specboot/local/`, that `git check-ignore` reports that path ignored, and that the store holds the path and **nothing else** — no checksum, no commit, no delivery mode. Add the three resume cases: same-machine resume reuses the store without asking **and still recomputes and compares the checksums**; a deleted store causes no failure beyond the run asking for a source path again; and `ADOPT-18` removes the store
- [x] 4.17 **RED: only selected clients' discovery entries are provisioned.** With two clients present in the fixture and one selected, assert the selected client's discovery entries exist and point at the external canonical guide and skill, the unselected client has **no** artifact created anywhere, and the manifest records it `NOT SELECTED` rather than `PENDING EVIDENCE`
- [x] 4.18 **RED: autodiscovery performs no write before human selection.** Snapshot the fixture tree, run autodiscovery, and assert the tree is **byte-for-byte unchanged** while findings are reported — no directory, no file, no permission entry, no discovery link. Assert the same for a fixture where autodiscovery finds **nothing**, so an empty result cannot be read as authorization to proceed with no client
- [x] 4.19 **RED: source drift blocks resume.** Bootstrap against a fixture source, mutate the canonical guide, then resume and assert the run **stops for reconciliation** rather than continuing. Add the two companion cases: an unchanged source resumes normally, and a source moved to a **new path with matching checksums** resumes on the checksums alone — no path is recorded in the committed manifest, so none can be required
- [x] 4.20 **RED: de-bootstrap removes project-local links and blocks but never modifies the external source.** Checksum the whole fixture canonical source before the run; execute a source-linked bootstrap and de-bootstrap; then assert the project-local discovery entries are gone, any appended instruction block is **byte-restored**, the payload and container obligations are recorded `SKIPPED — source-linked mode`, `.specboot/adoption/BOOTSTRAP-MANIFEST.json` is still present with terminal dispositions, and the external source is **byte-identical**, file for file, to its pre-run checksums
- [x] 4.22 Record the **partial RED baseline** for this revision. Write or revise the assertions of tasks **4.16** and **4.23** *before* any of their GREEN changes is implemented, then run `npm test` in `packages/specboot/` and confirm both halves of the result: the assertions **introduced or reversed by 4.16 and 4.23 FAIL for their expected pre-implementation reasons**, and the already-implemented, unaffected regression tests from **4.14-4.15 and 4.17-4.21 remain PASS**. Record both halves in `reports/installer-tdd-red-baseline.md` — the expected failures with their reasons, and the unaffected passes with their counts. A **premature PASS** for a 4.16 or 4.23 assertion invalidates the RED evidence: the assertion asserts nothing and must be corrected before group 5. An **unexpected failure in an unaffected test is a regression**, not RED, and blocks GREEN implementation until it is resolved. *(Reopened: the campaign gains 4.23 and reverses 4.16's manifest assertion under design D-S option B, so the recorded RED baseline no longer describes the suite it certifies. Unlike the original full-suite RED, only 4.16 and 4.23 may fail here — the rest of the delivery-mode campaign is already GREEN and must stay that way.)*
- [x] 4.24 **RED: a missing client selection refuses with zero writes.** Against a virgin fixture and a valid fixture canonical source, invoke `bootstrap` with **no** client selection and assert three things: the invocation is rejected, the fixture tree is **byte-for-byte unchanged**, and no `.specboot/` directory, manifest, run log, discovery entry, ignore rule, or machine-local store exists anywhere beneath it. Assert additionally that **no committed artifact anywhere records a placeholder selection** — no `undeclared`, no default, no empty-string client — since the current packaged-snapshot path writes exactly that value and it must not survive anywhere in the delivered CLI
- [x] 4.25 **RED: an unknown client refuses with zero writes and names what is supported.** Invoke `bootstrap` with `--client <name-with-no-recipe>` against a virgin fixture and a valid source; assert rejection, a byte-for-byte unchanged tree, that the error names the unsupported client **and lists the supported ones**, and that **no** artifact was created for any client — in particular that the run did not substitute the nearest supported recipe
- [x] 4.26 **RED: a pre-existing collision is detected in preflight and produces zero writes.** Build a fixture where **three** of the paths the selected client's recipe would occupy already exist; invoke `bootstrap` and assert that **all three** collisions are reported together **before** any write, that the tree is byte-for-byte unchanged, and that the reported mutation inventory names each path with its operation, mechanism, and reversibility. Add the **all-or-nothing** half: force a provisioning failure after the first path is written and assert the fixture is restored to its pre-provisioning state — no partial discovery entry, no partial `BOOTSTRAP-MANIFEST.json`, no partial run log, no orphaned ignore rule, and no orphaned `.specboot/local/` store
- [x] 4.27 **RED: dirty Git source provenance is recorded honestly.** Build three fixture sources and assert one manifest outcome each: a **clean** Git working tree records the commit; a **non-repository** records `unavailable` with that reason; and a Git working tree with **uncommitted changes** records `unavailable` **with that reason**, writes **no** commit value into the identity field, and — where a HEAD is retained at all — carries it only as context explicitly labelled as not identifying the source content. Assert in the dirty case that the guide and skill **checksums are still recorded and still match the bytes actually read**, so identity survives while provenance stops overstating. The assertion must fail on an inferred commit, on a bare HEAD written to the identity field, and on an omitted reason
- [x] 4.28 **RED: unsupported no-symlink discovery fails closed before provisioning.** Force symlink creation unavailable against a valid source and a selected client whose recipe has no native discovery mechanism that works without one; assert the run **stops before provisioning**, reports the capability limitation naming the client, the mechanism, and what was attempted, and leaves the fixture **byte-for-byte unchanged**. Assert specifically that **no `mode: pointer-file` discovery entry is created and no content copy is made** — a pointer file recorded as a discovery entry must make this test fail
- [x] 4.29 **RED: a genuinely virgin source-linked cold start reaches provisioning and stops.** From a fixture repository holding **no** SpecBoot files, no OpenSpec, no `/opsx:*` commands, no discoverable `specboot-adopt` skill, and no `.specboot/`, drive the source-linked path end to end with a valid source and one explicitly selected supported client, and assert the observable end state: the durable `BOOTSTRAP-MANIFEST.json` and run log exist under `.specboot/adoption/`, the selected client's discovery entries resolve to the **external** canonical guide and skill, no copied guide, phase file, or skill body exists anywhere, `.specboot/bootstrap/` was **never created**, the resolved path appears in **no** committed artifact and only in ignored `.specboot/local/`, and the run **stopped** after provisioning rather than continuing into the adoption steps. This is the test that would have caught the cold-start gap, so it must fail before the correction lands
- [x] 4.30 **RED: no packaged-snapshot path remains reachable.** Assert that invoking `bootstrap` with **no** `--source` fails closed with zero target-repository writes and reports that a validated canonical source is required — no payload assembled, no `.specboot/bootstrap/` created, no manifest written. Retire the packaged-snapshot behavior assertions of **4.3, 4.4, and 4.21** from the active suite in the same commit that adds this one, moving them behind the deferred change rather than deleting them, and **preserve `reports/installer-tdd-red-baseline.md` and `reports/installer-tdd-green.md` verbatim** as history for a deferred mode. Record the retirement and its reason; rewriting past evidence to match a later scope decision is the fabrication this contract forbids everywhere else
- [x] 4.31 Record the **RED baseline for this revision** in `reports/installer-tdd-red-baseline.md`, as an additive section that supersedes nothing already written. Run `npm test` in `packages/specboot/` **once**, from a clean checkout, and record both halves: tasks **4.24-4.30** FAIL for their expected pre-implementation reasons, each reason named; and every unaffected source-linked regression from 4.14-4.20 and 4.23 remains **PASS**, with counts. A **premature PASS** for any of 4.24-4.30 invalidates that assertion and must be corrected before group 5. An **unexpected failure in an unaffected test is a regression**, not RED, and blocks GREEN until resolved
- [x] 4.32 **RED: an unresolved permanent replacement refuses, and the transient entry survives.** Exercise the **real `debootstrap` command** — not a reimplementation of its logic in the test — against a fixture bootstrapped normally, then edit its committed manifest so exactly **one** entry carries an `intended-permanent-replacement` naming a path that does not exist or does not resolve. Leave every other entry untouched, so a failure cannot be confused with a whole-run abort. Assert all five: **(a)** the invocation returns the documented failing result — a non-zero exit, with the reconciliation report naming the entry and the replacement it was waiting on; **(b)** the **transient entry is intact** on disk, byte-for-byte, still resolving exactly as it did before the run; **(c)** **no unrelated path was removed** — snapshot the whole fixture tree before and after and assert the only differences are ones the step is permitted to make, so a refusal that silently deleted a sibling fails here; **(d)** that entry's manifest record shows **neither `cleanup-status: removed` nor a successful `final-disposition`** — a refusal that dispositioned the entry as done while leaving it on disk is worse than either outcome alone, because the manifest then disagrees with the filesystem; **(e)** **durable evidence survives for reconciliation and resume** — `.specboot/adoption/BOOTSTRAP-MANIFEST.json` and the adoption run log are both present and readable, and the manifest still names which entries reached terminal dispositions and which did not, so another operator or session can continue from the record. This test **must fail against the current implementation**: `runDebootstrap()` contains **zero** references to `intended-permanent-replacement`, removes the entry unconditionally, and records `cleanup-status: removed`. Capture the failing output verbatim in `reports/installer-tdd-red-baseline.md` as an additive section — a fix with no reproduced failure is unverified

## 5. GREEN: Installer Bootstrap Subcommand and Ignore Provisioning

- [x] 5.1 Add an explicit `bootstrap` subcommand to `packages/specboot/bin/init.js`, leaving the default install path's client-provisioning loop byte-for-byte unchanged. **Accept the canonical source path as runtime input** and route to the selected delivery mode; the default install path stays unchanged in both modes. *(Reopened: the delivered subcommand has one delivery path and no way to be given a source.)*
- [x] 5.2 Implement pack-time payload assembly from the canonical paths named in the kit manifest (`prepack` or equivalent), so no second copy is checked into git
- [x] 5.3 Implement the drift check: fail when any assembled or checked-in payload file differs from its canonical source; enforcing for the kit payload, warn-only for the pre-existing `packages/specboot/template/` drift recorded in 1.4
- [x] 5.4 Extend the ignore provisioning to the transient path set `.specboot/staging/`, `.specboot/bootstrap/`, and `.specboot/local/`, applying the existing `git check-ignore` probe and conservative fallback **per path** rather than a literal-line match. *(Reopened: `.specboot/local/` is the machine-local source-path store introduced by design D-S option B; it is provisioned by the same per-path probe, and the rule must never widen to a bare `.specboot/`, which would swallow the durable committed evidence.)*
- [x] 5.5 Verify `.specboot/adoption/` is never ignored and never has a rule provisioned for it, and that no broader `.specboot/` pattern is ever written — a broader rule would swallow the durable committed evidence
- [x] 5.6 Add the assembled payload directory to `packages/specboot/package.json` `files`
- [x] 5.7 Add `.specboot/bootstrap/` to this repository's `.gitignore`, leaving the existing `.specboot/staging/` rule unchanged, and confirm `.specboot/adoption/` remains tracked via `git check-ignore`
- [x] 5.8 Run `npm test` in `packages/specboot/` and confirm every test written in group 4 now **passes**, recording the output in `reports/installer-tdd-green.md`. The record must show a **first-invocation pass from a clean checkout**, a **first-invocation pass from an intentionally stale assembled-payload state**, and a **repeated pass** — with exact test counts for each. A pass that required a second invocation is not GREEN
- [x] 5.9 Confirm no test was weakened, deleted, or skipped to reach GREEN — compare the test file list, `test(...)` block count, and `assert.*` count against the group 4 RED record, and record **zero** skipped, `.todo`, or removed assertions. Specifically confirm the drift-detection assertion still fails on genuine drift and was not relaxed to accommodate the fixture change
- [x] 5.10 **GREEN: make 4.13 pass.** Handle `--help` and `-h` in `bin/init.js` **before** target resolution — print usage, exit 0, write nothing — so no argument beginning with `-` is ever silently accepted as a target path. Re-run `npm test` in `packages/specboot/` and confirm 4.13 passes on a **first invocation from a clean checkout**, per the D-O rule that no test may depend on a prior invocation's side effects. Confirm the default (`init.js <target>`) and `bootstrap` paths are byte-for-byte unchanged by comparing against the group 4 baseline fixtures, and record the result in `reports/installer-tdd-green.md`. **Do not delete the `--help/` directory already present in this working tree** — it is evidence of the defect, and its removal is a separate operator decision recorded in the run log, not a side effect of the fix
- [x] 5.11 **GREEN: make 4.21 and 4.15 pass — source resolution and mode selection.** Resolve the canonical source from the operator-supplied runtime input, validate the three required artifacts **before the first project write**, select source-linked when a local source is supplied and validates and packaged-snapshot when none is, and reject an incomplete source without creating anything. Record no machine-specific path in any shipped file
- [x] 5.12 **GREEN: make 4.14 pass — the source-linked bootstrap path.** Provision the selected client's temporary discovery entries pointing at the external canonical guide and skill, write durable state under `.specboot/adoption/` only, and create no `.specboot/bootstrap/` and no copy of any canonical artifact. Where symlinks are unavailable, write a `mode: pointer-file` entry rather than a content copy
- [x] 5.13 **GREEN: make 4.16 pass — portable manifest source provenance.** Write the run-level `source` block with the delivery mode, both checksums, either the source Git commit or an explicit unavailable record carrying its reason, and the explicit statement that the local source path is resolved per machine and is not stored in the committed manifest. Write **no** resolved absolute path into the manifest. Never infer, invent, or omit the Git value, and never record the external source as a manifest entry. *(Reopened: the delivered implementation writes the machine-specific `path` field into the committed manifest, which design D-S option B forbids.)*
- [x] 5.19 **GREEN: make 4.23 pass — the machine-local source-path store.** Write the resolved absolute path only to machine-local, git-ignored state under `.specboot/local/`, holding the path and nothing else; provision its ignore rule through the same per-path probe as 5.4; read it on resume when it exists on this machine and otherwise ask the operator or rediscover; never read it as evidence of identity and never treat its absence as a failure; and remove it in `ADOPT-18` alongside the other project-local temporary state
- [x] 5.14 **GREEN: make 4.17 and 4.18 pass — selection before configuration.** Implement the read-only autodiscovery probe that writes nothing and the display-and-wait handoff, provision artifacts only for explicitly selected clients, and record every unselected client `NOT SELECTED`
- [x] 5.15 **GREEN: make 4.19 pass — drift detection on resume.** Recompute the guide and skill checksums against the source supplied at resume, compare against the manifest, stop for human reconciliation on mismatch, and accept a new local path whose checksums (and recorded commit, where one exists) match — no path is recorded in the committed manifest, so none can be required to exist
- [x] 5.16 **GREEN: make 4.20 pass — source-linked de-bootstrap.** Remove the project-local discovery entries, byte-restore any appended instruction block, record the payload and container obligations `SKIPPED — source-linked mode`, write back terminal dispositions to the committed manifest, and leave the external canonical source byte-identical
- [x] 5.17 Run `npm test` in `packages/specboot/` and confirm every test from tasks 4.14-4.21 **and 4.23** now **passes**, recording the output in `reports/installer-tdd-green.md`. Per design D-O the record must show a **first-invocation pass from a clean checkout** and a **first-invocation pass from an intentionally stale assembled-payload state**, with exact test counts. A pass that required a second invocation is not GREEN. *(Reopened: the campaign gains 4.23 and 5.19 and reverses 4.16/5.13 under design D-S option B, so the recorded GREEN evidence no longer covers the suite.)*
- [x] 5.18 Confirm no test was weakened, deleted, or skipped to reach GREEN across the delivery-mode campaign — compare the test file list, `test(...)` block count, and `assert.*` count against the 4.22 RED record, and record **zero** skipped, `.todo`, or removed assertions. Confirm specifically that the packaged-snapshot regression (4.21) still asserts the full captured end state and was not relaxed to accommodate source-linked mode. *(Reopened: the comparison is against the 4.22 RED record, which is itself reopened; a count comparison against a superseded baseline proves nothing.)*
- [x] 5.20 **GREEN: make 4.24 and 4.25 pass — the client refusals.** Require an explicit, supported client selection before **every** bootstrap write, on every code path. Refuse with zero target-repository writes when none is selected and when a selected client has no recipe, naming the supported clients in the second case. Remove the `selectedClient: 'undeclared'` placeholder and every other default that lets an unmade selection reach the committed manifest; refuse instead. Both refusals precede the preflight, since there is nothing to preflight until the run knows whose recipe applies
- [x] 5.21 **GREEN: make 4.26 pass — preflight and all-or-nothing provisioning.** Resolve every target path the selected client's recipe and the durable state will occupy, classify each as absent, `pre-existing-untouched`, or colliding, and report **all** collisions together before the first write. Present the exact mutation inventory — path, operation, mechanism, reversibility — at the approval gate, and perform exactly that inventory. On any provisioning failure, restore the pre-provisioning state so no partial discovery entry, manifest, run log, ignore rule, or machine-local store survives
- [x] 5.22 **GREEN: make 4.27 pass — honest Git provenance.** Determine the source's working-tree state before reading its HEAD, and write the three-state disposition of design D-S: commit for a clean tree; `unavailable` with its reason for a non-repository; `unavailable` with its reason for a tree with uncommitted changes, with any retained HEAD carried only as labelled context and never in the identity field. Never infer, invent, or omit a value. Leave the checksums unchanged in all three cases — they are computed over the bytes actually read and remain identity
- [x] 5.23 **GREEN: make 4.28 pass — fail closed where discovery is unsupported without symlinks.** Detect symlink capability, consult the selected client's recipe for a native discovery mechanism that works without one, and where there is none, stop **before provisioning** and report the capability limitation naming the client, the mechanism, and what was attempted. Remove the `mode: pointer-file` discovery entry and the code path that creates it; a pointer file may remain only as operator-facing context, never recorded as a discovery entry
- [x] 5.24 **GREEN: make 4.30 pass — remove the packaged-snapshot path from the CLI.** Fail closed with zero target-repository writes when no canonical source is supplied, reporting that a validated canonical source is required. Remove the packaged-snapshot bootstrap branch from the user-reachable surface so the CLI cannot enter a mode this change does not deliver. **Retain the pack-time payload assembly and the drift check** as installer machinery for `add-specboot-packaged-snapshot-delivery`, with their tests still passing and clearly labelled as serving a deferred mode; retiring the mode must not silently retire the drift protection the kit depends on
- [x] 5.25 **GREEN: make 4.29 pass — the virgin source-linked cold start.** Drive the corrected path end to end and confirm the observable end state 4.29 asserts, including the stop after provisioning and the generated fresh-session handoff. This is the first task in the change that proves the critical path is executable rather than described
- [x] 5.26 Run `npm test` in `packages/specboot/` and confirm every test from tasks 4.24-4.30 **and 4.32** now **passes**, recording the output in `reports/installer-tdd-green.md` as an additive section. Per design D-O the record must show a **first-invocation pass from a clean checkout**, with exact test counts. Confirm no test was weakened, deleted, or skipped to reach GREEN — compare the test file list, `test(...)` block count, and `assert.*` count against the 4.31 **and 4.32** RED records, account for the retirements of 4.30 **by name and with their reason**, and record **zero** unexplained removals and zero `.todo` or skipped assertions. *(Reopened, eighth revision: the active suite gains 4.32, so the recorded GREEN counts no longer describe the suite they certify.)*
- [x] 5.27 **GREEN: make 4.32 pass — verify the permanent replacement before removing anything.** In `runDebootstrap()`, for **every** entry carrying an `intended-permanent-replacement`, confirm that replacement **exists and resolves** *before* the entry's removal — a dangling symlink resolves to nothing and is not a replacement. Where it does not resolve: record **FAIL**, **leave the transient entry in place**, leave that entry's `cleanup-status` non-terminal rather than `removed`, write **no** successful `final-disposition` for it, **preserve** the committed manifest and the run log so the step is resumable, **report the reconciliation** naming the entry and the unresolved replacement, and **exit unsuccessfully**. Entries whose replacements do resolve keep their existing behavior, and dispositions already reached stay written back — a refusal is a clean stop, not a rollback of work that was correct. Do not widen the change: this task adds the verification gate design **D-C** already requires, and nothing else

## 6. New Phase Files

- [x] 6.1 Create `specboot-adoption/09-bootstrap.md` defining `ADOPT-00` in the full step contract form (Condition / Purpose / Preconditions / Action / Approval gate / Validation / Evidence to record / On failure), with exactly one `Action` form and a `[HUMAN APPROVAL REQUIRED]` gate before the first repository-local write. **In order, ahead of that gate**: canonical-source resolution from runtime input with its three-artifact validation and a **fail-closed stop with zero writes** where no source validates; **an explicit, supported human client selection**, with a missing or unknown selection refusing before any write and no placeholder ever recorded; and the **complete preflight** of design D-X, whose exact mutation inventory is what the gate presents. Provisioning performs exactly that inventory and **restores the pre-provisioning state on failure**. There is one delivery mode, so the step has no mode branch. **The step's `Evidence to record` field, and every in-body instruction about what provenance to write, must name portable evidence only** (design D-S): the delivery mode; the guide checksum; the skill checksum; the **three-state** Git disposition — commit for a clean working tree, or an explicit unavailable record with its reason for a non-repository **and** for a working tree with uncommitted changes; and the statement that the local source path is resolved per machine and is **not committed**. Neither may ask for the resolved absolute path. *(Reopened, seventh revision: the step still selects between two delivery modes, has no preflight and no client refusal, and its Git evidence is two-state, so a dirty source is recorded as though its HEAD identified what the run read.)*
- [x] 6.2 Create `specboot-adoption/10-debootstrap.md` defining `ADOPT-18` as a structured written procedure that **matches the executable behavior clause for clause** (design D-C), including: **verify-replacement-before-removal** — no entry carrying an `intended-permanent-replacement` is removed until that replacement is verified to exist and resolve, and an unresolved replacement is **FAIL** with the entry left in place; the `pre-existing-untouched` protection; **terminal dispositions written back** for every entry into the committed manifest, with `pending` a FAIL rather than a partial PASS and unexercised obligations recorded `SKIPPED — source-linked mode` rather than blank or inferred; **refusal over guessing** — an unresolved replacement and unrecorded content in scope for removal both stop for human reconciliation; **resumability** — a refused run names which entries are terminal and which are not, so another session can resume it; and the `SKIPPED — no bootstrap performed` path. Source-linked mode removes only the project-local temporary discovery entries and the machine-local `.specboot/local/` store, byte-restores any file it appended a block to, **preserves** the committed manifest and run log, and leaves the external canonical source untouched — enforced by the source never being a manifest entry. The container procedure is retained, labelled as governing the deferred packaged-snapshot mode, and is not exercised by a source-linked run. *(Reopened, seventh revision: the written procedure and the executable behavior diverge on replacement-before-removal, on which dispositions are terminal, and on what a refusal leaves behind; and the step is still framed as a per-mode split between two live modes.)*
- [x] 6.3 Create `specboot-adoption/11-e2e-pilot-and-pr-gate.md` defining `ADOPT-19` (real-project E2E pilot on a human-chosen task) and `ADOPT-20` (PR-readiness gate blocking any PR until `ADOPT-00`…`ADOPT-19` are PASS)
- [x] 6.4 Give `ADOPT-20` a full step contract of its own rather than a clause inside `ADOPT-19` (design D-N): its validation checks the recorded PASS evidence for `ADOPT-00`…`ADOPT-19` **individually**, an empty evidence block for any step blocks readiness, and it is the only step authorized to declare pull-request readiness
- [x] 6.5 State in `10-debootstrap.md` and `11-e2e-pilot-and-pr-gate.md` that `ADOPT-18` and `ADOPT-19` each form their own checkpoint under the shared protocol — `ADOPT-17` is the final checkpoint of the adoption itself, not the last checkpoint of the workflow
- [x] 6.6 Verify each new file declares its steps' IDs without renumbering any existing `ADOPT-01`…`ADOPT-17`
- [x] 6.7 Give `09-bootstrap.md` and `10-debootstrap.md` acceptance criteria for the **one delivered mode**: `.specboot/bootstrap/` is **never created**, and the payload and container obligations read `SKIPPED — source-linked mode` as an explicit recorded outcome, never left blank and never inferred as PASS. Add the **refusal criteria**: a run with no validated source, no client, an unknown client, or unsupported no-symlink discovery ends with the target repository byte-for-byte unchanged. State in both files that the external canonical source is read-only for the whole adoption, and that a step which wrote to it is FAIL. *(Reopened, seventh revision: the criteria are written as a per-mode pair judging two live modes, and none of the four refusals has an acceptance criterion.)*

## 7. Contract Amendments (BREAKING — must land with the skill)

- [x] 7.1 Replace `02-codegraph.md`'s "is CodeGraph being adopted?" decision node with a mandatory capability-selection step: capability required, product a choice, no usable capability is FAIL with no waiver and no `PENDING EVIDENCE`; remove the file's conditional framing and its `SKIPPED`-when-not-adopted path
- [x] 7.2 Add the **reusable checkpoint protocol** to `00-conventions.md` as a single named procedure that any step can invoke by name, placed there because `00-conventions.md` is already inside every step's bounded three-file working set (design D-L). It must define, in order: validate → declare ready for human review → checkpoint approval gate → stage only intended files with no unconditional `git add -A` → apply the staged-scope checklist → fresh independent review after any correction → commit → determine and report remote impact → separate push approval gate → push to the current working branch → record improvement proposals → record checkpoint-ledger evidence
- [x] 7.3 State in that protocol that it applies after **every** independently validated `ADOPT` step or justified group, that the two approvals are distinct and neither carries forward, and that unknown or unapproved remote impact blocks the push
- [x] 7.4 Amend `ADOPT-17` in `07-baseline-and-checkpoint.md` to **invoke** the shared protocol by name instead of defining checkpoint behavior itself, and replace "Do not push. Remote mutation is outside this guide entirely." accordingly
- [x] 7.5 Preserve `ADOPT-17`'s `ADOPT-16 = PASS` precondition and its position after `ADOPT-16` as the final checkpoint **of the adoption itself**; confirm no artifact repurposes that precondition to gate earlier checkpoints, and that no earlier checkpoint is made to depend on the project baseline having run
- [x] 7.6 State in the protocol that being the adoption's final checkpoint does not exempt later steps: `ADOPT-18` (which commits the updated manifest) and `ADOPT-19` each form their own checkpoint under the same protocol; confirm no artifact reads `ADOPT-17` as the last checkpoint of the workflow
- [x] 7.7 Amend `00-conventions.md` §Scope boundaries so remote mutation is inside the contract under gates and company policy, rather than outside it entirely
- [x] 7.8 Add the working-set rule to `00-conventions.md`: the orchestrator skill body belongs to the executing agent's context, not the step's three-file working set, and its `references/` load on demand only (design D-H)
- [x] 7.9 Verify no phase file restates the checkpoint protocol's normative steps — each must invoke the single definition, so the contract has exactly one statement of the procedure
- [x] 7.10 Make `ADOPT-06`, `ADOPT-09`, and `ADOPT-11` unconditional consumers of the selected code-graph capability, replacing "use CodeGraph when available" wording
- [x] 7.11 State the forward-only applicability of both amendments: adoptions started after they land are bound; already-adopted repositories are recorded as pre-amendment and never retroactively marked FAIL
- [x] 7.12 Update `SPECBOOT_ADOPTION_GUIDE.md`: step index, happy path, section-number map (new rows, no rewrites), "How this works", resume/record table, and acceptance criteria. **State the source-linked delivery architecture**: one mode, how the source is resolved and validated, that a run with no validated source fails closed, and that the source location is runtime input the guide itself never records. **Extend the resume/record table** with the drift check. **Correct lines 58-59**, which carry the superseded D-S option-A sentence — the resolved path appears in *no* run evidence, committed or otherwise, only in ignored `.specboot/local/` state; identity is the checksums. *(Reopened, seventh revision: the guide describes two delivery modes and carries the option-A wording option B removed.)*
- [x] 7.13 Add pointer from `08-daily-workflow.md` to `ADOPT-19`
- [x] 7.14 Update `22-troubleshooting.md` entries for: bootstrap discovery failure; a real file blocking a canonical symlink; Windows symlink privilege failure; a partially consumed manifest; a supplied source failing the three-artifact validation; source drift detected on resume; and a resume with no local source path on this machine — resume **reuses the ignored `.specboot/local/` store when present**, otherwise **asks the operator or rediscovers a source**, then **verifies that candidate against the portable identity** and **blocks on mismatch**; an absent store is the ordinary cross-machine case, never a failure. **Replace the symlinks-unavailable entry**: the answer is a **fail-closed stop with the capability limitation reported**, never a pointer file presented as discovery and never a content copy (design D-D). **Add four refusal entries**: no canonical source supplied; no client selected; an unknown client selected; and a preflight collision — each stating that the target repository is byte-for-byte unchanged and what the operator supplies to proceed. *(Reopened, seventh revision: the symlinks-unavailable entry still offers the pointer file and packaged-snapshot as answers, and no entry covers the client or collision refusals.)*

## 8. Run-Log Template Extensions

- [x] 8.1 Add run-log blocks for `ADOPT-00`, `ADOPT-18`, and `ADOPT-19`, plus step-state table rows for the new IDs. The **`ADOPT-00` block** records the client selection and its route, the preflight's collision findings, the exact approved mutation inventory, and any refusal with its reason — and records **no resolved source path**, since the run log is committed. The **`ADOPT-18` block** carries terminal dispositions including `SKIPPED — source-linked mode`. *(Reopened, seventh revision: the `ADOPT-00` block asks for "the resolved source" and a delivery mode, and has nowhere to record the preflight, the approved inventory, or a refusal.)*
- [x] 8.2 Add the Checkpoint ledger block recording per checkpoint: id; the step or justified group with its written justification; validation results; evidence pointers; ready-for-review declaration; approval (who, when, what); exact staged file list; commit SHA; remote-impact assessment and verdict; push status; and improvement proposals raised
- [x] 8.3 Add the Improvement proposals block with status values `proposed | accepted | rejected | applied-in-change-<id>`
- [x] 8.4 Add the Client/company adaptations block (adaptation, reason, blast radius) and the Code-graph capability selection block (implementation, version, availability evidence, limitations)
- [x] 8.5 Replace the "where to keep the filled copy is your team's choice" paragraph with the decided default `.specboot/adoption/ADOPTION-RUN-LOG.md`, committed
- [x] 8.6 Remove the `SKIPPED`-valid-for-`ADOPT-04`/`ADOPT-05` note, which the CodeGraph amendment retires
- [x] 8.7 Confirm the existing "Daily workflow pilot" evidence block is reused as the `ADOPT-19` evidence sink rather than duplicated
- [x] 8.8 Add the **Source and delivery mode** block: delivery mode; guide checksum; skill checksum; the **three-state** Git disposition — commit for a clean working tree, or an explicit unavailable record with its reason for a non-repository **and** for a working tree with uncommitted changes, with any observed HEAD recorded only as context explicitly labelled as not identifying the source content; the explicit statement that the local source path is resolved per machine and is not recorded here; and the drift-check result at each resume, with the reconciliation decision when drift was found. The block provides **no field for a resolved absolute source path**. *(Reopened, seventh revision: the delivered block is two-state on Git, so a dirty source is recorded in the committed run log as though its HEAD identified the content the run read.)*
- [x] 8.9 Add the **Client selection** block: the route used (`manual` | `autodiscovery`); the findings displayed when autodiscovery ran; the clients the human selected; and the clients recorded `NOT SELECTED`. State that an autodiscovery finding alone is never recorded as a selection

## 9. GREEN: The Orchestration Skill

- [x] 9.1 Create `ai-specs/skills/specboot-adopt/SKILL.md` with frontmatter `name: specboot-adopt` and a third-person `description:` starting "Use when…" that states **triggering conditions only** — no workflow summary, since a summarizing description becomes a shortcut taken instead of reading the skill; ≤ 500 characters, total frontmatter ≤ 1024, letters/numbers/hyphens only
- [x] 9.2 Restore `SKILL.md` to valid content and bring it under 500 words (verify with `wc -w`). **First, remove every instruction introduced by the reverted design D-P**: the entire §"The only reasons to stop" section; the entire §"Red Flags — proceed if you think" mirror-image list; and the Common Mistakes row "Pausing between steps for a go-ahead | Ungated steps run straight through to the next real gate". Removal is **by origin, not by keyword** — re-read the reverted revision's diff and strip anything else it introduced. **Explicitly excluded from this removal:** the execution-mode wording in non-negotiable #8, which task 9.8 re-justifies on independent pre-existing grounds and which stays. **Then** reduce the remaining valid content to under 500 words **without changing its established behavior** — condense wording only; do not drop, weaken, or merge any of the ten non-negotiables, the Quick Reference rows, the surviving Common Mistakes rows, the §"Red Flags — stop if you think" list, or the `REQUIRED SUB-SKILL` line. Body contents remain: Overview with the core principle, When to Use, Quick Reference table, Common Mistakes, rationalization table, and Red Flags; flowcharts only where a decision is non-obvious. *(Reopened: measured 816 words. The 900-word budget that replaced this line came from the reverted revision and is withdrawn.)*
- [x] 9.3 Verify `SKILL.md` does not restate the guide's step content — a second authority would drift from the contract
- [x] 9.4 Create `references/checkpoint-protocol.md` describing how the orchestrator **drives** the canonical protocol — when to invoke it, how to assemble the ready-for-review declaration, how to fill the checkpoint ledger, and how to carry improvement proposals — while pointing at the `00-conventions.md` definition as the authority. It must **not** restate the protocol's normative steps (design D-L)
- [x] 9.5 Create `references/bootstrap-and-debootstrap.md`: per-client discovery, manifest fields and semantics, and the `ADOPT-18` procedure. Its "act only on manifest entries — never on path patterns" rule must state the container exception in the same breath: entries govern content, the verified-empty container is removed by name as the final act, and a non-empty container is FAIL. **Bring it into line with 6.2's reconciled `ADOPT-18`**: replacement verified before removal with an unresolved replacement FAIL and the entry left in place; terminal dispositions written back for every entry; refusal rather than a guess on unresolved replacement or unrecorded content; and a refused run resumable from the committed manifest. State that source-linked de-bootstrap removes the project-local entries **and the machine-local `.specboot/local/` store**, byte-restores appended blocks, and that the external source is never an entry and therefore never removable; and that the container procedure governs the deferred packaged-snapshot mode only. Carry the **fail-closed** no-symlink answer of design D-D, not a pointer-file entry. *(Reopened, seventh revision: the reference describes two live modes, admits a `mode: pointer-file` discovery entry, and does not state the replacement-before-removal, terminal-disposition, or refusal rules the executable step must follow.)*
- [x] 9.6 Create `references/portability-matrix.md`: client × OS × shell command equivalents, with declared PowerShell equivalents for `cp -rn`, `find -L`, `readlink`, and `ln -s`, and the shell-native array / explicit per-item guidance that avoids the recorded word-splitting failure mode
- [x] 9.7 Create `references/evidence-and-run-log.md`: run-log field mapping and the resume protocol, including that an empty evidence block means "not known to have completed"
- [x] 9.8 State in `SKILL.md` the prohibition on changing `model`, reasoning effort, permission mode, **or execution mode**, scoped to the orchestrator itself and explicitly not suppressing invoked skills' own documented behavior. *(Reopened on independent grounds, not on the reverted revision's evidence: the requirement "The orchestrator never changes execution mode or model selection" predates that session and names execution mode explicitly, so this task's original wording under-specified its own requirement. The correction is re-derived from the requirement, and the `SKILL.md` wording it produced during the reverted revision is to be re-applied on that basis, not carried over.)*
- [x] 9.9 Cross-reference other skills by name with `**REQUIRED SUB-SKILL:**` markers, never `@` links, which force-load and burn context
- [x] 9.15 Create `references/source-resolution.md` covering canonical-source resolution from runtime input, the three-artifact validation and its before-first-write ordering, **the fail-closed stop where no source validates**, provenance capture including the **three-state** Git disposition of design D-S, checksum-based identity and the drift stop on resume, the two client-selection routes with the rule that read-only autodiscovery never authorizes configuration, and **the explicit-supported-client refusals** that precede every write. It is a **reference**, loaded on demand (design D-H), and it explains how the orchestrator drives the canonical rules rather than restating them as a second authority (design D-L). `SKILL.md` is **not** edited by this task: under the Iron Law a skill-body change needs its own RED baseline first, and this revision records none. *(Reopened, seventh revision: the reference describes a two-mode selection with a packaged-snapshot fallback, a two-state Git record, and no client refusal.)*
- [x] 9.10 Record the S-1…S-14 revalidation evidence in `reports/skill-tdd-green.md` — all fourteen baseline scenarios, no subset, run against the **final** `SKILL.md` as it stands after the last skill edit. The sweep protocol that produces this evidence is defined once, in task 12.2; do not restate or vary it here. **Only a complete fourteen-scenario sweep executed after the final skill edit may close this task.** A focused re-run of a previously failing scenario is not sufficient evidence: the thirteen scenarios not re-run describe a file that no longer exists. If that final sweep fails, this task stays open. Use the existing S-1…S-14 baseline; add no new scenarios. *(Reopened: the existing GREEN record was produced against an 816-word file that task 9.2 both deletes sections from and materially condenses, so it no longer describes the shipped skill. That report may keep its account of why only four of the fourteen originally required GREEN remediation — valid history about the earlier file — but it does not reduce the revalidation set here.)*

## 10. Skill Exposure and Symlink Maintenance

- [x] 10.1 Create `.claude/skills/specboot-adopt` and `.kiro/skills/specboot-adopt` as relative symlinks to `../../ai-specs/skills/specboot-adopt`, matching the existing convention
- [x] 10.2 Change `ai-specs/skills/sync-agent-symlinks/SKILL.md`'s mirror list from the hardcoded `.claude` + `.cursor` to the repository's selected clients, so `.kiro/` (present, currently unmanaged) is covered and `.cursor/` (absent) is not assumed
- [x] 10.3 Run the `sync-agent-symlinks` workflow and confirm it reports zero blockers
- [x] 10.4 Verify `find -L` over the adapter paths returns empty, and that no symlink name contains a space

## 11. Selected-Client Discovery-and-Execution Gate (BLOCKING for any client-support claim)

> **Client-neutral by design D-Q.** The gate below is the canonical, client-agnostic procedure; tasks 11.1-11.6 record the > **Codex recipe's** pass through it, which is why they name Codex. That evidence is the Codex recipe's, never the definition > of the gate. The same three parts apply to whichever client is declared `SELECTED` at `ADOPT-00`; a client that is not > selected is recorded `NOT SELECTED` and never runs this gate at all.

- [x] 11.1 Request explicit user authorization for external research before consulting official Codex documentation, per `00-conventions.md` §Scope boundaries
- [x] 11.2 Establish from current official Codex documentation what the project-scoped skill/instruction discovery mechanism actually is; record the source and the date consulted
- [x] 11.3 Build the candidate mechanism in an isolated throwaway repository — never in the repository under adoption
- [ ] 11.4 Run a genuinely fresh Codex session with the candidate present at session start, and prove two distinct things: the skill is **discovered** without being pasted or supplied after start, and its **canonical content executes**. Perform this in a genuinely fresh Codex task rooted at the **isolated throwaway test repository** the gate requires, never at the repository under adoption. *(Still open and deliberately unexecuted. The `codex` CLI being absent from `PATH` is not evidence the client is unavailable — Codex Desktop is a valid host for this gate. Codex remains `PENDING EVIDENCE` because the fresh-session test has not been observed, never because a CLI lookup failed. Per design D-Q this is a **client-specific deferred obligation on the Codex recipe**, carried forward to any future adoption that selects Codex; it does **not** block this change, because the pilot selects Claude only and records Codex `NOT SELECTED`. Task 11.8 records the deferral. This task is closed by evidence or not at all — it is never marked PASS on the strength of a run that did not select Codex.)*
- [x] 11.5 Record the verdict in `reports/codex-discovery-gate.md`; if any task above cannot be completed or verified, record Codex as `unavailable` or `PENDING EVIDENCE` — never PASS, never quietly omitted
- [x] 11.6 Confirm every artifact reporting client support reflects the recorded Codex verdict, and that no artifact claims Codex support without a passing gate
- [x] 11.7 State the gate **client-neutrally** in the canonical layer and record the per-client status semantics wherever client support is reported: `SELECTED + PASS` satisfies the gate; `SELECTED + PENDING EVIDENCE` **blocks** the adoption; `SELECTED + FAIL` **blocks** the adoption; `NOT SELECTED` provisions and validates nothing and **never blocks**. Confirm no artifact records an unselected client as `PENDING EVIDENCE`, and that no artifact reports a single client's PASS as universal or multi-client support
- [x] 11.8 Record the **Codex deferral** explicitly, in the artifacts that report client support and in the run-log client-selection block, on both axes and without collapsing them: **as a shipped recipe**, Codex is `PENDING EVIDENCE` — a standing obligation discharged only when 11.4's fresh-session test is observed; **as a client of this change's pilot**, Codex is `NOT SELECTED`, so it provisions nothing, validates nothing, and blocks nothing. Confirm no artifact records Codex as PASS, none reports Codex as supported, none reports the Claude-only pilot as blocked by it, and none quietly drops the obligation. This task closes on the recording, not on 11.4's evidence — leaving it dependent on 11.4 would make the change impossible to complete for a reason unrelated to what it delivers

## 12. REFACTOR and Structural Validation

- [x] 12.1 Create `references/rationalizations.md` from the verbatim RED rationalizations captured in 2.3, pairing each with its counter and its red flag. **Remove §"The inverse failure: stopping where the contract does not ask" in full**, including its counter table and its S-7 symmetry note, leaving only rationalizations derived from the valid RED evidence captured under 2.3 — S-4, S-5, S-7, S-13, and the meta-rationalization. *(Reopened: the reverted revision appended that section, which labelled itself "Evidence status: no RED baseline" and which this task's own sourcing rule excludes.)*
- [x] 12.2 Iterate the restored skill to PASS under this bounded protocol, which is the **single definition** of the revalidation sweep for both this task and 9.10: **(1)** complete 9.2 and 12.1 first — do not sweep intermediate wording while the text is still being condensed; **(2)** run one complete S-1…S-14 sweep against the candidate skill; **(3)** if all fourteen PASS, that **is** the final sweep — no further campaign is required, and 9.10 records it; **(4)** if any scenario fails, correct the skill, using focused re-runs of **the affected scenario only** while developing the correction; **(5)** after the final skill edit, run one complete S-1…S-14 sweep against the final file — **only this post-edit sweep may close this task and 9.10**; **(6)** never infer from a focused PASS that the scenarios you did not re-run are still valid, since they describe a superseded file. Normally this permits **at most two complete sweeps**: the candidate sweep and, only if corrections were required, the final post-edit sweep — do not run complete sweeps between intermediate edits. If the final sweep fails, both tasks stay open. Keep the existing S-1…S-14 baseline; add no scenarios. *(Reopened: the previous iteration closed against the 816-word file.)*
- [x] 12.3 Validate frontmatter with a strict YAML parse; verify the name charset, the trigger-only description, and both character limits
- [x] 12.4 Verify `wc -w SKILL.md` is under 500 words, every referenced path resolves, and no `@` force-load link exists. *(Reopened: measured 816 words. The under-500 requirement stands unchanged; the file is brought to it, not the reverse.)*

## 13. Portability and Integrity Validation

- [x] 13.1 Run a bootstrap dry-run for each client × OS × shell combination that can actually be exercised on this host (macOS with Bash and zsh), recording exact commands and exit codes
- [x] 13.2 Record every combination that cannot be exercised as `PENDING EVIDENCE` with its reason — all Windows/PowerShell rows, since no Windows host is available (evidence from 1.5); never record any as PASS
- [x] 13.7 Verify the two statuses are used on the correct axis: an unexercisable **OS/shell** combination for a **selected** client is `PENDING EVIDENCE` with its reason, while a client that was never selected is `NOT SELECTED` with no rows, no provisioned artifacts, and no blocking effect. Confirm no artifact conflates the two — `PENDING EVIDENCE` asserts intended support a run could not exercise, which is a claim nobody made about an unselected client
- [x] 13.3 State in every artifact that claims portability that Windows support is **intended, not validated**, and confirm no artifact asserts validated Windows portability
- [x] 13.4 Verify the `ADOPT-18` single-copy assertion: after de-bootstrap every canonical artifact exists exactly once as content, with client-visible instances as symlinks or recorded copies carrying their reason. **Additionally verify the container end state**: after every manifest entry is dispositioned, `.specboot/bootstrap/` is absent — not merely emptied — and `.specboot/adoption/BOOTSTRAP-MANIFEST.json` is still present and committed. *(Reopened: the first pass proved single-copy (0 duplicated artifacts across the fixture and this repository) but also reproduced the container defect, so the run predates the corrected procedure. Re-verify against the revised `10-debootstrap.md`; prior evidence is retained in `reports/debootstrap-single-copy.md`.)*
- [x] 13.5 Verify no adapter exists for an unselected client and none for a client left unverified by its gate
- [x] 13.8 Verify the **external source is unmodified** by a full source-linked run: checksum every file in a fixture canonical source before `ADOPT-00`, run the adoption through `ADOPT-18`, and confirm the source is byte-identical file for file. Confirm separately that no manifest entry describes the source or any path inside it, so the guarantee rests on the inventory rule rather than on a promise
- [x] 13.9 Verify the **source-linked end state**: `.specboot/bootstrap/` was never created at any point in the run (not merely absent at the end), the payload and container obligations are recorded `SKIPPED — source-linked mode`, `.specboot/adoption/BOOTSTRAP-MANIFEST.json` is present and committed with terminal dispositions on every entry, and no copied guide, phase file, or skill body exists anywhere in the project
- [x] 13.10 Verify the **no-symlink fail-closed behavior**: where symlink creation fails and the selected client's recipe offers no native discovery mechanism that works without one, confirm the run stops **before provisioning**, reports the capability limitation naming the client, the mechanism, and what was attempted, and leaves the repository byte-for-byte unchanged. Confirm no run produced a content copy of the guide or skill, **and no run recorded a `mode: pointer-file` entry as a discovery entry** — a plain pointer file naming an external path is not a discovered skill. *(Reopened, seventh revision: the recorded validation certifies the superseded pointer-file fallback as a passing outcome.)*
- [x] 13.11 Verify **no source-linked discovery entry was staged or committed, and no absolute external path reached the committed tree**, at any checkpoint: inspect each checkpoint's staged file list and confirm the machine-specific entries are absent from all of them; that the committed tree — `BOOTSTRAP-MANIFEST.json` and the adoption run log included — contains no absolute external path; and that the machine-local `.specboot/local/` store is reported ignored by `git check-ignore` and appears in no staged file list. *(Reopened, seventh revision: clause 1 passed and clause 2 was unblocked by design D-S option B, but the re-verification was never run against the corrected artifacts, and the sweep of task 15.10 changes what the committed tree says. Re-verify once 3.1, 3.2, 3.7, 7.12, 9.5, 9.15, 14.1, and 14.2 land; prior evidence is retained in `reports/source-linked-integrity.md`, whose own option-A statements at lines 87 and 92 are corrected by 15.10.)*
- [x] 13.6 Verify the container **refusal** case: in a throwaway fixture, bootstrap, process every manifest entry to a terminal disposition, then place one unrecorded file inside `.specboot/bootstrap/` and run the container step. It must record **FAIL** and stop for reconciliation, removing neither the container nor the unrecorded file. This is the negative half of 13.4 — without it, an implementation that unconditionally `rm -rf`s the container passes 13.4 while reintroducing the exact pattern-based cleanup design D-C rejects
- [x] 13.12 Verify the **replacement-before-removal refusal** end to end, as the positive half's missing negative: bootstrap a throwaway fixture, register an entry with an `intended-permanent-replacement` that does not resolve, run `debootstrap`, and confirm the step **refused** rather than cleaned up — the entry is still present and resolving, its manifest record carries no terminal successful disposition, the durable manifest and run log are intact, the reconciliation report names the entry and the missing replacement, and no unrelated path was removed. Then confirm the **resume** half: repair the replacement, re-run, and confirm the step now completes and the previously refused entry reaches a terminal disposition — proving the refusal left recoverable state rather than a dead end. This is bounded to this refusal: do **not** re-run task 13.4's single-copy assertion or 13.6's container-refusal case, whose evidence is unaffected

## 14. Documentation Updates

- [x] 14.1 Update `ai-specs/specboot-instructions.md` with the bootstrap entry point and an installer-scope note stating that bootstrap exposure is temporary and is not permanent client provisioning. **Disclose source-linked delivery as the only mode**, that a validated canonical source and an explicitly selected supported client are both required, and that the command **fails closed with zero target-repository writes** when either is absent. State that the canonical source location is runtime input. **Correct line 442**, which carries the superseded D-S option-A sentence. *(Reopened, seventh revision: the delivered text documents two delivery modes and repeats the option-A wording.)*
- [x] 14.2 Update `README.md` with the bootstrap quick start, preserving the existing Claude/Cursor-provisioned and Kiro-not-provisioned disclosure. **Show the source-linked quick start as the only path**, with the source and the client both supplied as arguments, and state what happens when either is omitted — a clean refusal with nothing written. Remove the packaged-snapshot quick start. Record **no machine-specific path** in the example, and remove any option-A statement about the resolved path being recorded in run evidence. *(Reopened, seventh revision: the quick start still documents packaged-snapshot as a fallback invocation.)*
- [x] 14.3 Bring `packages/specboot/template/ai-specs/specboot-instructions.md` back into sync with `ai-specs/specboot-instructions.md`, so the two are **byte-identical except for the intentionally different image URL**, and verify it mechanically with a normalized diff that masks only that URL. *(Reopened, seventh revision: this was a **false green**. The live file carries a 33-line "Bootstrap entry point" section — entry prompt, delivery table, source-validation rules, and the temporary-exposure note — that the template does not have at all: the live file is 450 lines, the template 417, and the normalized diff shows the whole section missing. A project reading only the distributed template is told nothing about how to start an adoption. Re-sync **after** 14.1 lands, so the template inherits the corrected single-mode text rather than the superseded two-mode text.)*
- [x] 14.4 Verify no root instruction symlink (`CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `codex.md`) was edited directly — edits go to the canonical `docs/base-standards.md` only

## 15. Mechanical Verification Suite

- [x] 15.1 Verify every `ADOPT` step in the amended contract still carries all eight contract fields, exactly one `Action` form, and a resolvable `On failure` form (A, B, or C)
- [x] 15.2 Verify every cross-reference introduced by this change resolves, including the new phase files, the troubleshooting entries, the section-number map rows, every invocation of the shared checkpoint protocol, and the source-resolution references. **Additionally grep every canonical artifact for absolute filesystem paths** and confirm none records a machine-specific canonical source location — including `/Users/landaeta/repos/specboot`, which this revision records in the OpenSpec planning artifacts as pilot evidence and which must appear in **no** canonical artifact. *(Reopened, seventh revision: the sweep of 15.10 and the rewrites in groups 3, 6, 7, 9, and 14 change what is cross-referenced, and the pilot-source path is a new value the grep must exclude.)*
- [x] 15.3 Verify no existing `ADOPT-01`…`ADOPT-17` identifier changed number or meaning, and that `ADOPT-17` still follows `ADOPT-16` with its precondition intact
- [x] 15.4 Run the drift check and confirm it passes for the kit payload and warns (does not fail) for the pre-existing template drift
- [x] 15.5 Run `openspec validate add-specboot-adoption-orchestrator --strict` and confirm it passes. *(Reopened, seventh revision: the planning artifacts changed again and must be revalidated after implementation.)*
- [x] 15.6 Verify `ADOPTION-ENTRY-PROMPT.md` is **consistent with the contract it invokes**: check each of its statements against `ADOPT-00` (source resolution and the fail-closed stop, the explicit-supported-client refusals, the preflight and exact mutation inventory, the pre-write approval gate), `ADOPT-18` (cleanup, the `SKIPPED — source-linked mode` outcome, and removal of the machine-local `.specboot/local/` store), the manifest schema (the run-level `source` block carrying portable identity, **no** resolved path, and the **three-state** Git disposition; and the client-selection record with no placeholder), the selected-client discovery-and-execution gate including the **fail-closed no-symlink** answer, and the source-drift rules including how a resume acquires a local source path. Any divergence is corrected **in the prompt**, never by changing the contract to match it. *(Reopened, seventh revision: the recorded consistency check certifies statements the contract no longer makes — its own table lists "Source-linked preferred; packaged-snapshot fallback" as *consistent* — so it is evidence for a superseded contract.)*
- [x] 15.7 Verify the prompt's **coverage** mechanically, recording the located evidence for each: the single source-linked path is reachable and no delivery-mode question exists; both client-selection routes are offered; the no-write/no-authorization property of autodiscovery is stated; **all four fail-closed refusals of 3.19 are stated**; the preflight and the exact mutation inventory are named; the generated fresh-session handoff prompts are named; and the resume path is described. *(Reopened, seventh revision: the recorded coverage check asserts that both delivery modes are reachable from the prompt, which is now the opposite of the requirement.)*
- [x] 15.8 Verify the prompt's **cold-start contract** against a fixture repository holding no SpecBoot files, no OpenSpec, no `/opsx:*` commands, no discoverable `specboot-adopt` skill, and no `.specboot/`: confirm every instruction in the prompt is executable in that state; that **all seven spine steps of 3.10 are performable from what the prompt states**, with no step requiring an instruction the operator had to supply; that the source question and the three-artifact validation precede every orchestration load and every write; that a deliberately invalid source, an absent source, a missing client, and an unknown client each leave the fixture tree **byte-for-byte unchanged**; and that the source-relative read of `SKILL.md` is stated as a direct read and nowhere described or recorded as native skill discovery. *(Reopened, seventh revision: the recorded check was taken against the two-mode prompt and certifies a cold start whose first question no longer exists; the executable-spine and refusal clauses were never checked at all.)*
- [x] 15.9 Verify the prompt's **OpenSpec embargo and handoff boundary**: grep every `openspec` and `/opsx:` mention in the prompt and confirm each is gated behind `ADOPT-02` completion plus the explicit availability check; and confirm the prompt names the stop point after provisioning, the generated fresh-session handoff prompt, the fresh session as the only place native discovery is attempted, and the resume-from-manifest-and-run-log path with its drift block. Confirm the cold-start sequence is stated **once**, for the one delivered mode, with no mode branch. *(Reopened, seventh revision: the recorded check certifies a sequence "stated once and serving both delivery modes", a property that no longer describes the prompt.)*
- [x] 15.10 **Sweep out the superseded D-S option-A wording**, in one pass across every non-historical artifact. The rule it violates: a resolved source path exists **only** in ignored `.specboot/local/` state — never in the committed manifest, the committed run log, the handoff prompt, or any other evidence — and identity is the checksums. Grep for the option-A phrasings ("recorded only in that run's evidence", "labelled machine-specific and non-portable", "the resolved path appears only in") and correct every occurrence in: `SPECBOOT_ADOPTION_GUIDE.md` (lines 58-59); `specboot-adoption/bootstrap-kit/README.md` (63-64); `specboot-adoption/bootstrap-kit/manifest.json` (the `$comment` and the `recordedOnlyIn` value); **both** source-resolution references — `specboot-adoption/bootstrap-kit/source-resolution.md` (12-13) and `ai-specs/skills/specboot-adopt/references/source-resolution.md`; `ai-specs/skills/specboot-adopt/references/bootstrap-and-debootstrap.md`; `README.md`; `ai-specs/specboot-instructions.md` (442) and its template counterpart; the delta specs; this task list; and every **non-historical** report, including `reports/source-linked-integrity.md` (87, 92) and `reports/entry-prompt-verification.md`. **Historical RED/GREEN records are not rewritten** — they describe what was true when they were written. Record the final grep with a zero count over the non-historical set
- [x] 15.11 Verify **no active artifact offers packaged-snapshot**: grep the guide, the phase files, the kit (README, manifest, schema, recipes, entry prompt), the skill references, `README.md`, both `specboot-instructions` files, and the delta specs, and confirm every surviving mention is explicitly labelled as **deferred to `add-specboot-packaged-snapshot-delivery`** or as retained installer machinery, and that none presents it as a mode an operator can select. Confirm the CLI exposes no reachable packaged-snapshot path (task 5.24). Confirm separately that the **historical** RED/GREEN reports and the 4.21 baseline are **unmodified**, so the deferral preserved the evidence rather than erasing it

## 16. Review and Update Existing Unit Tests (MANDATORY)

- [x] 16.1 Review the installer test suite against the delta specs' installer requirements, confirming each scenario in the modified `specboot-verification-workflow` requirements and in the added `specboot-adoption-orchestration` requirements has a corresponding assertion. **Extend the review to task 4.32** and confirm the *De-bootstrap reconciles against the committed manifest* requirement's **replacement-before-removal** scenario — *Permanent replacement is missing* — now has an executable assertion behind it rather than a written rule alone. Confirm the retirements recorded under 4.30 still leave no requirement without coverage. *(Reopened, eighth revision: the previous review recorded this requirement as covered by the de-bootstrap blocks in `delivery-modes.test.js`, which walk the success path only. Its refusal scenario had no assertion at all — a written rule with nothing executing it is exactly what this review exists to catch.)*
- [x] 16.2 Review the existing Java test suite under `src/test/` for any test asserting on documentation paths, skill paths, or installer behavior affected by this change, and record the outcome with evidence (`git diff --name-only` showing no `src/main` or `src/test` path)
- [x] 16.3 Confirm the installer suite runs from a documented command (`npm test` in `packages/specboot/`) and that its RED and GREEN records both exist in `reports/`

## 17. Run Unit Tests and Verify Database State (MANDATORY)

- [x] 17.1 Capture the pre-change baseline: current branch, `git status --short`, and the last known baseline result
- [x] 17.2 Run `mvn -o test` and record exit code plus test counts
- [x] 17.3 Run `npm test` in `packages/specboot/` and record exit code plus test counts, so both suites appear in the same report. *(Reopened, seventh revision: the suite gains 4.24-4.30 and retires the packaged-snapshot assertions, so it must be re-run.)*
- [x] 17.4 Verify database state: this repository's suite uses no external database and this change touches no persistence code or `docs/data-model.md` — record the pre/post indicators as N/A with that justification rather than omitting the check
- [x] 17.5 Create `reports/YYYY-MM-DD-step-17-unit-test-and-db-verification.md` following the template in `docs/openspec-tasks-mandatory-steps.md`, including executed commands, both suites' results, and the recorded non-applicability. The Java result from 17.2 may be carried forward only with evidence that no `src/` path changed in this revision; the npm result must be re-executed. *(Reopened, seventh revision: the npm suite changed, so the report no longer describes the code it reports on.)*
- [x] 17.6 Mark this group complete only after both suites pass and the report file physically exists. *(Reopened, seventh revision: reopened with 17.3 and 17.5.)*

## 18. Manual Endpoint Testing with curl (MANDATORY — recorded non-applicability)

- [x] 18.1 Record explicitly that this change adds, removes, and modifies no HTTP endpoint: `docs/api-spec.yml` is untouched and no controller source appears in the diff — evidenced by `git diff --name-only`
- [x] 18.2 Confirm no curl testing is required, stating the evidence rather than silently omitting the group

## 19. E2E Testing with Playwright MCP (MANDATORY if applicable — recorded non-applicability)

- [x] 19.1 Record that Playwright E2E does not apply because this repository is backend-only (`docs/frontend-standards.md` — Not Applicable) and this change introduces no frontend, no browser interface, and no browser-mediated behavior for Playwright to drive. This change **does** introduce a user-facing workflow — the `npx @lidr/lidr-specboot bootstrap` CLI — but a CLI is not a Playwright target: it is covered by the installer `node:test` suite (groups 4 and 5) and by the end-to-end adoption validation (group 21)
- [x] 19.2 Confirm the non-applicability with evidence rather than silently omitting the group

## 20. Update Technical Documentation (MANDATORY)

- [x] 20.1 Confirm `docs/data-model.md` and `docs/api-spec.yml` need no update (no data model or API contract change) — state the evidence
- [x] 20.2 Confirm whether any `*-standards.md` file needs a stack/tooling update for the installer's new subcommand, its new test suite and `scripts.test` entry, and the changed symlink-maintenance scope; update it if so, and state the evidence if not
- [x] 20.3 Verify `docs/documentation-standards.md` conventions are followed by every new document created in groups 3, 6, and 9

## 21. End-to-End Adoption Validation (§9.5)

> **This group stays open and unexecuted until every correction above is implemented.**
> The pilot is **source-linked and Claude-only**, against a canonical SpecBoot source supplied at run
> time. That path is runtime input: it appears in no task, recipe, phase file, or schema, and it is
> **never committed** — no manifest, run log, or handoff prompt records it, and any retained path
> lives only in ignored `.specboot/local/` state. **Task 21.18 is a precondition of the whole group**
> and is performed before 21.1.

- [ ] 21.18 **PRECONDITION — validate the shared canonical source before the pilot starts.** *(Blocked upstream, eighth revision: this source **cannot be created yet**. It must byte-match this repository's **committed** guide, phase files, and skill, and none of that is committed — `HEAD` is `c900394104ca1be31d59684d31b2b2aa63fbd2f1` and the implementation is a working-tree modification. Creating it from the committed state would publish the pre-adoption contract; creating it from the working tree would publish the `ADOPT-18` defect tasks 4.32/5.27 correct. Creation is authorized only after that defect is corrected, validated, explicitly approved, and **committed**.)* The pilot links to an external source, so that source is part of what the pilot proves. Confirm both: it is a **Git working tree** (otherwise the run can only ever exercise the `unavailable` provenance path and design D-S's honest-provenance rule goes untested), and its `SPECBOOT_ADOPTION_GUIDE.md`, `specboot-adoption/` phase files, and `ai-specs/skills/specboot-adopt/SKILL.md` **byte-match** the final committed versions in this change, verified by checksum file for file. **`/Users/landaeta/repos/specboot` fails both today** — it has no `.git/`, its guide differs from this repository's committed guide, and its kit is missing `ADOPTION-ENTRY-PROMPT.md`, `source-resolution.md`, and `client-autodiscovery.md` — so **it must not be used for the pilot**. Record the source actually used, its checksums, and its Git commit in the run evidence. A pilot run against a stale source validates instructions that were superseded before the run began
- [ ] 21.1 Perform one complete adoption run in a genuinely clean repository, producing a filled run log at `.specboot/adoption/ADOPTION-RUN-LOG.md`. **Run it source-linked** against the canonical source validated by 21.18 and supplied at `ADOPT-00`, recording the source identity in the run evidence. **This pilot is Claude-only and controlled** (design D-Q): declare **Claude** as the sole `SELECTED` client at `ADOPT-00`, and record **Kiro and Codex as `NOT SELECTED`** — not `PENDING EVIDENCE`, since neither was chosen and neither is owed evidence by this run. The run validates the **canonical client-agnostic process** plus the **Claude recipe**, and nothing else; no Kiro or Codex artifact is provisioned or validated, and neither blocks the run
- [ ] 21.2 Confirm the run produced one commit per validated checkpoint, or per justified group with its written justification recorded in the ledger
- [ ] 21.3 Confirm every checkpoint in the run invoked the shared `00-conventions.md` protocol, including checkpoints reached long before `ADOPT-16`, and that both approvals were recorded separately for each
- [ ] 21.4 Confirm `BOOTSTRAP-MANIFEST.json` is committed with every entry at a terminal `cleanup-status` and `final-disposition`, and every `intended-permanent-replacement` resolving
- [ ] 21.5 Confirm `ADOPT-05B` executed as specified during the run: the approved generic baseline was located; it was copied when absent and **merged** when present, never overwritten; the supported-environment matrix (clients / stacks / shells / OSes) was declared; reconciliation removed out-of-matrix entries while **retaining** supported variants absent from this machine; the safety scan (credentials, home directories, machine paths, unsafe broad patterns) and the syntax check both passed before any permission file was committed; and the generic source baseline was left unchanged
- [ ] 21.6 Confirm the status axes were kept distinct throughout the run: every **OS/shell** combination that could not be smoke-tested **for the selected client** is `PENDING EVIDENCE` with its reason — never PASS, never silently dropped, with the Windows rows in particular carrying that state per task 13.2 — while **Kiro and Codex carry `NOT SELECTED`** throughout, with no rows, no artifacts, and no blocking effect
- [ ] 21.7 Confirm the `ADOPT-19` pilot passed on a real, human-chosen project task through the full six-capability workflow, and that `ADOPT-20` blocked pull-request creation until `ADOPT-00`…`ADOPT-19` were all PASS on recorded evidence
- [ ] 21.11 Confirm the pilot's conclusion is scoped to what it proved: the canonical process is validated, **Claude** is recorded PASS, and no artifact reports or implies Kiro or Codex support on the strength of this run. A PASS proves support for the client that produced it and no other
- [ ] 21.8 Confirm `ADOPT-18` and `ADOPT-19` each produced their own checkpoint under the shared protocol, with both approvals recorded separately — `ADOPT-17` being the adoption's final checkpoint did not exempt them
- [ ] 21.9 Confirm no file under any client settings path had its `model`, reasoning, or permission-mode setting modified during the run
- [ ] 21.10 Record the improvement proposals raised across checkpoints **in the project's run log** and file the single batching follow-up change for those accepted **against the canonical SpecBoot source** (design D-K, D-U); do not apply any of them silently in this change, do not edit any local copy of the guide, and confirm the pilot run itself did not switch to the improved instructions mid-execution
- [ ] 21.12 Confirm the run recorded complete **portable source provenance**: the delivery mode, the guide and skill checksums, either the source Git commit or an explicit unavailable record with its reason, and the explicit statement that the local source path is resolved per machine. Confirm **no resolved absolute path appears in the committed manifest or the committed run log**, and that any path retained for same-machine resume lives only in ignored `.specboot/local/` state. Where the supplied source is not a Git working tree, confirm the unavailable record carries that reason and that **no commit value was inferred**
- [ ] 21.13 Confirm the source-linked end state of the run: no `.specboot/bootstrap/` was created at any point, no copied guide, phase file, or skill body exists in the project, the payload and container obligations are recorded `SKIPPED — source-linked mode`, and the **external canonical source is byte-identical** to its pre-run state
- [ ] 21.14 Confirm the **client-selection route** used is recorded, that any autodiscovery performed wrote nothing before the human selected, that only Claude was provisioned, and that Kiro and Codex are recorded `NOT SELECTED` with no artifacts
- [ ] 21.15 Confirm **no source-linked discovery entry reached a commit**: inspect every checkpoint's staged file list and the resulting commits, and confirm no machine-specific absolute path was committed at any point in the run
- [ ] 21.16 Confirm the pilot was started from `ADOPTION-ENTRY-PROMPT.md` **verbatim, as the sole initial prompt**, with no additional operator instruction. Record every corrective prompt patch the run required; **any patch is a FAIL of this task**, is recorded as a gap in the entry prompt, and is raised as an improvement proposal under design D-U rather than absorbed as normal use. This is the only task that proves the prompt is sufficient in practice rather than by inspection
- [ ] 21.17 Confirm the pilot exercised the **cold start** as specified, recording evidence for each: at the moment the entry prompt was pasted the target repository had no SpecBoot files, no OpenSpec, no `/opsx:*` commands, no discoverable `specboot-adopt` skill, and no `.specboot/`; the initial session obtained its procedure by a **source-relative direct read** of the validated canonical `SKILL.md`, recorded as a direct read rather than as discovery; the exact project mutations were presented and approved before provisioning; the initial session **stopped** after provisioning and emitted the fresh-session handoff prompt; the first native skill discovery occurred in that fresh session; **no OpenSpec or `/opsx:*` command was used** before `ADOPT-02` completed and its availability check passed; and the fresh session resumed from the durable manifest and run log with source identity verified
- [ ] 21.19 Confirm the pilot exercised the **preflight and the refusals** on the real path, not only in fixtures: the preflight ran before the first write and its findings are recorded; the exact mutation inventory was presented at the approval gate and the run performed exactly that inventory; and the run log records, for each refusal the pilot happened to hit, that the repository was left byte-for-byte unchanged. Where the pilot hit none, record that explicitly rather than leaving the check blank — an unexercised refusal is `PENDING EVIDENCE` for that path, never an inferred PASS
- [ ] 21.20 Confirm the pilot's **source provenance disposition matches the source's actual state**: a clean Git working tree recorded its commit, and neither an inferred commit nor a bare HEAD for a dirty tree appears anywhere in the committed manifest or run log. Confirm the guide and skill checksums match the source validated by 21.18
- [ ] 21.21 File the deferred follow-up change **`add-specboot-packaged-snapshot-delivery`** with the scope design D-W enumerates — entry-prompt distribution, read-only bootstrap planning, explicit client selection, actual client discovery entries, non-null payload identity, verified resume, its own answer where symlinks are unavailable, and end-to-end cold-start evidence — and link the preserved historical RED/GREEN records and the 4.21 baseline to it as prior evidence. File it as a separate OpenSpec change; do **not** implement any of it here, and do not archive this change with the deferral recorded only in prose

## Notes (not tracked tasks)

- Installer GREEN is defined by design **D-O**: order-independent, first-invocation pass from both a clean checkout and a stale payload, per-test fixture assembly, and no test repairing state for another. A suite that passes only on a second run is not evidence.
- **Group 4's execution order is 4.11 → 4.12 → 4.2 → 4.5 → 4.6 → 4.9 → 4.10 → 5.8 → 5.9, not task-ID order.** 4.2 is blocked until 4.11 and 4.12 are complete: 4.2 begins the correction, and reproducing the defect must precede correcting it.
- Groups 2 and 9 are ordered by the `writing-skills` Iron Law: the RED baseline must be recorded before `SKILL.md` exists. Groups 4 and 5 apply the same discipline to the installer (design D-M). Reordering either pair invalidates its campaign.
- Group 7 is BREAKING and must land together with group 9 — the amendments are what make the skill's push and code-graph behavior legal.
- The checkpoint protocol has exactly one normative home: `00-conventions.md` (task 7.2). `ADOPT-17` invokes it (7.4) and the skill reference explains driving it (9.4); neither restates it.
- Group 11 gates only what may be *claimed* about Codex; it does not block implementation of anything else. Task 11.4 stays **unexecuted**, and task 11.8 records the deferral on both axes — `PENDING EVIDENCE` as a shipped recipe, `NOT SELECTED` for the Claude-only pilot — so the change can complete without either claiming Codex support or pretending the obligation was discharged.
- **The entry prompt is written last within group 3** (3.10-3.18, design D-V). It refers to the source-resolution recipe, the autodiscovery recipe, and the manifest schema, so writing it before those exist would mean writing it against intentions rather than against artifacts. It is verified in 15.6-15.9 and proven sufficient only by 21.16 and 21.17, where a corrective prompt patch is a FAIL rather than a workaround.
- **The cold-start contract binds the prompt's opening, not the guide's steps** (3.14-3.18, design D-V). The prompt runs in a repository that has none of the tooling the adoption installs, so it is ordinary prompt text, it validates the source before it loads anything, it reads the validated canonical `SKILL.md` by source-relative path rather than discovering a skill, and it touches no OpenSpec or `/opsx:*` command until `ADOPT-02` has passed its availability check. The initial session ends at the handoff by design: discovery can only be evidenced by a session that started after the discovery entries existed.
- **Implementation order for the seventh revision**: group 3 (schema 3.3, recipes 3.5 and 3.7, kit README and manifest 3.1-3.2, then the entry prompt 3.10, 3.13, 3.15, 3.16, 3.18, 3.19) → group 4 RED (4.24 → 4.25 → 4.26 → 4.27 → 4.28 → 4.30 → 4.29 → 4.31) → group 5 GREEN (5.20 → 5.21 → 5.22 → 5.23 → 5.24 → 5.25 → 5.26) → group 6 phase files (6.1, 6.2, 6.7) → group 7 guide and troubleshooting (7.12, 7.14) → group 8 run-log blocks (8.1, 8.8) → group 9 references (9.5, 9.15) → group 14 documentation (14.1 → 14.2 → 14.3, in that order, since the template inherits the corrected live text) → the option-A sweep and the packaged-snapshot audit (15.10, 15.11) → the remaining verification (13.10, 13.11, 15.2, 15.6-15.9, 16.1, 17.3-17.6, 15.5) → group 21, with **21.18 first**.
- **4.29 is written last within the RED campaign and passes last.** It is the end-to-end cold start, so it exercises every other correction; writing it first would produce a failure whose cause is any of six things.
- **The historical RED/GREEN records are never rewritten.** 4.30 retires the packaged-snapshot assertions from the active suite and records why; `reports/installer-tdd-red-baseline.md` and `reports/installer-tdd-green.md` keep their accounts of what was true when they were written, and 15.11 verifies they were not edited.
- **The guide-wide and S-1…S-14 campaigns are not rerun.** `SKILL.md` is not edited by this revision — the delivery rules land in `references/source-resolution.md` and `references/bootstrap-and-debootstrap.md` — so under the Iron Law there is no skill-body change needing a RED baseline, and groups 2, 12, and the unaffected tasks in group 9 stay closed. Only the verification tasks whose recorded evidence describes a superseded artifact are reopened.
- The canonical source path is **runtime input**, and it is also **never committed**. No task, recipe, phase file, schema, or document records a machine-specific path (design D-R), and no committed run artifact records one either — not the manifest, not the run log, not the handoff prompt (design D-S option B). A resolved path exists only as machine-local, git-ignored state under `.specboot/local/`, which `ADOPT-18` removes; identity is carried entirely by the recorded checksums, plus the Git commit where the source is a **clean** working tree. The one place `/Users/landaeta/repos/specboot` appears is in these OpenSpec planning artifacts, as this change's pilot evidence that the copy is unusable — task 15.2 confirms it reaches no canonical artifact.
- `SKILL.md` is **not** edited by this revision or the previous one. The delivery rules land in `references/source-resolution.md` and `references/bootstrap-and-debootstrap.md`, because under the Iron Law a skill-body change needs its own RED baseline first and neither revision records one — the same rule that forced the second revision's rollback.
- **The eighth revision's chain is 4.32 → 5.27 → 5.26 → 13.12 → 16.1, and nothing else.** RED before GREEN is binding: 4.32 must be observed failing against the current `runDebootstrap()` — which never reads `intended-permanent-replacement` — before 5.27 adds the gate. 13.12 is the negative half of 13.4's single-copy assertion and does not re-run it; 16.1 re-reviews coverage because the suite gained an assertion. No other group is reopened: the governing semantics were already correct, so `proposal.md`, `design.md`, and both delta specs are untouched.
- **Committing is a precondition of the pilot, not a step within it.** Task 21.18 needs a canonical source byte-matching the **committed** artifacts, and the implementation is uncommitted at `c900394`. The order is: correct the defect → validate → explicit human approval → commit → create the canonical source → 21.18 → the pilot. Skipping ahead publishes either the pre-adoption contract or an uncorrected defect.
- **Packaged-snapshot is deferred, not abandoned.** Task 21.21 files `add-specboot-packaged-snapshot-delivery` with the scope design D-W enumerates. A deferral recorded only in prose is how scope quietly disappears; the filed change is what makes it recoverable.
- Any Windows or PowerShell result that cannot actually be produced stays `PENDING EVIDENCE`, never PASS.